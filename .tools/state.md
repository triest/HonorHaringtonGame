# Honorverse dev state (updated every pass)
Phase: §56.1 vertical slice (fast visible-result mode)
Checklist:
  [x] 1. Camera (OrbitCamera, scripts/orbit_camera.gd)
  [~] 2. Placeholder ship geometry (hull+wedge done; sidewall panels done; canon shapes/materials explicitly OUT of scope for this slice)
  [~] 3. Weapon-fire visualization (energy/missile) -- rendering capability DONE and now VERIFIED LIVE for energy beams this pass (demo ships actually fire on each other; WeaponFx spawns real beam meshes in a headless run of main.tscn, confirmed both by a throwaway direct-tick script and by the extra mesh-instance count in a headless main.tscn run). Missile markers still only verified by unit test, NOT live, because the demo scenario (item 6, below) intentionally carries no missile tubes (energy-only weapon mounts, kept simplest per item 6's own instruction) -- this is a scenario-configuration gap, not a WeaponFx capability gap; not reopening item 3 to chase it right now.
  [ ] 4. Minimal HUD (subsystems/targets/contacts state) <- CURRENT
  [ ] 5. Order input wiring (hotkeys + Input Map -> FormationOrder/IndividualOrder/ShipCombatDirective)
  [x] 6. Hardcoded 1v1 scenario vs TacticalAI (world.set_team("alpha","red")/world.set_team("beta","blue"), weapon mounts registered directly on `world` via world.add_weapon_mount() -- not main.gd's own separate dict anymore -- so SimulationWorld._resolve_weapons_ai actually selects a target and fires each tick. Both sides are driven by the existing TacticalAI.select_weapon_target fallback inside _resolve_weapon_target; no separate "AI class" instantiation was needed since there is no player-controlled side yet (that's item 5). Only did 1v1, not 2v2 -- minimally sufficient, matches "keep it the SIMPLEST hardcoded setup" instruction. BUG FOUND+FIXED this pass: mounts were initially WeaponMount.bow_chaser_arc(), which never finds arc given the demo scene's actual start geometry (both ships end up seeing the other on their STARBOARD per AttackGeometry.classify(), never BOW, since neither ship turns to face the other) -- switched to WeaponMount.broadside_arc(). Verified via a throwaway, NOT committed, headless SceneTree script that replicated main.gd's wiring and called world.tick_simulation() directly: first shot at tick 1 (t=0.017s), 6 shots over 10 simulated seconds (bounded by recharge_time_s=4.0); with the original bow_chaser_arc() the same script showed 0 shots in 10s, confirming the bug before the fix. Also verified via headless `--import` (clean) and a headless run of res://scenes/main.tscn --quit-after 120: 0 new error/warning types; the already-documented dummy-renderer mesh_get_surface_count/"Parameter m is null" artifact count rose from the item-3-pass baseline of 14 to 16 -- +2, matching the 2 new beam meshes actually spawned by real weapon fire in the first ~2s of a live run, not a new bug.)
  [ ] 7. Win/lose screen (existing §63 destruction state)
  [ ] 8. Draft Windows export
Next concrete step: item 4 (Minimal HUD) -- per AGENTS.md §56.1 item 4's exact text: "A minimal HUD, §25.1-compliant (no health bars): a plain readout of per-ship subsystem condition (e.g. text list 'SENSORS 40%'), current target designation, and the sensor contact list -- pull this directly from existing ShipSubsystems/SensorContact/SimulationWorld state, invent no new game-design mechanic to support it." Concretely: new scripts/hud.gd (a CanvasLayer + Label node(s), plain text, no bars/icons/graphics), instanced in main.gd alongside weapon_fx, updated from _on_tick by reading world.ships[ship_id].subsystems.get_condition(SubsystemType.Type.X) per subsystem type, the current target designation (ship_combat_directives.get(ship_id)?.manual_target_ship_id, or the AI's live auto-selected target if no manual one is set -- check what's actually readable/meaningful here), and world.sensor_contacts[ship_id] (contact id + state, e.g. "beta: TRACKED"). Ships have no ShipSubsystems assigned yet in main.gd (alpha/beta only get .defense = ShipDefenseState.new(), no .subsystems) -- check whether HUD should read subsystems=null gracefully (show "N/A") or whether main.gd should also start assigning ShipSubsystems to the demo ships as part of this HUD work (leaning toward the latter, since "per-ship subsystem condition" is meaningless with subsystems always null -- but keep it minimal, don't build out a full subsystem-damage demo). Pick ONE ship (e.g. "alpha") as the HUD's point of view for this first slice unless showing both is trivial.
Blockers: none currently.
Last pass finished: 2026-09-22T23:36Z (item 6 done: hardcoded 1v1 scenario, teams+mounts wired onto world, bow-chaser->broadside arc bugfix, live-verified via headless run; pushed this pass)
