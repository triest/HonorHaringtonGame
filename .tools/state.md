# Honorverse dev state (updated every pass)
Phase: §56.1 vertical slice (fast visible-result mode)
Checklist:
  [x] 1. Camera (OrbitCamera, scripts/orbit_camera.gd)
  [~] 2. Placeholder ship geometry (hull+wedge done; sidewall panels done; canon shapes/materials explicitly OUT of scope for this slice)
  [~] 3. Weapon-fire visualization (energy/missile) -- rendering capability DONE and VERIFIED LIVE for energy beams (demo ships actually fire on each other in a headless run of main.tscn). Missile markers only verified by unit test, NOT live, because the demo scenario carries no missile tubes (energy-only weapon mounts, kept simplest per item 6). Scenario-configuration gap, not a WeaponFx capability gap; not reopening.
  [x] 4. Minimal HUD (subsystems/target/contacts state) -- scripts/hud.gd, wired to alpha, live-verified via headless run (see CHANGELOG.md for detail).
  [x] 5. Order input wiring (hotkeys + Input Map -> FormationOrder/IndividualOrder/ShipCombatDirective) -- new scripts/player_input.gd (PlayerInput), instanced in main.gd alongside hud/weapon_fx. Added a real Input Map to project.godot (order_select_ship=TAB, order_target_next=T, order_target_clear=C, order_weapons_toggle=F, order_turn_left/right=A/D, order_speed_up/down=W/S) written via a throwaway headless ProjectSettings script (not committed) so the InputEventKey resources are exactly what Godot itself serializes, then hand-verified project.godot still had its pre-existing physics_ticks_per_second/rendering_method settings intact (ProjectSettings.save() silently drops settings that equal engine defaults -- both got dropped and were restored explicitly). PlayerInput reads these actions in _unhandled_input and calls DIRECTLY into SimulationWorld's existing `transmit_*` order API (transmit_individual_order_now for course/speed via IndividualOrder.change_course/change_speed, transmit_ship_target/transmit_clear_ship_target for target designation over the ship's own live world.sensor_contacts, transmit_ship_weapons_free for weapons free/hold) -- the `transmit_*` family specifically (not the instant issue_*/set_* one) because SimulationWorld's own doc comment says that family represents "a commander issuing an order through the normal chain of command", exactly what a player hotkey is; this also means player orders honestly incur the same §25/§31 comms-delay-by-COMMUNICATIONS-condition modeling as any other order, not an unexplained instant special case. Player-controlled ship decided as "alpha" (documented in player_input.gd's own doc comment and in ASSUMPTIONS.md), matching Hud's existing POV -- "select ship" hotkey is wired but a visible no-op with only 1 controllable ship right now (honest, minimal; extending the roster later is just adding entries to PlayerInput._controllable_ship_ids). VERIFIED LIVE this pass: a throwaway (not committed) headless SceneTree script called each PlayerInput handler directly (bypassing real InputEvent dispatch, which needs a live viewport) against a running SimulationWorld -- confirmed turn/speed commands get queued in world._pending_command_transmissions (comms delay) and, after enough simulated time, actually create/activate an IndividualCommandState for alpha; target-cycle -> world.ship_combat_directives["alpha"].manual_target_ship_id == "beta" after the delay; clear-target -> back to ""; weapons-toggle -> weapons_free flips true->false after the delay. Also reverified via headless `--import` (clean) and a headless run of res://scenes/main.tscn --quit-after 120: still exactly 16 occurrences of the already-documented dummy-renderer mesh_get_surface_count/"Parameter m is null" artifact (unchanged baseline) -- PlayerInput (pure Node, no mesh, no rendering) added 0 new error/warning types.
  [x] 6. Hardcoded 1v1 scenario vs TacticalAI (world.set_team + world.add_weapon_mount; broadside_arc bugfix; live-verified)
  [x] 7. Win/lose screen (existing §63 destruction state) -- new scripts/win_lose_screen.gd (WinLoseScreen, CanvasLayer/Label, same plain-text §25.1 convention as Hud), instanced in main.gd alongside hud/weapon_fx/player_input and polled every tick in _on_tick (win_lose_screen.update(world), same call-site pattern as hud.update()/weapon_fx.update() -- pure poll, no new signal on SimulationWorld). Reads ONLY existing state: `ship.is_wreck` (§63.1, set by SimulationWorld._resolve_ship_destruction) and `world.teams` (§56.1 item 6's hostility model) -- no new destruction mechanic invented. A team is "alive" if it has >=1 non-wreck ship; once exactly one team is still alive, shows "<TEAM> WINS" (generic over team id strings, not hardcoded red/blue); simultaneous mutual destruction (both sides wrecked same tick) shows "DRAW -- ALL SHIPS DESTROYED" instead of guessing a side; ships with no team assigned are excluded from both tallies (matches SimulationWorld._are_hostile's own "no team = no side" treatment). One-shot: once shown, `_shown` latches true and further update() calls are a no-op -- no restart flow exists yet (explicitly out of scope), so nothing needs to un-show it. VERIFIED LIVE this pass: (a) headless `--import` clean, zero errors; (b) headless run of main.tscn --quit-after 120: still exactly 16 occurrences of the already-documented dummy-renderer mesh_get_surface_count/"Parameter m is null" artifact, unchanged baseline, zero new error/warning types -- confirms WinLoseScreen adds nothing to the live-scene error surface; (c) a throwaway (not committed) headless SceneTree script built a SimulationWorld with two teamed ships and drove WinLoseScreen directly (forcing `_ready()` since add_child() alone doesn't synchronously fire it in a bare SceneTree script): confirmed no verdict while both sides alive, "RED WINS" once beta is fully wrecked, text stays latched "RED WINS" even if alpha is wrecked afterward (one-shot honored), a fresh instance shows the draw message when both sides are wrecked simultaneously, and an unteamed third ship does not skew either team's tally. Also touched main.gd's item-6 doc comment (top of file) to stop claiming "no win/lose screen" now that item 7 exists, without rewriting its historical rationale for teams/mounts.
  [ ] 8. Draft Windows export <- CURRENT (final item -- closes §56.1 when done)
Next concrete step: item 8 (Draft Windows export) -- per AGENTS.md §56.1 item 8's exact text (check AGENTS.md verbatim at start of that pass): "A rough Windows export of the above (`godot --headless --export-*` or the editor GUI equivalent) -- does not need to be pretty, needs to run and be playable end to end on the user's machine." Concretely: (1) check project.godot / project/export_presets.cfg -- does a Windows Desktop export preset already exist (likely not yet, this project has been headless-only so far)? If not, one must be added -- either by scripting export_presets.cfg directly (know its exact format before generating one from scratch -- a malformed file may silently fail to produce a runnable .exe) or by driving the editor GUI's export dialog once headlessly if that's more reliable in this Godot 4.3 build. (2) Godot's Windows export needs an export template matching the engine version (4.3-stable) -- check whether `.tools/godot/` or a templates cache dir already has these (likely NOT downloaded yet, only the editor binary was cached) -- if missing, download them (check available network egress from this environment first; large download, may not fit in one pass) or note as a blocker in this file if unavailable rather than silently skipping the item. (3) Run the actual export command (e.g. `godot --headless --export-release "Windows Desktop" build/HonorHarington.exe` once a preset exists) and verify a .exe was actually produced (file exists, nonzero size) -- do NOT mark this done on "the command didn't error" alone, actually check the output artifact exists. (4) "playable end to end on the user's machine" -- this environment cannot itself launch a Windows .exe to click through it, so "playable" verification here is necessarily limited to "the export succeeded and produced a runnable-looking artifact"; note this limitation explicitly in CHANGELOG.md rather than overclaiming full playtest verification. This is likely a multi-pass item (template download + preset setup + first export attempt could each eat a full pass) -- if it doesn't finish in one pass, leave item 8 unchecked and update this "Next concrete step" with precisely what's done vs. still needed (e.g. "templates downloaded, preset still needs creating") rather than reverting to a vague restatement of the whole item.
Blockers: none currently.
Last pass finished: 2026-09-23T09:16Z (item 7 done: WinLoseScreen added and wired into main.gd's tick loop, reads only existing is_wreck/teams state, live-verified via unchanged headless-scene error baseline + a throwaway direct-drive SceneTree script covering win/draw/one-shot/unteamed-ship cases; pushed this pass)
