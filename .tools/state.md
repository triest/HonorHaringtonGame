# Honorverse dev state (updated every pass)
Phase: §56.1 vertical slice (fast visible-result mode)
Checklist:
  [x] 1. Camera (OrbitCamera, scripts/orbit_camera.gd)
  [~] 2. Placeholder ship geometry (hull+wedge done; sidewall panels done; canon shapes/materials explicitly OUT of scope for this slice)
  [~] 3. Weapon-fire visualization (energy/missile) -- rendering capability DONE and VERIFIED LIVE for energy beams (demo ships actually fire on each other in a headless run of main.tscn). Missile markers only verified by unit test, NOT live, because the demo scenario carries no missile tubes (energy-only weapon mounts, kept simplest per item 6). Scenario-configuration gap, not a WeaponFx capability gap; not reopening.
  [x] 4. Minimal HUD (subsystems/target/contacts state) -- scripts/hud.gd (CanvasLayer + single Label, plain text, no bars/icons per §25.1), instanced in main.gd next to weapon_fx, updated from _on_tick(world, HUD_POV_SHIP_ID="alpha"). Reads ship.subsystems.get_condition(type) for all SubsystemType values, world.get_weapon_target_designation(ship_id) (new read-only SimulationWorld accessor added this pass -- reuses the AI's own _resolve_weapon_target/_hostile_ship_ids instead of re-deriving target selection, so the HUD can never show a target that disagrees with what will actually fire next), and world.sensor_contacts[ship_id] (id + state). main.gd now also assigns alpha.subsystems = ShipSubsystems.new() / beta.subsystems = ShipSubsystems.new() (both were null before -- "per-ship subsystem condition" is meaningless with subsystems always null); everything else about the demo scenario is unchanged. Single-ship POV (alpha) per the checklist item's own "pick ONE ship unless showing both is trivial" allowance. VERIFIED LIVE this pass: a throwaway (not committed) headless SceneTree script replicating main.gd's wiring, run 300 ticks -- HUD text showed all 11 subsystems at 100%, TARGET "beta", CONTACTS "beta: DETECTED", matching world.get_weapon_target_designation("alpha") == "beta" independently. Also reverified via headless `--import` (clean, 0 errors) and a headless run of res://scenes/main.tscn --quit-after 120: still exactly 16 occurrences of the already-documented dummy-renderer mesh_get_surface_count/"Parameter m is null" artifact (same count as the item-6 pass baseline) -- HUD (pure CanvasLayer/Label, no mesh) added 0 new error/warning types to the live scene run.
  [ ] 5. Order input wiring (hotkeys + Input Map -> FormationOrder/IndividualOrder/ShipCombatDirective) <- CURRENT
  [x] 6. Hardcoded 1v1 scenario vs TacticalAI (world.set_team + world.add_weapon_mount; broadside_arc bugfix; live-verified)
  [ ] 7. Win/lose screen (existing §63 destruction state)
  [ ] 8. Draft Windows export
Next concrete step: item 5 (Order input wiring) -- per AGENTS.md §56.1 item 5's exact text: "Minimal player input wired DIRECTLY to the already-implemented backend order APIs (FormationOrder, IndividualOrder, ShipCombatDirective) -- a handful of buttons/hotkeys (select ship, set course/speed, designate target, weapons free/hold), not a polished command UI and not the general Scenario/editor system." Concretely: check FormationOrder/IndividualOrder/ShipCombatDirective's actual APIs first (grep simulation/ for their class definitions -- do not assume method names), then add an Input Map (project.godot) with a small hotkey set (e.g. TAB=select/cycle ship, number keys or click=designate target from current sensor_contacts, a couple keys for thrust/course adjust, a key to toggle weapons free/hold), wired from a new scripts/player_input.gd (or extend main.gd directly if that stays small) that constructs the actual order objects and feeds them into whatever SimulationWorld entry point already consumes them (check how TacticalAI/existing tests submit orders -- reuse that path, do not invent a second one). Since there is no player-controlled side yet, this pass should also decide (and document the decision, one line, wherever felt natural -- state.md/CHANGELOG/ASSUMPTIONS) which of alpha/beta the player controls; "alpha" (already the HUD POV ship) is the obvious default unless something concrete argues otherwise. Keep it a minimal hotkey set, not a polished UI -- explicitly out of scope per the checklist item itself.
Blockers: none currently.
Last pass finished: 2026-09-23T07:20Z (item 4 done: minimal HUD, wired to alpha, live-verified via headless run + unchanged live-scene error baseline; pushed this pass)
