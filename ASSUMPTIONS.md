# ASSUMPTIONS.md - Simulator Approximations

## 1. REPLAY AND SCENARIO ACCURACY [ASSUMPTION]
* **Deterministic Pseudo-Randomness:** All micro-variations in point-defense interception rates and sensor noise are bound to a seed variable initialized at scenario start. This guarantees that replays recreate spatial data frame-by-frame without recording full telemetry video.

## 2. SENSOR TRACKING STATES [ASSUMPTION]
* **Confidence Interpolation:** When a target is obscured by heavy ECM, the simulator interpolates its position based on its last known velocity vector and active acceleration. This creates a spatial displacement between "Sensor Truth" and "Actual Reality" for the AI and Player UI.

## 3. TACTICAL AI PARALLELISM [ASSUMPTION]
* **Reaction Loops:** Tactical AI calculates new flight matrices and targeting distributions on a throttled loop (every 10 simulation ticks) to conserve processing budget during massive missile salvos, simulating command reaction lag.

---

## Игровой движок

Допущение: Godot 4.3 (GDScript), экспорт в standalone Windows .exe.
Причина: бесплатный, открытый, полноценный 3D, нет зависимости от внешних
сервисов/интернета в готовом рантайме, укладывается в требование
"Simulation независим от UI/Rendering" (AGENTS.md §42). Решение принято
агентом самостоятельно по инструкции AGENTS.md §1 ("не превращай разработку
в бесконечный список вопросов"); можно пересмотреть при технической
необходимости.
Затронутый код: весь `project/`.

## Единицы измерения ускорения

Допущение: `ShipPhysicsState.max_acceleration_mps2` хранится в м/с²,
значение по умолчанию — placeholder ~500g, БЕЗ ложной точности (не
каноническая цифра конкретного класса). UI может показывать как множитель g
(1g = 9.80665 м/с²).
Причина: §11 запрещает одно глобальное значение ускорения для всех
кораблей — обязано переопределяться данными класса (§8) при появлении
ship database.
TODO: заменить на data-driven значения по классам/эпохам, со ссылкой на
CANON_RULES.md (см. запись про 46,000G пример) или UNKNOWN там, где данных
нет.

## Релятивистская кинематика (скорость света)

Допущение: Milestone 1 использует классическую ньютоновскую интеграцию
(semi-implicit Euler) с жёстким clamp скорости чуть ниже c
(`_clamp_velocity_below_c` в `ship_physics_state.gd`), чтобы избежать
NaN/сверхсветовых скоростей при экстремальном ускорении (§15). Это НЕ
полноценная релятивистская модель (замедление времени, релятивистский
Доплер для сенсоров и т.п.) — временное упрощение (§58).
TODO: до Milestone с боевой дистанцией/сенсорами на значимых долях c решить,
нужна ли полная релятивистская кинематика.

## Wedge / sidewalls / оружие / сенсоры / AI / формации / replay / сценарии

Статус на Milestone 1: НЕ реализовано в коде. Канонические/интерпретационные
данные по этим темам см. в `CANON_RULES.md`; допущения выше (replay
determinism, sensor confidence interpolation, AI throttled reaction loop)
добавлены заранее как план для будущих Milestone — при реализации сверить
с фактическим кодом и обновить при расхождении.

---

## Milestone 3: wedge / sidewalls — численные допущения

Допущение: порог "выгорания" sidewall (SIDEWALL_BURNOUT_THRESHOLD = 0.0
в `ship_defense_state.gd`), кривая затухания луча по condition (линейная:
transmitted = 1 - condition), и полуугол "острого угла" носовой/кормовой
уязвимости (15°, `BOW_STERN_ACUTE_ANGLE_HALF_WIDTH_RAD`) — ВСЕ ЭТИ ЧИСЛА
ЯВЛЯЮТСЯ PLACEHOLDER, не найдены в источниках (см. CANON_RULES.md, вторая
сверка). Оставлены configurable константами, а не зашиты в логику решения,
согласно §9/§4 AGENTS.md.
Затронутый код: `project/simulation/ship_defense_state.gd`.
TODO: заменить на данные, если найдётся более точный источник, либо
оставить как игровой баланс с явной пометкой INTERPRETATION.

Допущение: ось "нос" корабля = локальная -Z (соглашение Godot: "вперёд"
для Node3D/Camera3D по умолчанию -Z). Зафиксировано в `attack_geometry.gd`.
Причина: чтобы не изобретать нестандартную конвенцию осей вопреки движку.

---

## Milestone 4: энергетическое оружие — допущения

Допущение: конкретные числа (max_range_m=500 000 м, damage_per_hit=100,
recharge_time_s=4с в демо-сцене и тестах) — ЧИСТЫЙ PLACEHOLDER. В книгах
Honorverse нет чисел, готовых для прямой подстановки в симулятор (там
"предельная дистанция", "сближение на дистанцию энергетического боя" —
относительные описания). Источник: не найден численный канон при сверке
для CANON_RULES.md. Значения обязаны стать data-driven (по классам
кораблей/эпохам) до Definition of Done, см. §8/§48.
Затронутый код: `project/simulation/weapon_data.gd`,
`project/scripts/main.gd` (демо-инстанс).

Допущение: `HullState` — единый scalar пул прочности корпуса. Это ЯВНО
временное упрощение (§58), а не финальная модель урона. §25 требует
урон по конкретным подсистемам (propulsion, sensors, weapons и т.д.) —
будет заменено в Milestone 8 (Subsystem Damage). Не путать с готовой
функциональностью.
Затронутый код: `project/simulation/hull_state.gd`.

Допущение: огневые дуги оружия (`WeaponMount.arc_sectors`) заданы на той
же грубой 6-секторной сетке, что и защита (TOP/BOTTOM/BOW/STERN/PORT/
STARBOARD), а не непрерывным конусом. Упрощение для Milestone 4;
пересмотреть, если понадобится точная геометрия наведения.

---

## Milestone 5: ракеты + наведение — допущения

Допущение: `drive_max_acceleration_mps2`/`drive_burn_time_s` по умолчанию —
46 000G/180с. Это ЦИТИРУЕМЫЙ ПРИМЕР из канона (см. CANON_RULES.md,
Honorverse Wiki, Missile) для КОНКРЕТНОГО класса ракет конкретной эпохи —
используется как разумное значение по умолчанию, а НЕ как универсальная
константа для всех ракет всех эпох (§11 явно это запрещает). Обязано
переопределяться данными по классу ракеты, когда появится ship/missile
database (§8, §48).

Допущение: `terminal_detonation_range_m` = 50 000 м (50 км) — ЧИСТЫЙ
PLACEHOLDER, не найден в источниках при сверке. НЕ ПУТАТЬ с цитируемым
"25 000 км эффективного радиуса поражения laserhead" из CANON_RULES.md —
это разные вещи: терминальная дистанция здесь — это дистанция, на которой
ракета переходит в терминальную фазу наведения/подрыва, а 25 000 км — это
радиус поражения ПОСЛЕ подрыва боеголовки. Точная терминальная дистанция
для реализации laserhead ещё не решена — TODO для отдельного прохода
Milestone (когда будет подключаться реальная физика laserhead-детонации).

Допущение: `max_lifetime_s` = 600с (10 минут) — self-destruct по истечении,
чтобы ракеты не копились в памяти бесконечно (инженерная необходимость,
не канон).

Допущение: наведение — predicted intercept (lead pursuit) с итеративным
уточнением времени сближения, НЕ настоящая proportional navigation (PN).
Это соответствует минимальному требованию §19 (учитывать target velocity,
не просто "direction = target.pos - missile.pos"), но не самая точная
модель, которую используют реальные ракеты. TODO: заменить на true PN,
если игровая механика этого потребует (например для реалистичных
эвазивных манёвров цели).

Допущение: ориентация ракеты жёстко следует за вектором скорости
(`_look_rotation`), в отличие от кораблей, которые могут давать тягу не по
своей оси. Инженерное упрощение для реактивного снаряда, не канон и не
антиканон — обоснованное инженерное решение.

Допущение: `target` в `MissileState` — прямой object reference на
`ShipPhysicsState`, не id + lookup. Упрощение для однопроцессной
симуляции; при появлении сохранений/сети — пересмотреть.

---

## Milestone 5 (продолжение): laserhead detonation — допущение

INTERPRETATION (ТЗ §21 явно требует документировать трактовку laserhead):
в этом проходе laserhead смоделирован как один направленный удар из
позиции ракеты в момент подрыва, разрешаемый через ту же геометрию
wedge/sidewall, что и энергетическое оружие (`ShipDefenseState.resolve_attack`).
Реальный канонический механизм детальнее: разводящие штанги (lasing rods)
выдвигаются ~100м перед боеголовкой и стреляют по конкретному
экспонированному вектору цели (см. CANON_RULES.md) — эта более точная
геометрия НЕ реализована, это TODO для будущего прохода, если понадобится
точность (например для площадного поражения близких целей — CANON цифра
25 000 км радиуса поражения здесь тоже не используется, это отдельная
задача).
Допущение: `laserhead_damage` = 400 (placeholder, как и всё урона в этой
кодовой базе).
Затронутый код: `missile_resolution.gd`, `missile_state.gd`.


---

## Обновление ТЗ по laserhead (по запросу пользователя, 2026-09-18)

AGENTS.md §21 и CLOUD.md §2.2 дополнены точной канонической
последовательностью подрыва laserhead (см. CANON_RULES.md, третья
сверка). Текущая реализация (`missile_resolution.gd`) этому ТЗ пока НЕ
соответствует полностью — известный, задокументированный разрыв между
ТЗ и кодом:

Расхождение: код резолвит попадание как один луч из позиции ракеты
(`missile.position`) в момент подрыва. Канон: несколько независимо
наводимых штанг разводятся ~100м перед боеголовкой и КАЖДАЯ бьёт по
своему вектору на цели.
Причина оставить как есть сейчас: MVP-масштаб, полная модель штанг как
отдельных суб-сущностей — это больше работы (каждая штанга нуждается в
своей 3D-позиции, наведении, возможно индивидуальной резолюции через
`ShipDefenseState.resolve_attack`), решено оставить как TODO следующего
прохода, а не блокировать текущий прогресс.
TODO: при следующем прохода по ракетам — либо (а) смоделировать разведение
штанг как временные суб-сущности с собственной позицией/наведением, либо
(б) осознанно оставить упрощение и явно задокументировать это как
постоянное INTERPRETATION-решение (не временное), если полная модель
окажется избыточной для игровой механики. Также: `terminal_detonation_range_m`
(сейчас 50 км placeholder) — это НЕ то же самое, что 25 000 км
эффективного радиуса поражения (канон, но про другое — см. CLOUD.md
§2.2) — не путать при доработке.

---

## Закрытие TODO: разведение штанг laserhead (реализовано)

TODO из предыдущей записи ("Расхождение: код резолвит попадание как один
луч из позиции ракеты") — ЗАКРЫТ. `missile_resolution.gd` теперь честно
вычисляет позиции `rod_count` штанг (веером вокруг линии атаки, на
расстоянии `rod_offset_m` от ракеты — CANON "около 100м"), и резолвит
КАЖДУЮ независимо через `ShipDefenseState.resolve_attack()`, суммируя урон.

Осталось как задокументированный scope limit (НЕ реализовано, честно
названо, не подана как готовое):
* Mark-13-style "independently targetable submunitions" (разные штанги
  бьют по РАЗНЫМ целям) — `MissileState` пока несёт только один `target`,
  это отдельная, более крупная фича (мультитаргетинг одной ракеты).
* `rod_spread_half_angle_rad` (20°) — не канонический угол, ASSUMPTION,
  подобран инженерно. Поскольку `rod_offset_m` (100м) пренебрежимо мал
  относительно реальных дистанций боя (км+), на практике все штанги почти
  всегда попадают в один и тот же сектор атаки — это ОЖИДАЕМО и
  соответствует канону (один гауссов импульс, одно общее направление
  атаки), не баг.

---

## Дросселирование двигателя ракеты — допущение модели

Реализовано `MissileState.set_throttle(fraction)`. Модель: константный
"бюджет delta-v" (`base_accel * base_burn_time == throttled_accel *
throttled_burn_time`) — ЭТО ИНЖЕНЕРНОЕ ДОПУЩЕНИЕ, не прямая цитата канона.
Канон подтверждает САМ ФАКТ компромисса (меньше ускорение → больше время
горения/дальность), но не даёт формулу. Разумное физическое приближение
(константный запас топлива/энергии), задокументировано как ASSUMPTION.
Ограничение: throttle можно установить только до начала горения
(вызов `set_throttle()` после старта не поддержан релевантно — можно
вызвать, но `drive_burn_remaining_s` будет сброшен на новый burn_time,
что физически неверно для "уже летящей" ракеты — TODO: либо запретить
throttle mid-flight явной проверкой, либо продумать частичный останов).

## Известные пробелы по физике/поражающим факторам ракет (после 4-й сверки)

Зафиксированы в CANON_RULES.md, повторяю здесь для видимости при чтении
только ASSUMPTIONS.md:
* Многоступенчатые MDM (staged drives) — НЕ реализовано, одна ступень.
* Вращение ракеты для затруднения PD — НЕ реализовано геометрически.
* Пусковые трубы/mass driver (вынос ракеты за клин корабля-носителя) —
  НЕ реализовано, ракеты сейчас просто создаются с заданной позицией.
* Зависимость ослабления sidewall от ТИПА атакующего оружия (laserhead
  эффективнее против sidewall, чем чистый kinetic/fusion) —
  `ship_defense_state.gd` пока не различает тип атаки, одна формула для
  всех. TODO для будущего прохода: добавить параметр damage_type в
  resolve_attack() и weapon/warhead данные.

---

## Закрытие TODO: damage-type-зависимое ослабление sidewall (реализовано)

`damage_type.gd` (новый enum ENERGY/LASERHEAD/KINETIC) + `resolve_attack()`
теперь принимает `damage_type` (по умолчанию ENERGY — обратная
совместимость). LASERHEAD получает множитель `LASERHEAD_SIDEWALL_PENETRATION_MULTIPLIER
= 1.5` (ASSUMPTION, точный коэффициент не найден в источниках, канон
подтверждает только сам факт "эффективнее", не число), результат всегда
clamp в [0,1]. `weapon_resolution.gd` передаёт ENERGY, `missile_resolution.gd`
передаёт LASERHEAD. KINETIC зарезервирован (множитель 1.0 = как ENERGY),
не используется ни одним оружием пока.

---

## Процедурная 3D-модель корпуса — допущения

Допущение: `length_m=500`, `max_width_m=90`, `max_height_m=60` в
`ShipPhysicsState` — PLACEHOLDER (примерно масштаб лёгкого крейсера,
без привязки к конкретному канон-классу). Должны переопределяться
по данным класса корабля, когда появится ship database (§8).

INTERPRETATION: сам силуэт "flattened spindle" с hammerhead — основан
на текстовом описании (CANON_RULES.md, пятая сверка), но ТОЧНАЯ форма
профиля (`_radius_profile` в `hull_mesh_builder.gd` — синус + локальный
бугорок для hammerhead) — инженерная интерпретация этого описания, не
цитата конкретных пропорций (книги не дают формулу). Это ЗАГЛУШКА для
рендера, не финальная модель конкретного класса корабля — см. §7/§51.

Допущение: визуализация wedge — два плоских полупрозрачных PlaneMesh
сверху/снизу, размер пропорционален габаритам корпуса
(`length_m * 1.4`, `max_width_m * 2.2`). Не геометрически точная форма
клина (в каноне это не плоскость, а поле искажения пространства) —
чисто иллюстративная заглушка.

## Известная проблема точности на больших дистанциях (НЕ решена)

`ShipPhysicsState.position` — в метрах, реальный масштаб Honorverse
(тысячи-миллионы км). Godot Node3D transform использует float (32-bit)
внутри рендер-слоя — на дистанциях свыше ~10⁵-10⁶ м это может давать
джиттер/потерю точности при рендере (сама физика в `ShipPhysicsState`
считается на float Vector3 тоже, не double — GDScript/Godot Vector3 по
умолчанию single-precision, если явно не собран движок с double
precision build). ТЗ §14 требует "origin rebasing или локальные системы
отсчёта" для больших дистанций — ЭТО НЕ РЕАЛИЗОВАНО. Для текущего
прототипа (дистанции ~10⁴ м) не критично, но это известный технический
долг для реальных боевых дистанций (сотни тысяч - миллионы км).
TODO: рассмотреть либо double-precision сборку Godot, либо origin
rebasing (плавающая точка отсчёта рендера относительно камеры/игрока)
при переходе к реалистичным боевым дистанциям.

---

## Point Defense — допущения (Milestone §22)

Все числа — PLACEHOLDER, канон не даёт конкретных цифр (см. CANON_RULES.md,
шестая сверка): `max_engagement_range_m`=20 000м, `reaction_time_s`=1.5с,
`recharge_time_s`=0.75с, `hits_required_to_kill`=2. Структура (время
реакции + несколько попаданий + собственный recharge, то есть большой
залп ракет может "перегрузить" один PD-модуль) — инженерная реализация
подтверждённой канонической идеи "многослойная, насыщаемая оборона", не
цитата конкретных чисел.

Честно НЕ реализовано: выбор цели PD-модулем среди нескольких входящих
ракет (это задача tactical AI/§26, `PointDefenseResolution.engage()`
резолвит уже выбранную цель, не выбирает её сам — separation of concerns
как у `WeaponResolution`); зависимость от состояния сенсоров корабля
(§23, ещё не реализовано — сейчас "в радиусе = обнаружено", без модели
detection/tracking quality); влияние ECM (§24, не реализовано); влияние
повреждений подсистем на `condition` (§25, не реализовано); ничто пока
не вызывает `PointDefenseResolution.engage()` в игровом цикле — это
чистая логика, готовая к подключению, но не подключённая ни к одному
Milestone AI/tactical.

---

## Улучшенная визуализация клина — V-образная геометрия (по замечанию пользователя)

Заменил плоский PlaneMesh на процедурный `wedge_mesh_builder.gd`:
V-образное (шатром) поперечное сечение, сужается ближе к середине
корабля и раскрывается к носу/корме (где расположены генерирующие
impeller-кольца, §7/§9). Как и раньше — ЧИСТАЯ ИЛЛЮСТРАТИВНАЯ ЗАГЛУШКА
(ASSUMPTIONS.md), точная геометрия сечения клина не найдена в
источниках, это инженерная интерпретация словесного описания
"клин/wedge", не цитата конкретных пропорций.

---

## Sensors (§23) — первая версия, пороговые числа (по итогам "Седьмая сверка")

Канон (первоисточник "On Basilisk Station" гл.3, см. CANON_RULES.md
"Седьмая сверка") подтверждает МЕХАНИЗМ: гравитационные сенсоры ловят
сигнатуру impeller-клина, корабль может СНИЗИТЬ обнаруживаемость,
погасив impellers ("shutting down her impellers and dropping off the
enemy's passive scanners"). Канон НЕ даёт конкретных цифр дальности
обнаружения или времени перехода DETECTED→TRACKED — поэтому ВСЕ числа
ниже ASSUMPTION (инженерная реализация подтверждённого механизма, не
цитата):

* `SensorResolution.DEFAULT_SENSOR_RANGE_M` = 2 000 000 км — выбрано
  заведомо больше `terminal_detonation_range_m` (50 км = 50 000 м по умолчанию
  у ракеты), чтобы контакты создавались не только в момент терминальной
  фазы боя.
* `DETECTED_TO_TRACKED_TIME_S` = 6с — время устойчивого контакта для
  перехода от "замечено" к "уверенно отслеживается" (weapons-quality
  lock). Без канонической опоры.
* `ESTIMATED_GRACE_PERIOD_S` = 30с — сколько времени цель остаётся
  ESTIMATED (dead-reckoning по последним известным позиции/скорости)
  после потери контакта, прежде чем перейти в LOST. Без канонической
  опоры.

Модель обнаруживаемости (см. `sensor_resolution.gd`): корабль
обнаруживаем ТОЛЬКО пока `defense.wedge_up == true`; ракета — только
пока у неё ещё горит двигатель (`drive_burn_remaining_s > 0`, то есть в
фазах BOOST/MIDCOURSE с работающим двигателем; неподвижная баллистика/
погашенный двигатель — вне модели, "печатает" на пассивные сенсоры
только активный клин). Это прямое инженерное прочтение подтверждённого
канона, не более.

Честно НЕ реализовано: активные сенсоры (в отличие от пассивных
гравитационных — канон вики упоминает разные типы, не проверено
первоисточником); дальность/качество обнаружения в зависимости от
класса/размера корабля (используется одна константа для всех); влияние
ECM (§24 — совсем не начато); shipbуровневые contact-словари ещё не
заведены нигде в живом коде — `SensorResolution`/`SensorContact`
протестированы изолированно, но ничто пока не вызывает их из
`MissileGuidance` (наведение ракет по-прежнему "видит" цель идеально,
напрямую через `target.position`) или из `PointDefenseResolution`
(PD по-прежнему проверяет дистанцию напрямую, не через SensorContact).
Это следующий шаг — подключение, а не эта заготовка.

---

## Терминальная детонация всё ещё по истинной дальности (не по сенсорной оценке)

После подключения сенсоров к наведению (`MissileGuidance.resolve_thrust_direction`)
осталась НЕ решённая асимметрия: `MissileState.integrate()` проверяет
вход во терминальную дальность и взвод боеголовки через
`target.position` (истинную позицию), а не через `sensor_contact`.
То есть ракета может "физически" пролететь достаточно близко и
сдетонировать, даже если её наведение (уже сенсорно-честное) в этот
момент её не видит. ASSUMPTION: оставлено намеренно как временное
упрощение — разделение "физическая близость взрывателя" (обычно
собственный, отдельный от основного радара, ближний датчик
неконтактного взрывателя) от "радар наведения для довода до цели" —
правдоподобно и канонически не противоречиво, но не проверено
источниками. Следующий шаг для честности — либо явно обосновать это
разделение (ближний взрыватель как отдельный сенсор с собственной,
очень малой дальностью), либо тоже завязать на `sensor_contact`.

---

## Subsystem Damage (§25) — контейнер есть, распределение урона по подсистемам ещё нет

`ship_subsystems.gd` даёт все 11 подсистем из ТЗ §25 с condition 0..1,
но НИЧТО пока не решает, КАКАЯ подсистема получает урон при попадании
(это отдельная логика, ещё не написана). Также контейнер не унифицирован
с более ранними, уже существующими полями condition
(`ShipPhysicsState.propulsion_condition`/`.compensator_condition`,
`WeaponMount.condition`, `PointDefenseMount.condition`, sidewall
condition в `ShipDefenseState`) — они возникли раньше, по одной задаче
за раз, и пока живут отдельно от `ShipSubsystems`. Объединение — открытый
рефакторинг, сознательно отложенный, чтобы не трогать уже протестированные
модули без необходимости. Подключён пока только ОДИН потребитель
(`SensorResolution` читает `SENSORS`) — это прямая реализация примера
из самого ТЗ ("sensor damage → degraded tracking"), не полное покрытие
Milestone 8.
