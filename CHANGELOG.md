# CHANGELOG.md

## 2026-09-25 (проход: §56.3 item B -- именованная командная группа + squadron-list панель)

Реализован пункт B чек-листа §56.3 (`.tools/state.md`): выбор
превращается в РЕАЛЬНУЮ командную структуру, а не в UI-only список --
как и требует §1.10.5 ("должно использовать существующую архитектуру
CommandEchelon, FormationState и IndividualOrder, а не создавать
отдельную параллельную систему управления").

* `project/scripts/command_group_controller.gd` (новый, Node, класс
  `CommandGroupController`): по хоткею G (`selection_make_group`,
  project.godot [input], physical_keycode 71) берёт текущий
  multi-select (`SelectionState`, item A) и создаёт
  `world.add_formation()` + `world.add_command_echelon()` +
  `world.attach_formation_to_echelon()` -- НЕ параллельную модель
  группы. Критерии допуска в группу (см. новую запись в
  ASSUMPTIONS.md "§56.3 item B"): только реальные корабли одной
  команды; ведущим становится первый выбранный id; станции остальных
  членов фиксируются как ТЕКУЩЕЕ относительное положение в локальном
  фрейме ведущего (тем самым реально включается уже существующий
  `SimulationWorld._resolve_formation_keeping` -- физическое удержание
  формации, а не просто список имён). Название группы = `CommandEchelon.
  kind` напрямую ("Squadron N", N -- счётчик) -- отдельного поля "имя
  группы" нигде не заводилось (это и была бы запрещённая §1.10.5
  параллельная модель).
* `project/scripts/command_group_panel.gd` (новый, CanvasLayer, класс
  `CommandGroupPanel`): текстовая read-only панель (тот же паттерн, что
  Hud/WeaponFx/TacticalPlot -- чистое чтение уже посчитанного
  `world.command_echelons`/`world.formations`, ничего не вычисляет
  сама), список текущих squadron-групп с ростером и отметкой `>` у
  выбранных сейчас кораблей. Позиция -- фиксированный отступ слева под
  панелью Hud (не буквальная геометрия референс-картинки, см.
  ASSUMPTIONS.md: полноценная раскладка панелей отложена до пункта F).
* `project/scripts/main.gd`: инстанцирует оба новых скрипта, передаёт им
  тот же `world`/`selection`, что и tactical_plot (никакого второго
  selection), добавляет `command_group_panel.update()` в тот же
  per-tick вызов, что Hud/TacticalPlot/WinLoseScreen.
* `project/project.godot`: новый input action `selection_make_group`
  (G).
* Тест (новый, ALL TESTS PASSED, 8/8): `simulation/tests/
  test_command_group_controller.gd` -- одиночный выбор = no-op, пустой
  выбор = no-op, создание formation+echelon для 2 своих кораблей,
  исключение корабля другой команды (mixed-team selection), исключение
  не-корабельных id (например id ракеты) из членства, инкремент индекса
  группы между вызовами (Squadron 1, Squadron 2, ...), корректный
  перевод relative offset в локальный фрейм повёрнутого ведущего
  (guide.orientation != identity), первый выбранный -- всегда ведущий
  независимо от алфавитного порядка id.
* `ASSUMPTIONS.md`: новая запись "§56.3 item B" фиксирует все
  интерпретационные решения выше (хоткей, критерии допуска, выбор
  ведущего, заморозка станций, именование, временная раскладка панели).

НЕ входит в этот пасс (следующие пункты того же чек-листа §56.3, ещё НЕ
реализованы): C (RMB move order с визуализацией курса), D (контекстное
меню приказов), E-I. Также НЕ реализовано в рамках B: роспуск группы,
слияние двух групп, переименование группы после создания (открытые
хвосты, см. ASSUMPTIONS.md/`.tools/state.md`). Сценарий по-прежнему 1v1
(item H ещё не сделан) -- живая проверка группировки 2+ своих кораблей
пока невозможна, только юнит-тесты (синтетические корабли) и headless
smoke.

Проверено (режим "fast visible-result" §56.3 всё ещё в силе, полный
симуляционный набор не гонялся):
1. `godot --headless --import` -- чисто.
2. `test_command_group_controller.gd` -- ALL TESTS PASSED (8/8).
3. `test_selection_state.gd` (регресс) -- ALL TESTS PASSED (9/9).
4. `test_tactical_plot_selection.gd` (регресс) -- ALL TESTS PASSED (8/8).
5. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit 0,
   те же самые 16x baseline `mesh_get_surface_count`/"Parameter m is
   null" (dummy-рендерер), ноль новых типов ошибок -- новые
   CommandGroupController/CommandGroupPanel компилируются и проходят
   `_ready()`/per-tick `update()` чисто в headless-режиме.

**НЕ заявляется "§56.3 закрыт"** -- это пункт B из 9+1 (A уже был
сделан ранее). G-хоткей и панель реально НЕ проверялись живым игроком
(headless не может сгенерировать настоящий keypress с полноценным
рендером) -- см. .tools/state.md, тот же урок §56.1/§56.2/item A ещё
раз: не объявлять готовым без живого подтверждения.


## 2026-09-23 (проход: §56.1 item 8 -- черновой Windows-экспорт; §56.1 ЗАКРЫТ)

Добавлен `project/export_presets.cfg` с пресетом "Windows Desktop" (id 0,
x86_64, `embed_pck=false`, `application/modify_resources=false`) -- до
этого прохода файла пресетов не существовало вообще (проект был чисто
headless). `custom_template/debug`/`/release` оставлены пустыми, чтобы
Godot сам находил шаблоны экспорта по установленной версии в
`~/.local/share/godot/export_templates/4.3.stable/`.

Шаблонов экспорта под Windows на машине не было (кэшировался только сам
Linux-бинарник редактора/экспортёра, см. предыдущие проходы). Официальный
`Godot_v4.3-stable_export_templates.tpz` -- это единый мультиплатформенный
архив на 1.07 ГБ, и при текущей скорости egress этой VM (~300-400 КБ/с,
проверено отдельным замером: один поток curl скачал только 48 МБ за
169 секунд) полное скачивание заняло бы больше часа сплошных попыток.
Вместо этого написан отдельный скрипт
`.tools/godot_templates_cache/remote_extract.py` (кэш-папка в
`.gitignore`), который читает ZIP central directory архива через HTTP
Range-запросы (без скачивания всего файла), а затем докачивает и
инфлейтит ТОЛЬКО нужные записи по точному имени: `templates/version.txt`,
`windows_debug_x86_64.exe` (+`_console`), `windows_release_x86_64.exe`
(+`_console`) -- итого ~59 МБ вместо 1.07 ГБ. Для каждого извлечённого
файла CRC-32 сверяется с тем, что хранится в central directory архива
(скрипт падает при несовпадении) -- все 4 бинарника + version.txt прошли
проверку чисто. Установлены в стандартный путь Godot
(`$HOME/.local/share/godot/export_templates/4.3.stable/`, версия
"4.3.stable" взята из `version.txt` самого архива, не угадана); копия тех
же файлов также лежит в гитигнорнутом `.tools/godot_templates_cache/extracted2/`
на случай, если `$HOME` вне примонтированной папки проекта не переживёт
границу между запусками cron (не проверялось на реальной границе прохода
-- если в следующий раз стандартный путь окажется пуст, скопировать из
этого кэша, а не перекачивать заново).

Запуск `godot --headless --export-release "Windows Desktop"
project/build/HonorHarington.exe`: exit 0, НОЛЬ предупреждений/ошибок
(первая попытка с `modify_resources=true` по умолчанию логировала два
предупреждения -- отсутствующий `project/icon.svg`, которого в проекте
никогда не было, это чисто косметика и явно вне рамок этого пункта
("does not need to be pretty"), и "could not start rcedit", ожидаемо при
кросс-экспорте под Windows с Linux-хоста -- теперь подавлено отключением
`modify_resources`). Проверен САМ АРТЕФАКТ, а не только код возврата:
`project/build/HonorHarington.exe` -- 84 214 784 байта, `file` определяет
как "PE32+ executable (GUI) x86-64 (stripped to external PDB), for MS
Windows" -- настоящий Windows-бинарник, не заглушка; рядом
`project/build/HonorHarington.pck` (347 КБ, т.к. `embed_pck=false` --
оба файла нужно распространять вместе, это стандартная раскладка
Godot). `project/build/` добавлен в `.gitignore` (большой
пересобираемый бинарник); сам `export_presets.cfg` закоммичен, чтобы
пресет не терять между проходами. После билда прогнан чистый headless
`--import` -- 0 ошибок.

ОГРАНИЧЕНИЕ (явно проговорено, не переоценивается): эта среда не может
сама запустить .exe под Windows, поэтому "playable end to end" здесь
означает "чистый экспорт без единого предупреждения произвёл корректно
сформированный Windows-бинарник + файл данных" -- фактический запуск на
Windows пользователя не проверен и остаётся единственным, что может
подтвердить только сам пользователь (на его же машине, где и лежит этот
проект). Проверить: `project\build\HonorHarington.exe` в Windows
Explorer (main_scene = `res://scenes/main.tscn`, так что должен сразу
загрузить вертикальный срез).

Итог: §56.1 (играбельный вертикальный срез) ЗАКРЫТ -- все 8 пунктов
чеклиста выполнены (пункты 2 и 3 приняты с явно задокументированными и
согласованными сокращениями объёма, не багами). Milestone 13-17 и прочая
работа вне §56.1, приостановленная на время этой фазы, разморожена --
следующий проход должен свериться с AGENTS.md заново, чтобы определить
следующий приоритет.


## 2026-09-23 (проход: §56.1 item 7 -- экран победы/поражения)

Добавлен минимальный экран победы/поражения (`scripts/win_lose_screen.gd`,
класс `WinLoseScreen`), использующий ТОЛЬКО уже существующее состояние
разрушения корабля (§63: `ShipPhysicsState.is_wreck`, выставляется
`SimulationWorld._resolve_ship_destruction`) и модель команд/сторон из
item 6 (`world.teams`, `world.set_team`) -- никакой новой механики
"разрушения" не изобретено, только чтение уже посчитанного состояния
(§42, тот же принцип, что и у `weapon_fx.gd`/`hud.gd`).

* `WinLoseScreen extends CanvasLayer` (тот же минималистичный
  Label-подход, что и у `Hud`, §25.1 -- обычный текст, никаких
  декоративных полосок здоровья): опрашивается раз в тик из
  `main.gd._on_tick` (`win_lose_screen.update(world)`, добавлено сразу
  после `hud.update()`/`weapon_fx.update()` -- тот же порядок вызовов,
  что и у остальных чисто-визуализирующих потребителей состояния мира).
* Условие победы: команда считается "живой", если у неё есть хотя бы
  один корабль с `is_wreck == false`. Как только "живой" остаётся ровно
  одна команда -- показывается "<TEAM> WINS" (не захардкожено на
  red/blue, берётся реальный id команды из `world.teams`). Если оба
  борта одновременно потеряли все корабли в один тик -- показывается
  "DRAW -- ALL SHIPS DESTROYED" вместо того, чтобы произвольно выбрать
  победителя. Корабль без назначенной команды не учитывается ни в одном
  из подсчётов -- ровно то же правило "нет команды = нет стороны", что
  уже использует `SimulationWorld._are_hostile`.
* Одноразовый показ (`_shown`): после первого показа `update()`
  становится no-op -- в этом срезе нет флоу рестарта (explicitly out of
  scope по AGENTS.md §56.1), поэтому скрывать экран обратно не для чего.
* Попутно поправлен doc-comment в начале `main.gd` (relic item 6),
  который до этого прохода всё ещё утверждал "no win/lose screen" --
  исправлено на актуальное состояние (item 5 и 7 оба сделаны), сама
  историческая мотивация абзаца про teams/mounts оставлена как есть.

ПРОВЕРЕНО вживую в этом проходе:
1. `godot --headless --import` -- чистый импорт, ноль ошибок.
2. Headless-прогон `res://scenes/main.tscn --quit-after 120` --
   по-прежнему ровно 16 вхождений уже задокументированного артефакта
   dummy-рендерера (`mesh_get_surface_count`/"Parameter m is null"),
   без единой новой ошибки/предупреждения -- подтверждает, что
   `WinLoseScreen` не добавил ничего к baseline ошибок живой сцены.
3. Одноразовый (не закоммиченный) headless SceneTree-скрипт напрямую
   собрал `SimulationWorld` с двумя кораблями на разных командах и
   вызывал `WinLoseScreen.update()` напрямую (пришлось явно вызвать
   `_ready()`, т.к. `add_child()` в голом SceneTree-скрипте не вызывает
   `_ready()` синхронно) -- подтверждены все случаи: нет вердикта, пока
   живы обе стороны; "RED WINS" после полного уничтожения beta; текст
   остаётся залипшим на "RED WINS" даже если alpha после этого тоже
   станет wreck (one-shot соблюдён); отдельный экземпляр корректно
   показывает "DRAW..." при одновременном взаимном уничтожении; третий
   корабль без команды не влияет ни на один из подсчётов.

Явно НЕ сделано в этом проходе: item 8 (черновой Windows-экспорт) --
последний пункт §56.1, всё ещё не начат.

## 2026-09-23 (проход: §56.1 item 5 -- ввод игровых приказов на хоткеях)

Реализован минимальный ввод игровых приказов (`scripts/player_input.gd`,
класс `PlayerInput`), напрямую вызывающий уже существующий API
`SimulationWorld` для приказов -- ничего нового не изобретено:

* Добавлена реальная Input Map в `project.godot` ([input]-секция):
  `order_select_ship` (TAB), `order_target_next` (T), `order_target_clear`
  (C), `order_weapons_toggle` (F), `order_turn_left`/`order_turn_right`
  (A/D), `order_speed_up`/`order_speed_down` (W/S). До этого прохода
  `project.godot` не объявлял НИ ОДНОГО input action (см. собственный
  комментарий `orbit_camera.gd`, честно откладывавший это на item 5).
  Секция сгенерирована через одноразовый (не закоммиченный) headless
  `ProjectSettings`-скрипт, а не написана руками -- формат
  `InputEventKey`-ресурсов в `project.godot` слишком многословный и
  легко испортить вручную. ПОБОЧНЫЙ ЭФФЕКТ, исправлен в этом же проходе:
  `ProjectSettings.save()` тихо выбросил `physics/common/
  physics_ticks_per_second=60` и `rendering/renderer/
  rendering_method="forward_plus"`, т.к. оба значения совпадали со
  встроенными дефолтами движка -- оба возвращены явно обратно (тем же
  проходом, до коммита), т.к. явное задание `forward_plus` важно для
  будущего Windows-экспорта (item 8), а неявная зависимость от дефолта
  движка -- хрупкая.
* `PlayerInput._unhandled_input` читает эти actions и вызывает ПРЯМО в
  `transmit_*`-семейство `SimulationWorld` (не instant
  `issue_*`/`set_*`): `transmit_individual_order_now` +
  `IndividualOrder.change_course`/`change_speed` для курса/скорости,
  `transmit_ship_target`/`transmit_clear_ship_target` для designation
  цели (цикл по ЖИВОМУ `world.sensor_contacts[ship_id]` -- ровно тому же
  списку, что уже показывает HUD), `transmit_ship_weapons_free` для
  weapons free/hold. `transmit_*` выбран осознанно, не `issue_*`/`set_*`:
  собственный комментарий `simulation_world.gd` описывает именно этот
  набор как представляющий "commander issuing an order through the
  normal chain of command" -- это ровно то, что представляет нажатие
  клавиши игроком; как следствие, приказы игрока честно получают ту же
  §25/§31 задержку связи по состоянию подсистемы COMMUNICATIONS, что и
  любой другой приказ, без необъяснённого мгновенного спецслучая для
  игрока.
* Курс/скорость хранятся ЛОКАЛЬНО в `PlayerInput` как "последнее
  скомандованное" значение (`_desired_heading_by_ship`/
  `_desired_speed_by_ship`), а не перечитываются из текущей скорости
  корабля при каждом нажатии -- только что переданный приказ ещё не
  подействовал (задержка связи + время на разгон/поворот), так что
  повторное нажатие сразу после первого дало бы непредсказуемый шаг,
  если бы отталкивалось от ещё не изменившейся живой скорости.
* Какой корабль под игроком -- решено этим проходом: "alpha" (то же,
  что уже показывает HUD с прохода item 4); задокументировано в
  собственном doc-комментарии `player_input.gd` и в `ASSUMPTIONS.md`.
  Хоткей "select ship" подключён (по явному требованию чеклиста), но
  честно является visible no-op при ровно одном управляемом корабле --
  `PlayerInput._controllable_ship_ids` сейчас содержит только "alpha";
  расширение до нескольких кораблей -- это только добавление элементов
  в этот список, остальной код не меняется.
* Проверено ДВУМЯ способами (полный набор симуляционных тестов по
  правилу §56.1 "fast visible-result" не гонялся):
  1. Одноразовый (не закоммиченный) headless-скрипт на `SceneTree`,
     вызывающий обработчики `PlayerInput` НАПРЯМУЮ (минуя реальную
     диспетчеризацию `InputEvent`, для которой нужен живой viewport) на
     фоне реально идущей `SimulationWorld`: поворот/изменение скорости
     кладут запись в `world._pending_command_transmissions` (задержка
     связи) и, после достаточного симулированного времени, реально
     создают/активируют `IndividualCommandState` для alpha; цикл цели
     -> `world.ship_combat_directives["alpha"].manual_target_ship_id ==
     "beta"` после задержки; очистка цели -> обратно `""`; тумблер
     оружия -> `weapons_free` меняется `true` -> `false` после задержки.
  2. Headless `--import` (чисто, без ошибок) + headless прогон
     `res://scenes/main.tscn --quit-after 120` -- всё те же 16
     повторений уже задокументированного dummy-renderer артефакта
     `mesh_get_surface_count`/"Parameter m is null" (без изменений от
     базовой линии item 4/6) -- `PlayerInput` (чистый `Node`, без меша и
     рендера) не добавил ни одного нового типа ошибки/предупреждения.

Честно НЕ сделано (items 7-8, и явно оставленные внутри item 5):
полноценный выбор корабля при нескольких управляемых кораблях (сейчас
только "alpha"); polished command UI (это явно вне рамок чеклиста);
win/lose экран (item 7); Windows-экспорт (item 8).


## 2026-09-23 (проход: §56.1 item 4 -- минимальный HUD)

Реализован минимальный HUD (`scripts/hud.gd`) -- по §25.1 без health-баров,
чисто текстовый `Label` внутри `CanvasLayer`: список состояния всех
подсистем корабля (`ShipSubsystems.get_condition()` в процентах),
текущая designated-цель оружия и список сенсорных контактов
(`world.sensor_contacts`). Точка обзора HUD на этот срез -- один корабль
(`alpha`, константа `HUD_POV_SHIP_ID` в `main.gd`), по собственной
оговорке чеклиста ("pick ONE ship... unless showing both is trivial").

Добавлен новый read-only метод `SimulationWorld.
get_weapon_target_designation(ship_id)`: сознательно переиспользует
ТЕ ЖЕ приватные хелперы, что и сама AI (`_resolve_weapon_target` +
`_hostile_ship_ids`), а не отдельную копию логики выбора цели -- так
HUD физически не может показать цель, расходящуюся с той, по которой
реально выстрелит оружие в следующий тик.

`main.gd`: обоим демо-кораблям (alpha/beta) теперь присваивается
`ShipSubsystems.new()` (раньше `.subsystems` оставался `null`) -- без
этого "per-ship subsystem condition" в HUD было бы показывать нечего.
Больше в демо-сценарии ничего не меняется.

Проверено (полный набор симуляционных тестов по правилу §56.1
"fast visible-result" не гонялся):
1. Одноразовый (не закоммиченный) headless-скрипт на `SceneTree`,
   воспроизводящий wiring `main.gd._ready()` и гоняющий
   `world.tick_simulation()` вручную 300 тиков (~5с) -- HUD-текст после
   прогона показал все 11 подсистем по 100%, `TARGET: beta`,
   `CONTACTS: beta: DETECTED`, что совпало с независимым вызовом
   `world.get_weapon_target_designation("alpha") == "beta"`.
2. Headless `--import` (чисто, без ошибок) + headless прогон
   `res://scenes/main.tscn --quit-after 120` -- по-прежнему ровно 16
   повторений уже задокументированного dummy-renderer артефакта
   `mesh_get_surface_count`/"Parameter m is null" (та же база, что и
   в конце прохода item 6) -- HUD (чистый `CanvasLayer`/`Label`, без
   мешей) не добавил ни одного нового типа ошибки/предупреждения в
   живом прогоне сцены.

Честно НЕ сделано (items 5, 7, 8): ввод приказов на хоткеях (item 5,
следующий пункт); win/lose (item 7); Windows-экспорт (item 8).


## 2026-09-22 (проход: обнаружен и исправлен параллельный внешний коммит `5cd067c "test commit@"` -- §41.1 Crossing the T доведён до рабочего состояния)

В начале прохода `git log` показал коммит `5cd067c` поверх `origin/main`
с сообщением "test commit@", НЕ сделанный этой scheduled-задачей (автор
Honorverse Dev, но добавляет `.continue/agents/*.yaml` -- конфиги
JetBrains/Continue-плагина, явный след другого инструмента/локального
запуска пользователя, а не этой сессии). Коммит содержал реальную,
частично рабочую работу вперемешку с НЕПРОВЕРЕННЫМ, НЕ ПРОХОДЯЩИМ импорт
кодом -- прямое нарушение правила AGENTS.md §1 "каждая механика --
реально прогнанный код", раз оно попало в git. Разобрано и исправлено
по частям, а не откачено целиком:

* **Реально рабочее, оставлено как есть:** §62.1 mass-based physics
  (`ship_physics_state.gd` -- `max_thrust_n` + `effective_max_thrust()`,
  `a = F/m`, с полностью обратно-совместимым `max_acceleration_mps2`
  computed-property и `effective_max_acceleration()`-обёрткой -- ни один
  существующий вызывающий код не пострадал); §23.1 Active vs Passive
  Sensing (документация в AGENTS.md, без кода); новые поля
  `SensorContact.estimated_orientation`/`estimated_angular_velocity`
  (были объявлены, но НИЧЕМ не заполнялись -- см. отдельный пункт ниже).
* **СЛОМАНО и не проходило `--import` вообще (ДОБАВЛЕНО этим проходом,
  не входило в тот коммит):**
  - `simulation_world.gd`: `tick_simulation()` вызывала
    `_resolve_crossing_t_maneuver(dt)`, а `_resolve_individual_orientation_order`
    вызывала `_steer_toward_world_facing(...)` -- ОБЕ функции не
    существовали вообще нигде в проекте (проверено `grep -rn` по всему
    `project/`). Реализованы честно, не заглушкой: `_steer_toward_world_facing`
    -- вынесенная в общий хелпер СТАРАЯ рабочая логика поворота (EXACT-
    STOP по `max_angular_speed_rad_s`), которая раньше была inline-кодом
    прямо в `_resolve_individual_orientation_order` (сам коммит эту
    старую логику УДАЛИЛ, заменив вызовом несуществующей функции --
    честно говоря, без этого исправления §30 CHANGE_ORIENTATION был бы
    СЛОМАН, а не только §41.1); `_resolve_crossing_t_maneuver` -- новый
    per-tick проход, дающий любому кораблю без более конкретного §30-
    приказа и не удерживаемому формацией (`_is_station_kept_formation_member`,
    новый хелпер) маневр от `TacticalAI.compute_crossing_t_maneuver`
    (эта функция в исходном коммите РЕАЛЬНО была написана корректно и
    осмысленно -- просто никогда не вызывалась).
  - `point_defense_mount.gd`/`point_defense_resolution.gd`: новое поле
    `sector: AttackGeometry.Sector` (это ENUM, не класс) и вызов
    `mount.sector.is_inside(...)` -- метода `is_inside` на enum-значении
    не существует ни в каком виде, парс-ошибка. В отличие от
    `_resolve_crossing_t_maneuver` эта фича была ПОЛНОСТЬЮ мёртвым кодом
    -- `grep -rn "\.sector\s*="` по всему проекту не находит ни одного
    места, где `mount.sector` хоть раз устанавливается в не-null (ни в
    коде, ни в тестах) -- то есть ветка `if mount.sector != null` никогда
    не могла бы выполниться, даже если бы скомпилировалась. Честно
    УДАЛЕНО целиком (оба поля + проверка), а не исправлено -- реальный
    §22.1 "сектор покрытия ПРО" (формационное взаимное прикрытие) по-
    прежнему отдельная, не начатая задача (см. ASSUMPTIONS.md), это была
    непроверенная, невостребованная заглушка под тем же номером, не
    настоящая реализация.
  - `tests/test_crossing_t_maneuver.gd` (новый файл в том же коммите):
    `var dot_with_los := abs(thrust.normalized().dot(los))` -- статический
    анализатор GDScript 4.3 не может вывести конкретный тип из
    полиморфного `abs()`, что делает `is_perpendicular := dot_with_los <
    0.5` невыводимым типом -- парс-ошибка ("Cannot infer the type").
    Исправлено явной типизацией (`absf()` + `: float`/`: bool`), без
    изменения логики теста.
* **Дополнено этим проходом (не в исходном коммите), чтобы §41.1 не
  читал фиктивные данные:** `SensorResolution.update_contact()` теперь
  реально заполняет `estimated_orientation`/`estimated_angular_velocity`
  из истинной ориентации/угловой скорости цели В МОМЕНТ обнаружения (тот
  же уровень честности "точно в радиусе действия, ничего за его
  пределами", что уже применяется к `estimated_position`/`estimated_velocity`
  -- в этом кодовой базе вообще нет модели шума ни для одного
  estimated-поля). Без этого правки `compute_crossing_t_maneuver` всегда
  читал бы `Quaternion.IDENTITY` (поле объявлено, но никогда не
  записывалось никаким кодом до этого прохода) -- т.е. считал бы, что
  ЛЮБОЙ враг всегда смотрит вдоль мировой -Z, что не является читерством
  (не использует истинные данные напрямую), но и не является честной
  sensor-based оценкой, как заявлено в комментариях того же коммита.
  При потере контакта (dead-reckoning/ESTIMATED/LOST) ориентация
  намеренно НЕ экстраполируется дальше (остаётся последней известной) --
  честно узкий выбор, симметричный тому, что позиция/скорость там ЖЕ
  экстраполируются, а ориентация пока нет (см. ASSUMPTIONS.md).
* `tests/test_tactical_ai_retreat.gd::_test_healthy_ship_still_fires_and_does_not_retreat`
  -- существовавший ДО этого прохода тест проверял, что здоровый корабль
  получает `commanded_thrust_local == Vector3.ZERO` (правильно на момент
  написания -- у здоровых вступающих в бой кораблей раньше вообще не
  было маневрирующего AI). После включения §41.1 это стало ложным
  ожиданием -- не регрессия, а честно устаревший инвариант теста.
  Переписан на то, что тест на самом деле должен проверять по своему
  названию: что §26 disengage-правило НЕ сработало (корабль не
  отступает, т.е. thrust НЕ направлен в сторону +X, "от" врага, симметрично
  проверке в соседнем тесте на реально повреждённом корабле).
* Полный прогон headless-тестов (Godot v4.3-stable_linux.x86_64, эта
  сессия, бинарь скачан заново -- в `$HOME/godot_tmp` его не было):
  ВСЕ 30 файлов `project/simulation/tests/test_*.gd` зелёные (exit code 0
  каждый), включая новый `test_crossing_t_maneuver.gd`.
* `.gitignore` -- добавлен `.idea/` (JetBrains-кэш, попал в untracked
  файлы этого прохода, не относится к проекту).
* AGENTS.md §41.1/CLOUD.md §2.10 -- статус Crossing the T обновлён с
  OPEN на IMPLEMENTED (первый срез), с честными границами (см. ниже в
  ASSUMPTIONS.md).
* Честно НЕ сделано: §22.1 формационное покрытие ПРО (сектор покрытия
  PD-установки) по-прежнему не начато -- ложная заглушка под этим
  номером убрана, не заменена настоящей реализацией в этот проход;
  формационный ("wall") Crossing the T как командный приказ для целой
  формации; настоящий intercept-vector solver (§41.1 сейчас использует
  ту же прямолинейную идеализацию горизонта закрытия, что и §34.2);
  шумовая модель для `estimated_orientation` при потере контакта
  (сейчас просто замораживается на последнем известном значении).


## 2026-09-19 (проход: §34.1 Doubling -- формационная координация распределения целей)

По списку пользователя от 2026-09-19 (по возрастанию номера раздела ТЗ,
после закрытия §33.1 предыдущим проходом) -- следующий по порядку пункт,
§34.1 Doubling, реализован. §34.2 (missile TOT) и §41.1 (Crossing the T)
по-прежнему открыты -- см. ниже.

* `tactical_ai.gd` -- новая чистая функция
  `TacticalAI.select_formation_target_for_member(ship, contacts,
  hostile_ids, hulls, assigned_counts, max_useful_attackers=2,
  critical_fraction=0.3)`. Тот же контракт "no cheat vision", что и у
  `select_weapon_target` (только USABLE контакты, дистанция по
  `estimated_position`), плюс два дополнительных координационных сигнала:
  цель, чей отслеживаемый hull integrity уже <= `critical_fraction` (тот
  же порог, что `is_critically_damaged` уже использует для СВОЕГО
  отступления -- переиспользован, а не изобретён заново), считается уже
  эффективно нейтрализованной; цель, уже "занятая" `max_useful_attackers`
  (ASSUMPTION, по умолчанию 2 -- канонической цифры "сколько кораблей
  реально усиливают друг друга по одной цели" не существует) или больше
  соратниками по формации в этом же тике, считается уже достаточно
  обстреливаемой. Трёхуровневый выбор (не занятая и не нейтрализованная →
  занятая, но не нейтрализованная → нейтрализованная) гарантирует, что
  корабль НИКОГДА не остаётся без цели просто потому, что все видимые
  враги оказались "невыгодными" -- честный fallback, тот же принцип, что
  и везде в этом модуле. При пустых `assigned_counts` и отсутствии
  нейтрализованных целей функция ТОЧНО повторяет `select_weapon_target`.
* `simulation_world.gd` -- новая функция `_resolve_formation_target_assignment`,
  выполняется в `tick_simulation` ПОСЛЕ `_resolve_damage_response` и ПЕРЕД
  `_resolve_weapons_ai`/`_resolve_missile_launch_ai`. Для каждой формации
  с действующим (не потерянным, `TacticalAI.is_guide_lost`) guide --
  "выполняется от лица guide-корабля в иерархии команд", как и требует
  формулировка §34.1 -- обходит участников в детерминированном порядке
  (guide, затем `member_offsets.keys()`) и для каждого, кто СПОСОБЕН сам
  выбрать цель в этот тик (есть команда/team, не критически повреждён, не
  под приказом "hold fire", и НЕ имеет активного ручного §30-назначения --
  явный приказ командира по-прежнему приоритетнее автоматики, та же
  иерархия приоритетов, что уже используется everywhere в этом файле),
  вызывает `select_formation_target_for_member` с общим для формации на
  этот тик счётчиком `assigned_counts`, так что выбор второго участника
  уже "видит" выбор первого -- настоящая последовательная координация, не
  просто одинаковые независимые догадки. Результат кладётся в новый
  словарь `_formation_assigned_targets[ship_id]`, полностью
  перестраиваемый каждый тик (без "залипания" между тиками).
* `_resolve_weapon_target` (общая политика выбора цели для энергооружия
  И пусковых ракет) расширена третьим приоритетом: (1) ручное §30-
  назначение, (2) координированное §34.1-назначение этого тика, (3)
  прежний автоматический "ближайший враг". Устаревшая/более не валидная
  запись на любом уровне честно проваливается на следующий -- тот же
  принцип, что уже был у ручного назначения.
* Формация без валидного guide (в процессе передачи командования, §33)
  НЕ получает координированного назначения в этот тик -- её участники
  просто используют прежний независимый fallback, ровно как до этого
  прохода (нет "фантомного координатора" для обезглавленной формации).
* `test_tactical_ai.gd` -- 6 новых unit-тестов на
  `select_formation_target_for_member` (вырождение в
  `select_weapon_target` без координации; уклонение от уже "занятой"
  цели при наличии свободной; всё равно выбор занятой/нейтрализованной
  цели, если других нет; деприоритизация нейтрализованной цели; null при
  отсутствии врагов). `test_formation.gd` -- 2 новых сквозных теста через
  `SimulationWorld.tick_simulation()`: формация guide+2 wing с двумя
  враждебными кораблями (один близкий, один далёкий) -- первые два
  участника в детерминированном порядке дублируют огонь по ближней цели
  (2 <= дефолтного cap), третий уходит на дальнюю, а не садится третьим
  на уже дважды занятую; отдельно -- формация без guide не получает
  координированного назначения вовсе. Итого 19 assertion-тестов в
  test_tactical_ai.gd (было 13), 113 в test_formation.gd (было 109), все
  29 headless-тестов проекта зелёные (Godot v4.3-stable, эта сессия,
  включая `--headless --path project --import` после правок без ошибок).

Честно НЕ сделано этим проходом (следующие кандидаты по списку
пользователя, по возрастанию): §34.2 Missile time-on-target -- этот
проход координирует только ВЫБОР цели, не момент запуска/подлёта ракет
к ней (staggered launch для synchronized arrival остаётся отдельной,
ещё не начатой задачей); §41.1 Crossing the T -- манёвренный
intercept-geometry solver по-прежнему не существует вообще, Tactical AI
всё ещё не умеет вычислять курс для выхода на выгодный угол, только
выбирает цель среди уже видимых контактов.

## Unreleased — Milestone 1: engine skeleton + 3D world + simulation loop

* Выбран стек: Godot 4.3 / GDScript (см. docs/ASSUMPTIONS.md).
* Добавлен `project/` — минимальный Godot 4 проект.
* `simulation/sim_clock.gd` — детерминированный fixed-timestep clock
  (60 Hz, time scale ТЗ §44, pause/single-step).
* `simulation/ship_physics_state.gd` — 3D инерционная физика корабля
  (position/velocity/acceleration/orientation как Vector3/Quaternion,
  thrust→accel→velocity→position, relative velocity, closure velocity,
  clamp скорости ниже c). ТЗ §6, §11-15.
* `simulation/simulation_world.gd` — держит корабли, тикает от SimClock.
* `scripts/ship_view.gd`, `scripts/main.gd` — рендер-слой, 2 корабля-заглушки
  (BoxMesh) на встречных курсах, для проверки связки Simulation/Rendering.
* `simulation/tests/test_ship_physics_state.gd` — headless smoke-тесты
  (thrust, инерция при нулевой тяге, clamp по c, relative velocity/closure).
  **Прогнаны в этой сессии** через Godot v4.3-stable_linux.x86_64:
  `ALL TESTS PASSED`, exit code 0.
* Импорт проекта и запуск `main.tscn` headless проверены без ошибок скрипта.

### Явно НЕ сделано (не путать с готовым, ТЗ §58/§59)

Wedge, sidewalls, оружие, ракеты, контрракеты, point defense, сенсоры, ECM,
damage по подсистемам, tactical AI, формации, command hierarchy, replay,
сценарии, UI, экспорт в .exe. Всё это — следующие Milestone (ТЗ §56).

## Unreleased — реструктуризация документации

* Пользователь добавил в корень проекта CLOUD.md, ASSUMPTIONS.md,
  CANON_RULES.md с расширенной канон-спецификацией (wedge, sidewalls,
  missiles/laserheads, sensors/ECM, damage, formations/command lag).
* Перенёс ARCHITECTURE.md/BUILD.md/CHANGELOG.md из `docs/` в корень —
  структура репозитория теперь точно соответствует AGENTS.md §5.
* Объединил дублирующиеся CANON_RULES.md и ASSUMPTIONS.md (docs/ + корень)
  в единые файлы в корне.
* Сверил через веб-поиск (Honorverse Wiki) два числовых утверждения из
  нового CANON_RULES.md: ускорение ракет ("thousands of gravities" →
  уточнено примером 46,000G/180s для конкретного класса, не универсальная
  константа) и дистанция поражения laserhead ("thousands of km" → уточнено
  25,000 км для конкретного примера). Остальные пункты CANON_RULES.md
  (Krennikov Coupler, механика overload sidewall, ECM Ghost Rider) пока
  НЕ сверены с первоисточником — помечены TODO.
* `docs/` удалена как каталог (дубли слиты в корневые файлы).

## Unreleased — Milestone 3: Impeller Wedge + Sidewalls + ориентация

* Добавлена вторая сверка канона по оставшимся пунктам CANON_RULES.md —
  см. предыдущую запись выше в этом файле и сам CANON_RULES.md.
* `simulation/attack_geometry.gd` — классификация направления атаки
  (6 секторов) относительно ориентации корабля.
* `simulation/ship_defense_state.gd` — раздельная модель wedge/sidewalls
  (не shieldHP), включая канон про уязвимость носовой стенки под острым
  углом и блокировку ускорения при поднятой носовой/кормовой sidewall.
* `ship_physics_state.gd`: интеграция тяги теперь учитывает
  `defense.bow_sidewall_raised/stern_sidewall_raised`.
* `scripts/main.gd`: оба демо-корабля получили `ShipDefenseState`.
* Новые headless-тесты `test_wedge_sidewall.gd` — 8 проверок, все пройдены.
* Все новые численные константы — задокументированные ASSUMPTION-placeholder
  (ASSUMPTIONS.md), не выданы за канон.

### Явно НЕ сделано на этом Milestone

Визуализация wedge/sidewall геометрии в 3D (только логика решения, без
рендер-мешей), оружие/урон от резолва атаки (resolve_attack() пока не
подключён ни к какой системе оружия — это Milestone 4), damage-модель
подсистем, влияющая на condition sidewalls/wedge.

## Unreleased — Milestone 4: Energy Weapons

* `weapon_data.gd`, `weapon_mount.gd`, `hull_state.gd`,
  `weapon_resolution.gd` — первая рабочая цепочка урона: range → cooldown →
  огневой сектор → wedge/sidewall → hull damage.
* 6 новых headless-тестов, все пройдены (Godot v4.3-stable, exit 0).
* Демо-сцена: корабли получили корпус и оружие (не стреляют автоматически).
* Все числовые допущения (дистанция, урон, recharge) зафиксированы как
  PLACEHOLDER в ASSUMPTIONS.md — канон таких чисел не даёт.

### Явно НЕ сделано

Ракеты/laserhead, контрракеты, point defense, сенсоры/ECM (weapon
resolution пока не проверяет обнаружение цели — целится "в слепую"),
полноценный subsystem damage (§25), AI/ручное управление огнём (никто
пока не вызывает `WeaponResolution.fire()` в игровом цикле), визуализация
выстрелов/трассеров.

## Unreleased — Milestone 5: Missiles + Guidance

* Вынес общую кинематику кораблей/ракет в `kinematics_utils.gd`
  (устранил дублирование clamp-по-c и интеграции, которое появилось бы
  при копировании логики `ShipPhysicsState` в `MissileState`).
  `ShipPhysicsState` отрефакторен на использование этого модуля.
* `missile_state.gd` — ракета как полноценная 3D-сущность симуляции
  (§18), с ограниченным по времени горением двигателя (канон-пример
  46 000G/180с из CANON_RULES.md) и переходом в баллистический полёт
  после выгорания.
* `missile_guidance.gd` — предиктивное наведение (lead pursuit по
  target velocity), не прямое "на текущую позицию цели" (§19). Явная
  обработка потери захвата (SensorState.LOST) без "телепортации" наводки.
* 5 новых headless-тестов, все пройдены; полный набор из 4 файлов тестов
  (19+ проверок) — все зелёные после рефакторинга.
* Все числа (терминальная дистанция подрыва, время жизни) —
  задокументированы как PLACEHOLDER в ASSUMPTIONS.md, отделены от
  цитируемого канона (46 000G/180с — реальный пример, не выдумка).

### Явно НЕ сделано

Настоящий подрыв laserhead и урон по цели через `ShipDefenseState`
(missile долетает до TERMINAL, но пока не наносит урон — это следующий
проход), контрракеты, point defense, ECM влияние на sensor_state, пусковые
трубы/боезапас корабля, визуализация полёта ракеты.

## Unreleased — Milestone 5 (продолжение): Laserhead Detonation

* `missile_resolution.gd` — ракета теперь реально наносит урон через
  wedge/sidewall при подрыве (закрыл пробел, отмеченный в предыдущей
  записи этого файла).
* 4 новых теста, все пройдены. Итого 5 файлов тестов / 23 проверки,
  всё зелёное на headless Godot 4.3.

### Явно НЕ сделано

Контрракеты, point defense, влияние ECM на sensor_state ракеты, пусковые
трубы/боезапас корабля (никто пока не запускает MissileState в игровом
цикле — только прямые unit-тесты), точная геометрия lasing rods,
визуализация полёта/подрыва.

## Unreleased — доработка ТЗ: механика laserhead по книгам (по запросу пользователя)

* AGENTS.md §21 (Laserheads) переписан: полная каноническая
  последовательность подрыва (разведение штанг → позиционирование ~100м
  перед боеголовкой → кольцо гравитационных генераторов → гауссова
  фокусировка → независимо наводимые лучи), количество/тип штанг по
  классам (Mark 23 vs Mark 13), эффективный радиус поражения (25 000 км),
  явно перечислено, что остаётся UNKNOWN (точный триггер подрыва, энергия
  луча на штангу, влияние ECM на наведение штанг отдельно от общего
  наведения ракеты).
* CLOUD.md §2.2 дополнен той же механикой + справочные ТТХ реальных
  классов ракет (Mark 23 MDM, Mark 31, Viper, Apollo control missile) —
  как примеры для конкретных классов, не универсальные константы.
* CANON_RULES.md — третья сверка, источники процитированы.
* ASSUMPTIONS.md — зафиксирован ИЗВЕСТНЫЙ разрыв между обновлённым ТЗ и
  текущей реализацией `missile_resolution.gd` (один луч из позиции ракеты
  вместо разведённых штанг) — TODO для следующего прохода по ракетам,
  НЕ исправлено в этом проходе (этот проход — доработка документов по
  прямому запросу, не код).

## Unreleased — реализация: разведение штанг laserhead (закрытие TODO)

* `lasing_rod.gd` (новый), `missile_state.gd` (rod_count/rod_offset_m/
  rod_spread_half_angle_rad), `missile_resolution.gd` (полностью
  переписан: `compute_rod_positions()` + резолюция каждой штанги
  независимо, суммирование урона, `rod_results` в `DetonationResult`).
* 3 новых теста, все пройдены. Полный набор: 5 файлов / 26 проверок,
  все зелёные (headless Godot 4.3, exit 0 везде).
* Закрыт TODO из предыдущей записи ("один луч из позиции ракеты" →
  честное разведение штанг по канону).

### Явно НЕ сделано

Mark-13-style суббоеприпасы с независимым выбором разных целей (нужен
мультитаргетинг ракеты — MissileState пока несёт один target), визуализация
штанг, контрракеты, point defense, влияние ECM на наведение.

## Unreleased — Milestone 6a: Counter-Missiles + доработка ТЗ по физике полёта ракет

* По ссылке пользователя (Honorverse Wiki, Missile) вытащил детали физики
  полёта (трёхфазная модель, дросселирование, MDM-ступени, вращение для
  PD, mass driver для выхода за клин носителя) и поражающих факторов
  (laserhead эффективнее против sidewall, чем kinetic/fusion) — дополнил
  AGENTS.md §18.1 (новый) и §21.25 (новый), CLOUD.md §2.2.1 (новый).
* `counter_missile_resolution.gd` (новый) + рефакторинг `missile_state.gd`
  (`target` untyped, `GuidanceState.INTERCEPTED`) — контрракеты реально
  наводятся на входящие ракеты (переиспользуя MissileGuidance) и
  перехватывают их по каноническому механизму наложения клиньев.
* `missile_state.gd`: `set_throttle()` + `estimated_powered_range_m()` —
  дросселирование двигателя как реальная механика, не только текст в ТЗ.
* 7 новых тестов (5 counter-missile + 2 throttle/range). Полный набор:
  6 файлов тестов, 38 проверок, все зелёные (headless Godot 4.3, exit 0).
* Честно зафиксирован НОВЫЙ известный пробел: sidewall attenuation пока
  не различает тип атакующего оружия (laserhead vs energy vs kinetic) —
  канон явно требует разницы, код пока её не даёт. TODO для будущего
  прохода.

### Явно НЕ сделано

Point defense (лазерные кластеры для перехвата ракет, отдельно от
контрракет), многоступенчатые MDM, вращение ракеты, пусковые трубы,
damage-type-зависимое ослабление sidewall, визуализация.

## Unreleased — закрытие TODO: damage-type sidewall + переход к прототипу/моделям

* Реализован damage-type-зависимый sidewall (см. выше) — закрыт TODO из
  предыдущей записи. Полный набор тестов: 6 файлов / 39 проверок, все
  зелёные.
* Пользователь попросил переключиться на сборку видимого прототипа
  (модели/визуал) — следующая запись этого файла будет про это, ТЗ-доработка
  временно отложена, но не забыта.

## Unreleased — Первый визуальный прототип

* Процедурная модель корпуса ("flattened spindle" по канону, см. CANON_RULES.md
  пятая сверка) вместо BoxMesh-заглушки — `hull_mesh_builder.gd`.
* Визуализация wedge (полупрозрачные плоскости, честно читают
  существующее состояние `ShipDefenseState`).
* Найден и исправлен баг Milestone 1: камера демо-сцены на самом деле
  никогда не смотрела на корабли (обнаружено только сейчас, при первой
  реальной визуальной проверке — headless-тесты этого не ловят).
* Чёрный фон космоса вместо серого по умолчанию.
* Dev-инструменты для скриншотов через Xvfb (screenshot_capture,
  closeup_capture) — оставлены в проекте как debug-утилита.
* Реально сгенерированы и просмотрены 2 скриншота, отправлены
  пользователю.
* Новый тест-файл `test_hull_mesh_builder.gd` (3 проверки). Полный
  набор: 7 файлов тестов, все зелёные.
* Зафиксирована известная нерешённая проблема: нет origin rebasing /
  double precision для больших (боевых, сотни тысяч - миллионы км)
  дистанций — актуально не для этого прототипа, но для реального боя.

### Явно НЕ сделано

Визуализация sidewalls (только wedge), настоящие per-class модели
кораблей (только одна процедурная заглушка на все корабли), текстуры/
детализация корпуса, UI/HUD поверх 3D-вида, origin rebasing для больших
дистанций.

## Unreleased — Milestone: Point Defense

* `point_defense_mount.gd` + `point_defense_resolution.gd` — PD как
  отдельный слой обороны от контрракет (§22), с временем реакции и
  накоплением попаданий (насыщаемая защита — большой залп может
  "перегрузить" один модуль). Переиспользует `GuidanceState.INTERCEPTED`.
* 6 новых тестов, все пройдены. Полный набор: 8 файлов / 47+ проверок,
  все зелёные.
* Честно зафиксировано: выбор цели, влияние сенсоров/ECM/повреждений —
  не реализовано, и ничто пока не вызывает `engage()` в игровом цикле
  (готовая логика без AI-подключения).

## Unreleased — Milestone: Point Defense + улучшение визуализации клина

* Point Defense (§22): `point_defense_mount.gd` + `point_defense_resolution.gd`,
  6 тестов, все зелёные (см. отдельную запись выше по деталям).
* По замечанию пользователя ("клин мне не нравится") — заменил плоский
  PlaneMesh-клин на процедурный V-образный `wedge_mesh_builder.gd`,
  3 новых теста. Полный набор: 9 файлов тестов, 50+ проверок, все зелёные.
* Новый скриншот сгенерирован и отправлен пользователю для подтверждения.

## Unreleased — Milestone: Sensors (§23), первая версия

* Первоисточник найден и учтён: "On Basilisk Station" гл.3 (Baen sample
  chapter mirror) подтверждает механизм гравитационных сенсоров и то,
  что корабль может снизить обнаруживаемость, погасив impellers.
  Записано в CANON_RULES.md "Седьмая сверка" — явно помечено как
  ОПРОВЕРГАЮЩЕЕ более раннюю неподтверждённую заметку ("третья сверка"),
  а не тихо переписывающее её.
* `simulation/contact_state.gd` — общий `ContactState.Type` enum;
  `MissileState.SensorState` теперь алиас на него (не дублирование).
* `simulation/sensor_contact.gd` + `simulation/sensor_resolution.gd` —
  новая система sensor-контактов: state machine DETECTED→TRACKED→
  ESTIMATED (dead-reckoning)→LOST, обнаружение по дальности +
  "испускает ли цель сигнатуру" (клин поднят / двигатель ракеты горит).
  Все пороговые числа — ASSUMPTION, канон цифр не даёт (см.
  ASSUMPTIONS.md).
* 13 новых тестов (`test_sensor_resolution.gd`), все зелёные. Полный
  набор: 10 файлов тестов, 60+ проверок, все зелёные (полный прогон
  подтверждён — ничего не сломано рефакторингом `SensorState`).
* Честно НЕ сделано: подключение к `MissileGuidance`/
  `PointDefenseResolution` (наведение и ПРО по-прежнему видят цель
  идеально — эта система пока изолированная логика, не подключена
  к живому циклу); ECM (§24) — не начато; активные сенсоры/типы
  сенсоров — не реализовано, используется одна пассивно-гравитационная
  модель.

## Unreleased — Подключение сенсоров к самонаведению ракет

* `MissileGuidance.update_target_tracking()` / `resolve_thrust_direction()`
  — ракета теперь МОЖЕТ наводиться по сенсорной оценке цели
  (`SensorContact`, dead-reckoning при потере контакта), а не по истинной
  позиции всегда. Старый путь (`compute_thrust_direction()` напрямую с
  `SensorState.TRACKED`) остаётся рабочим для обратной совместимости —
  ничего не сломано.
* Найден и исправлен баг до попадания в прод: на первом тике до первого
  обнаружения `sensor_state` был бы `UNKNOWN`, но `compute_thrust_direction`
  раньше держал курс только при `LOST` — ракета целилась бы в
  `Vector3.ZERO`. Исправлено: держит курс при любом "неиспользуемом"
  состоянии (UNKNOWN/LOST/UNCERTAIN), не только LOST.
* `MissileState.sensor_state` по умолчанию теперь `UNKNOWN` (было
  `TRACKED`) — честнее отражает "пока не подключено/не обнаружено"; все
  существующие тесты передают `TRACKED` явно и не затронуты.
* 11 новых тестов (`test_missile_guidance_sensors.gd`), все зелёные.
  Полный набор: 11 файлов тестов, 73+ проверок, все зелёные, полный
  прогон подтверждён.
* Честно НЕ сделано: детонация по терминальной дальности в `integrate()`
  всё ещё смотрит на истинную позицию цели, не на сенсорную оценку;
  Point Defense и контрракеты всё ещё не читают `SensorContact`; ECM
  (§24) не начато; ничто из этого пока не вызывается из живого игрового
  цикла — самого игрового цикла (тикающего все корабли/ракеты разом)
  всё ещё не существует, это следующий необходимый шаг для того, чтобы
  всё написанное реально управляло ходом боя, а не было изолированной,
  пусть и протестированной, логикой.

## Unreleased — Подключение сенсоров к Point Defense

* `PointDefenseResolution.engage()` — новый опциональный параметр
  `sensor_contact`: если передан, ПРО может стрелять только пока контакт
  DETECTED/TRACKED/ESTIMATED (новый `Outcome.NOT_DETECTED` иначе) —
  "нельзя стрелять по тому, чего не видишь". `null` (по умолчанию) —
  старое поведение без сенсорной привязки, обратная совместимость
  сохранена для всех существующих вызовов/тестов.
* 5 новых тестов (`test_point_defense_sensors.gd`), все зелёные. Полный
  набор: 12 файлов тестов, 78+ проверок, все зелёные, полный прогон
  подтверждён без регрессий.
* Честно НЕ сделано: контрракеты (`CounterMissileResolution`) всё ещё не
  сенсорно-завязаны; ECM (§24) не начато; ничто из сенсорной системы
  пока не вызывается из живого игрового цикла — самого игрового цикла
  ещё нет.

## Unreleased — Milestone 7 закрыт: ECM (§24)

* `ecm_state.gd` — данные: глушение (вкл/выкл + множитель дальности),
  ограниченный список decoy-позиций (канон: "only a handful... at a
  time" — реализовано как MAX_DECOYS=4).
* `sensor_resolution.gd` — глушение сокращает эффективную дальность
  обнаружения; decoy подменяет позицию цели, если ближе к наблюдателю
  (детерминированно, без RNG). Источник: Honorverse Wiki "Electronic
  warfare" (вторичный, книжный первоисточник для ECM не найден в этот
  проход — честно зафиксировано).
* 9 новых тестов (`test_ecm.gd`), все зелёные. Полный набор: 13 файлов
  тестов, 87+ проверок, все зелёные, регрессий нет.
* Milestone 7 ("Sensors + ECM", §56) закрыт на уровне протестированной
  логики. Честно НЕ сделано: decoy — не физические летящие дроны
  (только позиции, задаваемые вручную); ECM-поле не встроено по
  умолчанию в ShipPhysicsState; PD/контрракеты получают эффект ECM
  косвенно через SensorContact, отдельного API "заглушить ПРО" нет.
  Следующий логический шаг по §56 — Milestone 8 (Subsystem Damage,
  §25) либо, что более критично для игры в целом, первый настоящий
  живой игровой цикл, тикающий корабли/ракеты/ПРО/сенсоры вместе —
  пока вся протестированная логика существует изолированно.

## Unreleased — Milestone 8 начат: Subsystem Damage (§25)

* `subsystem_type.gd` + `subsystem_state.gd` + `ship_subsystems.gd` —
  контейнер из 11 подсистем ТЗ §25, каждая с condition 0..1.
* Первый реальный потребитель: `SensorResolution` (наблюдатель) читает
  свою `SENSORS` подсистему — повреждённые сенсоры сокращают
  эффективную дальность обнаружения, полностью выбитые блокируют
  обнаружение совсем. Прямая реализация примера из самого ТЗ.
* 33 новых теста (28 + 5), все зелёные. Полный набор: 15 файлов
  тестов, 120+ проверок, все зелёные, полный прогон без регрессий.
* Честно НЕ сделано: 10 из 11 подсистем есть, но не подключены ни к
  одному потребителю; нет логики распределения входящего урона по
  подсистемам; старые отдельные condition-поля (propulsion/weapon/PD/
  sidewall) не унифицированы с этим контейнером. Milestone 8 открыт, не
  закрыт.

## Unreleased — Milestone 8 продвинут: распределение урона по подсистемам

* `subsystem_damage_resolution.gd` — детерминированная (без RNG)
  секторная карта, решающая какая подсистема получает урон при
  попадании; STRUCTURAL_INTEGRITY получает долю от любого хита.
  Секторная карта — инженерная догадка, честно помечена как НЕ канон.
* `WeaponResolution.fire()` / `MissileResolution.resolve_detonation()`
  — новый опциональный параметр `target_subsystems`, обратная
  совместимость сохранена (по умолчанию `null`, старое поведение).
* 13 новых тестов, все зелёные. Полный набор: 16 файлов тестов, 130+
  проверок, все зелёные, полный прогон подтверждён без регрессий.
* Честно НЕ сделано: только SENSORS реально подключён к потребителю
  (SensorResolution); остальные 10 подсистем копят урон, но ничего его
  не читает; старые отдельные condition-поля не унифицированы с этим
  контейнером.

## Unreleased — Milestone 8 продвинут: PROPULSION/MANEUVERING → ускорение

* `ShipPhysicsState.subsystems` (опциональный `ShipSubsystems`) —
  `effective_max_acceleration()` теперь домножается на condition
  PROPULSION и MANEUVERING, если контейнер задан. Складывается со
  старыми полями `propulsion_condition`/`compensator_condition`, не
  заменяет их. Прямая реализация примера ТЗ §25.
* 6 новых тестов, все зелёные. Полный набор: 17 файлов тестов, 136+
  проверок, все зелёные, полный прогон подтверждён без регрессий.
* Статус Milestone 8: 3 из 11 подсистем (SENSORS, PROPULSION,
  MANEUVERING) реально влияют на поведение; урон распределяется по
  всем 11, но 8 из них пока без потребителя. Следующий логичный шаг по
  §56 — либо докрутить оставшиеся подсистемы (WEAPONS/POINT_DEFENSE →
  их существующие condition-поля; POWER/STRUCTURAL_INTEGRITY → пока
  не к чему подключать без более общей модели), либо переходить к
  Milestone 9 (Tactical AI, §26), которая уже назрела: до сих пор нет
  ни одного живого игрового цикла, который сам вызывал бы всю эту
  протестированную логику подряд, тик за тиком.

## Unreleased — Первый живой игровой цикл (Milestone 1 закрыт по существу)

* `simulation_world.gd` переписан: `tick_simulation(dt)` теперь реально
  тикает сенсоры, ракеты (наведение+полёт+детонация), контрракеты, ПРО
  и физику кораблей ВМЕСТЕ, каждый тик, а не только физику кораблей
  как раньше. Это первое место, где весь массив ранее написанных
  Milestones 2-8 работает совместно, а не только по отдельности в
  тестах.
* Честно ограничено: выбор цели ПРО — плейсхолдер ("ближайшая ракета,
  нацеленная на этот корабль"), не тактический AI (§26). Стрельба
  корабль-корабль не автоматическая (`fire_weapon()` — явный вызов).
* 8 новых сквозных тестов, все зелёные. Полный набор: 18 файлов
  тестов, 144+ проверок, все зелёные, полный прогон подтверждён без
  регрессий.
* Следующий логичный шаг по §56 — Milestone 9 (Tactical AI, §26): пока
  ничего не выбирает цели для оружия/ПРО осмысленно, только этот
  ближайший-по-дистанции плейсхолдер для ПРО.

## Unreleased — Milestone 9 начат: Tactical AI (§26), первая версия

* Новый модуль `tactical_ai.gd` (`TacticalAI`, статические функции):
  `select_pd_target(ship, contacts)` и
  `select_weapon_target(ship, contacts, hostile_ship_ids)`. Оба читают
  ТОЛЬКО `SensorContact` (estimated_position, state) — никогда истинную
  позицию/идентичность цели напрямую. Реализация "no cheat vision"
  (§26).
* Это одновременно ИСПРАВЛЯЕТ реальный баг: старый плейсхолдер ПРО в
  `simulation_world.gd` сканировал `missiles` по истинной позиции/
  идентичности — сам был нарушением §26, хоть и не назывался тактическим
  AI.
* `simulation_world.gd`: добавлена минимальная модель принадлежности —
  `teams: Dictionary`, `set_team()`, `is_hostile()` (оба корабля должны
  иметь непустую, РАЗНУЮ команду — иначе нейтрален, никогда не выбирается
  автоматически целью). `_resolve_point_defense` переписан на
  `TacticalAI.select_pd_target`. Новый `_resolve_weapons_ai(dt)`: каждый
  корабль с оружейными установками и назначенной командой сам выбирает
  ближайший пригодный вражеский контакт и стреляет всеми установками —
  через существующий `fire_weapon()`. `tick_simulation` теперь вызывает
  `_resolve_weapons_ai` после ПРО и перед физикой.
* Новый тестовый файл `test_tactical_ai.gd` (8 проверок) на сам модуль
  выбора цели изолированно. `test_simulation_world.gd` расширен двумя
  сквозными тестами: AI стреляет по вражескому кораблю, но не по
  ближайшему нейтральному без команды; AI не стреляет вообще, если
  команда не назначена ни одному кораблю.
* Полный набор: 19 файлов тестов, 155+ проверок, все зелёные, полный
  прогон подтверждён без регрессий.
* Честно ограничено (открыто для §26 дальше): нет взвешивания угрозы
  кроме "ближайший пригодный контакт", нет управления построением, нет
  выбора по манёвру/дистанции отдельно от ближайшего, нет решений о
  запуске ракет (только стрельба существующим оружием по существующим
  установкам), нет реакции на повреждения/потери, нет отступления.

## Unreleased — Milestone 9 продвинут: "respond to damage" / "retreat" / "disengage" (§26)

* `TacticalAI.is_critically_damaged()` + `select_retreat_vector_world()`
  (sensor-contact-only, no cheat vision) добавлены.
* `simulation_world.gd`: `_resolve_damage_response(dt)` заставляет
  критически повреждённый (≤30% целостности корпуса, ASSUMPTION-порог)
  корабль отступать от известных враждебных контактов;
  `_resolve_weapons_ai` перестаёт стрелять для такого корабля же тем же
  критерием.
* `test_tactical_ai_retreat.gd` — 13 новых проверок.
* Полный набор: 20 файлов тестов, 168+ проверок, все зелёные, без
  регрессий.

## Unreleased — Milestone 9 продвинут: "launch missiles" (§26)

* `missile_tube.gd` (`MissileTube`): ammo/cooldown/дальность пусковой
  установки, ASSUMPTION-плейсхолдеры.
* `simulation_world.gd`: `missile_tubes`, `add_missile_tube()`,
  `_resolve_missile_launch_ai(dt)` + `_launch_missile_from_tube()` — AI
  реально решает запускать ракеты по враждебным целям в пределах
  дальности трубы, тем же sensor-contact-only критерием, что и оружие;
  не запускает при отсутствии команды/враждебных/при отступлении.
* `test_missile_launch_ai.gd` — 15 новых проверок.
* Полный набор: 21 файл тестов, 183+ проверки, все зелёные, без
  регрессий.

## Unreleased — ТЗ дополнено: AGENTS.md §61 "Combat Philosophy — 'Nelson in a Skirt'"

* Из второго `Honorverse_Docs.zip` (примечания пользователя по боям) в
  AGENTS.md добавлена новая §61 — формальный design mandate: decisive
  battle mindset, geometry-as-weather-gauge, wall-of-battle formation
  fighting, concentration of force, calculated risk under incomplete
  information, commander-not-pilot, initiative within the plan.
* Краткий кросс-референс добавлен в CLOUD.md §2.7.
* ASSUMPTIONS.md: прежняя неформальная заметка про Nelson/Age-of-Sail
  обновлена — статус повышен до "раздел ТЗ" (было устное указание
  пользователя, теперь формальный §61).
* Архив `Honorverse_Docs.zip` и распакованная `Honorverse_Docs/`
  удалены из папки проекта после переноса содержимого — не часть
  исходников (см. CANON_RULES.md "Десятая сверка").

## Unreleased — Milestone 10 начат: Formation Command, "wall of battle" (§61)

* `formation_state.gd` (`FormationState`): ведущий + member_offsets в
  локальных координатах ведущего.
* `simulation_world.gd`: `formations`, `add_formation()`,
  `_resolve_formation_keeping(dt)` — станция-киппинг с мёртвой зоной,
  безопасный no-op без ведущего, disengage-отступление перекрывает
  удержание строя.
* `test_formation.gd` — 8 новых проверок.
* Полный набор: 22 файла тестов, 191+ проверка, все зелёные, без
  регрессий.

## Unreleased — ТЗ дополнено: AGENTS.md §62 "Inertia, Ship Mass, and Time Scale"

* Новая секция AGENTS.md §62: масса корпуса по классам (CANON,
  Honorverse Wiki), механика инерциального компенсатора и доктрины
  запаса безопасности (CANON), ускорения курьерских катеров до ~800G
  (CANON, остальные классы — UNKNOWN), разграничение тика симуляции и
  презентационного time control (§44), явная граница объёма проекта
  (тактическое реальное время, не гипер-транзит).
* Краткий кросс-референс добавлен в CLOUD.md §2.8.
* CANON_RULES.md: "Одиннадцатая сверка" — источники и цитаты.
* ASSUMPTIONS.md: честно зафиксирован текущий разрыв — `mass_kg` не
  подключён к формуле ускорения (F=ma), это ТОЛЬКО декларация ТЗ, не
  реализация; известный дефолт 500G НЕ привязан к конкретному классу.
* Это документационное обновление ТЗ, без изменений в коде/тестах.

## Unreleased — Milestone 10 продвинут: matching скорости в удержании строя (§29/§32)

* `simulation_world.gd` `_resolve_formation_keeping(dt)`: заменена
  прежняя "тяга-только-по-направлению" (bang-bang к желаемой позиции) на
  PD-регулятор (ошибка позиции + ошибка скорости относительно ведущего),
  закрывающий честно заявленный пробел прошлого прохода ("нет
  затухания/матчинга скорости — член может проскакивать станцию и
  колебаться"). Мёртвая зона теперь проверяет ОБА условия (позиция И
  скорость) — член на станции, но с рассинхронизированной скоростью,
  больше не остаётся без тяги. ASSUMPTION: коэффициенты K_P_STATION=0.02/
  K_D_STATION=0.9 — инженерный выбор (сильно передемпфированный ПД-
  регулятор), не канон; см. ASSUMPTIONS.md. Честно НЕ сделано (не
  входило в этот проход, открыто дальше): угловая скорость ведущего не
  учитывается при развороте офсета (только линейная скорость); резервный
  ведущий/перестроение при потере ведущего (§33) по-прежнему отсутствует.
* `test_formation.gd` — 2 новых теста (4 проверки): член на станции, но
  с ненулевой относительной скоростью, получает тормозящую тягу вместо
  нуля; член далеко от станции сходится к ней за много тиков под новым
  законом управления.
* Полный набор: 22 файла тестов, все зелёные, полный прогон подтверждён
  без регрессий (включая уже существующие 5 тестов `test_formation.gd`
  из первого слайса Milestone 10, которые остаются валидны без
  изменений под новым законом управления).
* ПРИМЕЧАНИЕ О ПАРАЛЛЕЛЬНОЙ РАБОТЕ: в начале этого прохода на диске уже
  был обнаружен новый коммит "Milestone 10 started: Formation Command
  first slice" (95daf7e) и, позже в том же проходе, ещё один
  ("Spec update: AGENTS.md §62...", 9fade27) — оба явно от другого
  параллельно выполняющегося запуска этой же автономной задачи над той
  же папкой проекта. Собственная первая попытка этого прохода (отдельные
  `formation.gd`/`formation_ai.gd`/`test_formation_ai.gd` с похожей, но
  independently-разработанной архитектурой) была отменена (не
  закоммичена, файлы перемещены в `_to_delete/`, не отслеживается git),
  чтобы не дублировать/не конфликтовать с уже принятой в git реализацией
  `formation_state.gd`. Дальнейшая работа в этом проходе построена НА
  ВЕРХУ уже принятой архитектуры, а не заново. Пользователю стоит знать:
  похоже, эта scheduled-задача может запускаться параллельно более чем
  одним экземпляром на одну и ту же папку — возможный источник будущих
  конфликтов git/файлов, стоит проверить настройку расписания.

## Unreleased — Milestone 10 продвинут: резервный ведущий / перестроение при потере ведущего (§33)

* Закрыт открытый пробел предыдущей записи ("резервный ведущий/
  перестроение... по-прежнему НЕ реализовано"). `FormationState` получил
  `succession_order`/`guide_lost_since`; `TacticalAI.is_guide_lost()` +
  `TacticalAI.select_formation_successor()` (новые статические функции,
  читают только истинное состояние `ships`/`hulls` — это своя-к-своим
  бухгалтерия, не "no cheat vision" §26 обнаружение врага); `simulation_world.gd`
  получил `COMMAND_TRANSFER_DELAY_S` (ASSUMPTION, 3с — честно НЕ то же
  самое, что каноническая скорость-света command lag из CANON_RULES.md
  §7, актуальная для флотских дистанций в миллионы км, а не для текущих
  тактических дистанций) + `world_sim_time` (накапливается вызовами
  `tick_simulation`, не зависит от `SceneTree`/`clock`) + новую
  `_transfer_formation_command()`, которая реально переносит команду
  преемнику и переcчитывает офсеты членов строя относительно НОВОГО
  ведущего (заморозка текущего относительного положения, а не
  переиспользование офсетов старого ведущего).
* 5 новых тестов в `test_formation.gd` (~14 проверок): регистрация
  цепочки команд; уничтоженный ведущий → передача первому в цепочке
  ПОСЛЕ задержки распознавания (не мгновенно); недееспособный ведущий +
  недееспособный первый преемник → пропуск ко второму, старый ведущий
  остаётся рядовым членом строя; fallback на порядок вставки Dictionary,
  когда цепочка команд не задана явно; полное отсутствие годного
  преемника → строй безопасно остаётся без ведущего, без падения
  симуляции и без "магической" выдумки преемника (§33: "Do not
  magically transfer information unavailable to subordinate ships").
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново, т.к. на машине не было
  сохранённого бинарника): все 22 файла `project/simulation/tests/`
  зелёные (exit code 0 каждый), включая обновлённый `test_formation.gd`
  (теперь 27 проверок в этом файле, было 12). Регрессий не найдено.
* Честно НЕ сделано (открыто дальше по Milestone 10): передача команды
  не переиздаёт формационные ПРИКАЗЫ §29 — новая геометрия строя вокруг
  нового ведущего не переpланируется, member-офсеты просто заморожены
  там, где корабли физически были в момент передачи; угловая скорость
  ведущего по-прежнему не учитывается формационным PD-регулятором;
  многоуровневая иерархия команд (element/division/squadron/task
  force/fleet, §28) не реализована; §33 step 4 "account for
  communication limitations" реализован как инженерная задержка
  распознавания (ASSUMPTION), а НЕ как реальная модель задержки по
  скорости света в зависимости от дистанции между кораблями — при
  текущих тактических (не флотских) дистанциях проекта разница
  ничтожна, честно зафиксировано, а не тихо выдано за один и тот же
  механизм.

## Unreleased — Milestone 10 gap closed: 4 of 8 subsystems without a damage consumer now wired (§25)

* `simulation_world.gd`: новая `_sync_subsystem_driven_conditions()`,
  вызывается первым шагом каждого тика. Синхронизирует
  `WeaponMount.condition` / `PointDefenseMount.condition` /
  `MissileTube.condition` (новое поле) из собственных подсистем
  корабля-носителя (`WEAPONS`/`POINT_DEFENSE`/`MISSILE_SYSTEMS`) — это
  прямая реализация того, что уже было заложено в комментариях этих
  полей ("damaged by §25 later" / "not yet modeled here"), просто
  никогда не подключено. Корабль без `ShipSubsystems` (subsystems ==
  null) не затрагивается — полная обратная совместимость.
* `weapon_resolution.gd`/`point_defense_resolution.gd` уже читали
  `mount.condition` — теперь оно реально живое, а не всегда 1.0:
  повреждённая WEAPONS пропорционально снижает урон выстрела
  (`damage_per_hit * mount.condition`), полностью уничтоженная
  обнуляет `is_ready()`; POINT_DEFENSE при `condition <= 0` отключает
  `is_ready()` (честно задокументировано как hard on/off, не
  непрерывная деградация — см. обновлённый class doc
  point_defense_resolution.gd).
* `missile_tube.gd`: новое поле `condition` + `is_ready()` теперь также
  требует `condition > 0.0` (MISSILE_SYSTEMS подключена).
* `counter_missile_resolution.gd`: `check_intercept()` получил новый
  опциональный параметр `counter_missile_system_condition` (по
  умолчанию 1.0, полностью обратная совместимость с каждым
  существующим вызовом) — масштабирует эффективный радиус
  wedge-overlap перехвата (`INTERCEPT_KILL_RADIUS_M * condition`).
  INTERPRETATION задокументирована в class doc: представляет
  бортовой fire-control launching-корабля, обеспечивающий терминальные
  поправки наведения уже выпущенному counter-missile (не сам полёт
  ракеты — тот управляется независимо через MissileGuidance).
  `simulation_world.gd`'s `_resolve_counter_missile_intercepts()`
  находит ВЛАДЕЮЩИЙ counter-missile корабль через `missile_owners` и
  подставляет его COUNTER_MISSILE_SYSTEMS condition; безхозный
  counter-missile (owner_id == "") или несуществующий владелец —
  честный fallback на 1.0 (пред-§25 поведение).
* `ship_subsystems.gd`: HONEST STATUS docstring обновлён — было 3 из 11
  реально подключённых consumer'ов (SENSORS/PROPULSION/MANEUVERING),
  стало 7 из 11. Честно НЕ подключены (заблокированы отсутствием более
  крупных систем, не просто "забыли"): COMMUNICATIONS (нет
  command/reporting §26/§28), POWER (нет power-budget модели),
  STRUCTURAL_INTEGRITY (HullState — временный §58 placeholder,
  требующий полной замены перед §59 DoD, подключать поверх него
  бессмысленно), DEFENSIVE_SYSTEMS (нет канонической разницы с уже
  существующими wedge/sidewall системами ShipDefenseState — что именно
  эта подсистема должна отдельно моделировать, UNKNOWN).
* **РЕАЛЬНЫЙ БАГ НАЙДЕН И ИСПРАВЛЕН** при написании нового теста для
  этого прохода (не гипотетический — падал с реальным SCRIPT ERROR):
  counter-missile (у которого `target` — другой `MissileState`, не
  корабль) вооружает боеголовку той же самой generic
  дистанционной проверкой, что и обычная ракета
  (`MissileState.integrate()` не различает тип цели). До исправления
  `simulation_world.gd`'s `_update_missiles(dt)` затем безусловно
  вызывал `MissileResolution.resolve_detonation()`, который читает
  `target.defense` — поля, которого нет на `MissileState` (оно есть
  только на `ShipPhysicsState`) — падение "Invalid access to property
  or key 'defense'". Баг был скрытым: ни один существующий тест
  counter-missile не проходит через `SimulationWorld.tick_simulation()`
  (все дергают `integrate()`/`check_intercept()` напрямую). Исправлено:
  `_update_missiles` теперь явно исключает laserhead-детонацию для
  ракет, чья цель сама является `MissileState` — counter-missile
  разрешается ИСКЛЮЧИТЕЛЬНО через `_resolve_counter_missile_intercepts()`
  (wedge-overlap), никогда через §21 laserhead/rod-детонацию против
  другой ракеты, что и так уже соответствовало документированной
  канонической механике в `counter_missile_resolution.gd`.
* Новый `test_subsystem_damage_consumers.gd` — 8 новых тестов (13
  проверок): синхронизация condition для всех 3 mount/tube типов;
  реальное снижение урона выстрела; полное отключение при
  `condition == 0`; корабль без ShipSubsystems не затрагивается;
  масштабирование радиуса перехвата counter-missile + обратная
  совместимость параметра по умолчанию; сквозной сценарий через
  `world.tick_simulation()`, который и поймал баг выше.
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново): все 23 файла
  `project/simulation/tests/` зелёные (exit code 0 каждый), включая
  новый файл. Регрессий не найдено.
* Честно НЕ сделано (открыто дальше): POINT_DEFENSE/MISSILE_SYSTEMS —
  только hard on/off, не непрерывная деградация точности/скорострельности
  как у WEAPONS; COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
  DEFENSIVE_SYSTEMS по-прежнему без consumer'а (см. выше, честные
  архитектурные блокеры); переданное командование формации по-прежнему
  не переpланирует §29 приказы под нового ведущего; угловая скорость
  ведущего по-прежнему не учитывается PD-регулятором удержания строя;
  многоуровневая иерархия команд §28 не реализована; Milestone 11+ не
  начаты.

## 2026-09-21 (проход: §62.1 Mass-based Physics)

Закрыт честно залогированный пробел из AGENTS.md §62 и CLOUD.md §2.8: `mass_kg` теперь реально интегрирован в формулу ускорения.

* `ship_physics_state.gd`: замена `max_acceleration_mps2` на `max_thrust_n` (Сила в Ньютонах).
* `ShipPhysicsState.integrate()`: ускорение теперь вычисляется как `a = F / m` (Force / mass_kg), что делает массу корабля определяющим фактором его маневренности.
* Переименован `effective_max_acceleration()` в `effective_max_thrust()` для соответствия физическим единицам (Ньютоны).
* Сохранена обратная совместимость с `propulsion_condition`/`compensator_condition` и подсистемами PROPULSION/MANEUVERING — они теперь масштабируют доступную ТЯГУ, которая затем делится на массу.


## 2026-09-18 (проход: continuous degradation для POINT_DEFENSE/MISSILE_SYSTEMS, §25)

Закрыт честно залогированный пробел: PointDefenseMount и MissileTube
раньше деградировали от урона подсистемы ТОЛЬКО как hard on/off
(`condition > 0.0` gate в `is_ready()`), в отличие от WEAPONS, где урон
выстрела уже масштабировался непрерывно через `mount.condition`.

* `point_defense_mount.gd`: три новых геттера —
  `effective_reaction_time_s()` / `effective_recharge_time_s()` (обе
  делятся на `condition`, floor 0.05 — время реакции/перезарядки растёт
  по мере повреждения) и `effective_engagement_range_m()` (умножается на
  `condition`, без floor — эффективная дальность честно падает до 0).
  Модель деградации ВРЕМЕНИ через деление, а не КАКОГО-ТО ДРУГОГО
  штрафа — INTERPRETATION, не канон (нет источника про то, КАК именно PD
  деградирует от частичного урона). Дальность масштабируется линейно —
  та же модель, что уже использована для `ECMState.jamming_range_multiplier`
  и `CounterMissileResolution.check_intercept()`'s condition-scaled
  intercept radius (согласованность с прошлым проходом, не новая с нуля
  придумка).
* `point_defense_resolution.gd`: `engage()` теперь читает эти effective_*
  геттеры вместо сырых `reaction_time_s`/`recharge_time_s`/
  `max_engagement_range_m` — при `condition == 1.0` поведение бит-в-бит
  идентично предыдущему (геттеры дают тождество), полная обратная
  совместимость с каждым существующим тестом/вызовом.
* `missile_tube.gd`: тот же паттерн — `effective_reload_time_s()` (делится
  на condition) и `effective_max_range_m()` (умножается). `is_ready()`
  теперь сравнивает с `effective_reload_time_s()` вместо сырого
  `reload_time_s`.
* `simulation_world.gd`: `_resolve_missile_launch_ai()`'s дальнобойная
  проверка (`distance > tube.max_range_m`) переключена на
  `tube.effective_max_range_m()`.
* `ship_subsystems.gd`: HONEST STATUS docstring обновлён — POINT_DEFENSE/
  MISSILE_SYSTEMS больше не описываются как "только hard on/off";
  документирован новый continuous-falloff путь.
* Новый `test_pd_missile_continuous_degradation.gd` — 24 проверки:
  тождество геттеров при `condition == 1.0` (обратная совместимость),
  корректное масштабирование при `condition == 0.5`/`0.25`, floor-защита
  от деления на ноль при `condition == 0.0`, реальное удлинение времени
  захвата цели и перезарядки PD, реальное сужение дальности PD и tube,
  respect эффективного reload_time в `MissileTube.is_ready()`, и один
  сквозной тест через `SimulationWorld.tick_simulation()` (тот же
  урок с прошлого прохода — стыковые баги ловит только сквозной тест).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 24 файла
  `project/simulation/tests/` зелёные (exit code 0), включая новый файл
  и оставшиеся 23 (Godot v4.3-stable_linux.x86_64, скачан заново в этом
  проходе — кэш от прошлого прохода в `$HOME` не сохранился между
  запусками VM). Регрессий не найдено, в т.ч. от параллельного прохода
  (см. ниже), чьи изменения (station-keeping угловая скорость) уже были
  в рабочем дереве до начала тестирования этого прохода.

Честно НЕ сделано (открыто дальше): ECM по-прежнему не деградирует
точность/трекинг PD (отдельный, не связанный с §25 пробел); §25
consumer'ов для COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
DEFENSIVE_SYSTEMS по-прежнему нет (архитектурно заблокированы, см. выше
и ASSUMPTIONS.md); переданное командование формации по-прежнему не
перепланирует §29 приказы под нового ведущего; многоуровневая иерархия
команд §28 не реализована; Milestone 11+ не начаты.

## ОБНАРУЖЕНА ПАРАЛЛЕЛЬНАЯ РАБОТА (четвёртый проход подряд с этим наблюдением)

В начале этого прохода `git log` показал 2 новых коммита (angular
velocity в station-keeping + очередная заметка о параллелизме),
которых не было в момент чтения СТАТУС-блока промпта, сделанные другой
сессией (`session_01GYmrSzDqT83f7u9ymfnxXe`) — при этом `list_triggers`
на Honorverse-задачу показывает РОВНО ОДИН зарегистрированный trigger
(`trig_01MR2jCcsytL2gPbDvhVgkG7`), чей `last_run.session_id` совпадает
с ЭТОЙ сессией, а не с той. Заметная деталь: у trigger'а `updated_at`
(20:14:49) отстаёт от его же `last_fired_at` (20:14:53) всего на 4
секунды — то есть предыдущий проход, судя по всему, вызвал
`update_trigger` практически в момент своего же повторного срабатывания,
хотя cron `59 */3 * * *` (минута 59, не 0) не должен подпадать под
описанное в документации инструмента поведение "anchor to now" (то
относится только к `0 * * * *`-подобным часовым паттернам). Причина
остаётся не выясненной до конца этой сессией — либо пользователь сам
вручную перезапускает задачу (`fire_trigger`) вскоре после завершения
предыдущего прохода, либо где-то раньше временно стоял более частый
cron и сейчас доигрывают уже запущенные с того момента долгие сессии.
Практического вреда в этом проходе не было (общий git-репозиторий на
диске, оба прохода работали над РАЗНЫМИ файлами/пробелами, конфликтов
слияния не было — не настоящий concurrent write, просто две сессии
активны в одном временном окне). Открытый вопрос пользователю ОСТАЁТСЯ:
стоит проверить, действительно ли что-то запускает эту задачу чаще, чем
раз в 3 часа.


## 2026-09-18 (проход: §29 Formation Orders — replan вместо freeze при передаче командования)

Закрыт честно залогированный пробел, упомянутый в нескольких предыдущих
записях подряд: "переданное командование формации не перепланирует §29
приказы под нового ведущего (member-офсеты просто заморожены)".

* `formation_state.gd`: новое поле `design_offsets: Dictionary` —
  ОРИГИНАЛЬНАЯ спланированная геометрия строя (ship_id -> Vector3 offset
  в собственной вращающейся системе координат строя), захватывается
  лениво один раз при ПЕРВОЙ передаче командования и больше не
  перезаписывается автоматически. В отличие от `member_offsets`
  (которое перезаписывается при КАЖДОЙ передаче, отражая, кто сейчас
  где стоит), `design_offsets` хранит неизменную "форму" строя.
* `simulation_world.gd`'s `_transfer_formation_command()`: теперь
  вычисляет новый offset выжившего корабля как
  `design_offsets[ship] - design_offsets[new_guide]` — та же
  относительная геометрия, что и всегда, просто пересчитанная
  относительно нового ведущего (строй "смыкается" вокруг преемника на
  исходном интервале), вместо замораживания фактической позиции
  корабля на момент передачи (старое поведение).
* Честный fallback: корабль без design-записи (присоединился к строю
  ПОСЛЕ того, как design уже был захвачен, например подкрепление) не
  имеет спланированной геометрии — для него (и для всех перевозимых в
  ЭТОЙ передаче кораблей, т.к. `can_replan` проверяется по наличию
  НОВОГО ведущего в design, не по каждому кораблю отдельно) сохраняется
  старое поведение "заморозить фактическую позицию". Результат такого
  fallback-прохода становится НОВОЙ design-базой, чтобы СЛЕДУЮЩАЯ
  передача снова могла перепланировать геометрически, а не повторяла
  fallback бесконечно.
* INTERPRETATION, не канон: нет источника Honorverse, описывающего, КАК
  именно перестраивается "стена" при потере ведущего — "сохранить
  спланированную относительную геометрию, сомкнутую вокруг нового
  ведущего" выбрано как наиболее естественное прочтение доктрины
  formation-keeping (строй — это ФОРМА, а не набор абсолютных позиций),
  не числовое допущение. См. ASSUMPTIONS.md.
* 3 новых/переписанных теста в `test_formation.gd` (было 34 проверки в
  файле, стало 40): `_test_transferred_command_replans_from_design_not_frozen_actual_position`
  (заменяет старый `_test_transferred_command_freezes_member_station_relative_to_new_guide`
  — доказывает, что offset теперь берётся из design, а НЕ из
  фактической/устаревшей позиции), `_test_transfer_falls_back_to_freeze_when_new_guide_has_no_design_entry`
  (честный fallback для позднего подкрепления без design-записи) и
  `_test_second_transfer_replans_from_refreshed_design_after_fallback`
  (сквозной сценарий из ТРЁХ последовательных передач командования,
  доказывающий, что после fallback design действительно обновляется и
  СЛЕДУЮЩАЯ передача снова перепланирует геометрически, а не повторяет
  fallback).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново — кэш от прошлых
  проходов в `$HOME` не сохраняется между запусками VM): все 24 файла
  `project/simulation/tests/` зелёные (exit code 0 каждый). Регрессий
  не найдено.

Честно НЕ сделано (открыто дальше): многоуровневая иерархия команд §28
(Fleet/Task Force/Squadron/Division/Element/Ship) не реализована —
сейчас есть только один плоский уровень "guide + members" на формацию;
остальные §29 Formation Orders (change course/speed/formation/attack/
target distribution и т.д.) как явные игровые ПРИКАЗЫ по-прежнему не
существуют как механика — реализовано только пассивное
formation-keeping + его переpланирование при смене ведущего, а не
активная выдача приказов игроком/ИИ; ECM по-прежнему не деградирует
точность/захват PD; COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
DEFENSIVE_SYSTEMS (§25) по-прежнему без consumer'а; Milestone 11+ не
начаты.

## Расследование параллельного исполнения scheduled-задачи — итог этого прохода

По просьбе пользователя проверена конфигурация самого trigger'а
(`list_triggers`, доступно из облачной сессии, не только из
device_bash): зарегистрирован РОВНО ОДИН trigger
(`trig_01MR2jCcsytL2gPbDvhVgkG7`) с cron `59 */3 * * *` — это
ПРАВИЛЬНЫЙ, ожидаемый интервал ровно раз в 3 часа (минута 59: 00:59,
03:59, ..., 21:59), никаких признаков более частого срабатывания на
уровне конфигурации нет. `last_fired_at` этого прохода (21:59:42)
ровно на 3 часа позже характерного времени предыдущих упоминаний
(18:59 и т.д.) — расписание само по себе не является причиной
наблюдавшихся 4 подряд "чужих" коммитов.

Рабочая гипотеза (не доказана, но правдоподобна и не проверялась
предыдущими проходами): `create_trigger`/`list_triggers` не
гарантируют, что предыдущий запуск ЗАВЕРШИЛСЯ до следующего
срабатывания по расписанию — если один проход по какой-то причине
выполняется дольше 3 часов (завис, застрял на скачивании Godot при
плохой сети, долгий тестовый прогон и т.п.), следующее срабатывание
по cron стартует ПАРАЛЛЕЛЬНО с ещё не завершившимся предыдущим,
создавая ровно ту картину, что наблюдалась 4 раза подряд (два
экземпляра, оба видят один и тот же git-репозиторий, оба коммитят
независимо) — без необходимости в "ручных" перезапусках или
неправильном cron. Это НЕ подтверждено (нет доступа к точной
длительности предыдущих сессий из этой сессии), но это самое простое
объяснение, совместимое со всеми фактами (единственный trigger,
корректный cron, `updated_at`≈`last_fired_at` объясняется тем, что
предыдущий проход вызвал `update_trigger` в конце СВОЕЙ работы, что
могло совпасть по времени с ОЧЕРЕДНЫМ штатным cron-срабатыванием, а не
с чем-то аномальным). Рекомендация пользователю: если проходы
регулярно занимают около или больше 3 часов — либо увеличить интервал
cron, либо это стоит просто принять как известную особенность (не
критично: общий git-репозиторий, оба прохода коммитят независимо, как
уже отмечено в предыдущих записях).

## 2026-09-18 (проход: §29 Formation Orders — явные ПРИКАЗЫ формации, а не только пассивное holding)

Закрыт крупный честно залогированный пробел, упомянутый во многих
предыдущих записях подряд: "остальные явные §29 Formation Orders (change
course/speed/formation/attack/target distribution и т.д. как ИГРОВЫЕ
ПРИКАЗЫ) не реализованы — есть только пассивное formation-keeping".
Этот проход даёт формации реальные, выполняемые приказы для ведущего
корабля, поверх уже существующего member station-keeping (которое не
менялось).

* Новый файл `formation_order.gd` (`FormationOrder`, class_name):
  Kind-энум `HOLD_FORMATION` / `CHANGE_COURSE` / `CHANGE_SPEED`.
  `CHANGE_COURSE` замораживает текущую скорость ведущего в момент выдачи
  приказа и перенацеливает направление; `CHANGE_SPEED` замораживает
  текущий курс и перенацеливает величину скорости — вместе они
  покрывают весь кинематический список §29 (change course/change
  speed/accelerate/decelerate). `withdraw_orders()` — составной
  хелпер (не новый Kind): сначала развернуться от угрозы, затем
  разогнаться до скорости отхода — покрывает §29 "withdraw"/"disengage"
  как композицию двух примитивов, честно не новая механика. Статический
  `hold_formation()` — явный "прекратить манёвр" приказ.
* `FormationState`: новые поля `current_order`/`order_queue` (§35
  Command Queue: "Orders can be queued") + методы `issue_order`/
  `issue_orders`/`issue_order_now` (немедленно прерывает текущий и
  очищает очередь — для срочных приказов типа disengage)/`clear_orders`.
* `simulation_world.gd`'s новая `_resolve_formation_orders(dt)`,
  запускается ПЕРЕД `_resolve_formation_keeping` в `tick_simulation`:
  переводит активный приказ формации в `commanded_thrust_local`
  ведущего корабля. Закон выполнения — EXACT-STOP clamp: ускорение
  всегда направлено точно на остаточную ошибку скорости, а величина
  ограничена `min(max_accel, error.length()/dt)` — то есть ведущий
  никогда не проскакивает цель за один тик, что даёт гарантированную
  монотонную сходимость БЕЗ колебаний и БЕЗ подбора коэффициентов (в
  отличие от bang-bang, который на типичных для этого кода ускорениях
  в сотни g проскочил бы цель на первом же тике, и в отличие от
  PD-контроллера station-keeping'а, которому нужны подобранные
  K_P/K_D). `HOLD_FORMATION` — не разовый манёвр: он НЕ завершается
  сам, а каждый тик явно обнуляет собственный manoeuvring thrust
  ведущего, пока его не заменят другим приказом. Формация без приказов
  (текущее и очередь пустые) — полный no-op: thrust ведущего не
  трогается вообще, поведение идентично коду ДО этого прохода (обратная
  совместимость подтверждена отдельным тестом).
* 7 новых тестов в `test_formation.gd` (было 52 проверки/17 функций,
  стало 59 проверок/24 функции): no-op без приказа; change_course
  реально разворачивает вектор скорости и завершается, сохраняя
  модуль скорости; change_speed реально разгоняет, сохраняя курс;
  hold_formation обнуляет thrust и НЕ завершается сама по себе;
  issue_order_now прерывает и текущий, и очередь; сквозной тест через
  `SimulationWorld.tick_simulation()` на очереди из ДВУХ приказов
  (разгон до 100, затем до 300 м/с) — доказывает, что очередь реально
  передаёт исполнение второму приказу после завершения первого, ПРИ
  ЭТОМ member формации продолжает держать станцию на ведущем в течение
  всего манёвра (тот же урок с прошлых проходов: именно стыковые тесты
  ловят реальные баги); сквозной тест на составном `withdraw_orders`
  (разворот + разгон до скорости отхода).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 25 файлов
  `project/simulation/tests/` (было 24, +1 новый файл не создавался —
  тесты добавлены в существующий `test_formation.gd`) зелёные, 0
  Failed каждый (Godot v4.3-stable_linux.x86_64, скачан заново — кэш
  от прошлых проходов в `$HOME` не сохраняется между запусками VM).
  Регрессий не найдено.

INTERPRETATION, не канон: "change course" здесь перенацеливает ВЕКТОР
СКОРОСТИ (траекторию полёта) ведущего, а не обязательно его носовую
ориентацию — согласовано с уже существующей в кодовой базе моделью
всенаправленного (относительно ориентации) impeller-thrust
(`commanded_thrust_local` трансформируется ориентацией, но её
направление не привязано к тому, куда "смотрит" корабль — см.
ShipPhysicsState.integrate; только поднятые нос/корма sidewalls
блокируют thrust, не ориентация). Управление носовой ориентацией
(angular_velocity) от AI/приказа по-прежнему НЕ реализовано нигде в
кодовой базе — честный отдельный пробел, не привязанный к этому. См.
ASSUMPTIONS.md для полного разбора допущений (exact-stop clamp,
tolerance-константы, композитность withdraw).

Честно НЕ сделано (открыто дальше): §29 items, требующие контекста
цели/оружия (attack, select target, target distribution, missile use,
counter-missile posture, defensive posture, evasive maneuver) — вне
рамок этого прохода, не реализованы как формационные приказы; "change
formation" (смена геометрии design_offsets на лету) не реализована —
приказы этого прохода двигают ведущего, но не меняют саму форму строя;
"approach" (движение к точке) не реализован как отдельный примитив (его
можно собрать вручную из change_course к вычисленному направлению +
change_speed, но не готового хелпера-конструктора под него нет);
многоуровневая иерархия команд §28 по-прежнему плоская ("guide +
members", не полное дерево Fleet/Task Force/Squadron/Division/
Element/Ship); управление носовой ориентацией (angular_velocity) от
AI/приказов по-прежнему нигде не реализовано; ECM по-прежнему не
деградирует точность/захват PD; §25 consumer'ов для COMMUNICATIONS/
POWER/STRUCTURAL_INTEGRITY/DEFENSIVE_SYSTEMS по-прежнему нет;
Milestone 11+ не начаты.

## 2026-09-18 (проход: §29 "change formation" — реальная смена геометрии строя на лету)

Закрыт честно залогированный пробел, упоминавшийся в каждой из предыдущих
нескольких записей: "change formation" (смена самой геометрии
`design_offsets` на лету, а не просто движение ведущего) отсутствовала —
приказы прошлого прохода (CHANGE_COURSE/CHANGE_SPEED/HOLD_FORMATION)
двигали только ведущего, но не могли переформировать саму форму строя.

* `FormationOrder`: новый Kind `CHANGE_FORMATION` + поле
  `new_offsets_local: Dictionary` (ship_id → Vector3 offset в локальном
  кадре ведущего). Новый статический конструктор `change_formation(offsets_local)`.
  Приказ переназначает офсеты станций ТОЛЬКО для кораблей, явно
  перечисленных как ключи словаря — не упомянутый существующий член
  строя остаётся на своей текущей станции без изменений (реальный приказ
  "раскрыть интервалы на левом фланге" не должен молча снимать со
  станции правый фланг). Ключ, которого раньше не было среди членов
  строя, добавляет НОВОГО члена на эту станцию — §29 не различает
  "перестроение" и "постановку новичка на станцию", оба случая — просто
  "офсет этого корабля теперь X".
* `SimulationWorld._resolve_formation_orders`: `CHANGE_FORMATION`
  обрабатывается как отдельный случай (аналогично HOLD_FORMATION) ДО
  общей проверки `is_complete`/`commanded_thrust_local` — приказ НЕ
  трогает тягу ведущего вообще (курс/скорость ведущего продолжаются как
  были), применяется через новый `_apply_change_formation` и СРАЗУ ЖЕ
  считается выполненным (снимается с очереди в тот же тик).
* `SimulationWorld._apply_change_formation` (новый метод): переносит
  `new_offsets_local` в `formation.member_offsets`, затем СНИМАЕТ СНИМОК
  результирующей полной геометрии (все текущие члены + ведущий=ZERO) как
  новый `formation.design_offsets`. Именно этот второй шаг — суть
  фичи: сам манёвр "долёта" до новой станции НЕ реализован отдельно —
  он полностью переиспользует уже существующий, уже протестированный
  PD-контроллер station-keeping (`_resolve_formation_keeping`,
  НЕ изменён в этом проходе), который в следующий тик увидит новый
  целевой офсет и сам полетит к нему — ровно так же, как он уже летит к
  design-станциям после передачи командования (§33). Никакой новой,
  отдельной "механики перестроения" не изобреталось — простота и
  консистентность (§43) вместо второй параллельной системы конвергенции.
* Перезапись `design_offsets` результатом (а не сохранение старой формы)
  — осознанное решение: намеренное перестроение — это НОВЫЙ план формации,
  так что ПОСЛЕДУЮЩАЯ передача командования (§33) должна перепланировать
  выживших вокруг формы, заданной ЭТИМ приказом, а не тихо откатываться
  к форме, существовавшей до него.
* 5 новых тестов в `test_formation.gd` (было 68 проверок/29 функций,
  стало 73/34): мгновенное переназначение офсета + самозавершение приказа
  в тот же тик; не упомянутые в приказе члены остаются нетронутыми;
  приказ может добавить СОВЕРШЕННО НОВОГО члена строя; сквозной тест
  через `SimulationWorld.tick_simulation()`, доказывающий, что член
  РЕАЛЬНО долетает до новой станции (калибровано эмпирически — при
  сильно передемпфированном PD station-keeping, K_P_STATION=0.02, полный
  долёт с ~940м занимает порядка нескольких МИНУТ игрового времени, не
  секунд — тест использует 18000 тиков/300с игрового времени, что с
  запасом покрывает калибровочное значение); и стыковой (module-boundary)
  тест на ТРЁХ кораблях, доказывающий, что перестроение реально
  становится НОВОЙ design-базой для §33: перестраивается только один
  фланг, затем ведущий уничтожается и командование переходит к ДРУГОМУ
  (не перестроенному) кораблю по явному succession_order — переpланированный
  офсет перестроенного корабля вычисляется по формуле "design ПОСЛЕ
  перестроения минус design нового ведущего" и явно отличается от того,
  что дала бы устаревшая (до-перестроечная) геометрия, что доказывает,
  что design реально обновился, а не просто "выглядит обновлённым".
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 24 файла
  `project/simulation/tests/` зелёные (Godot v4.3-stable_linux.x86_64,
  скачан заново — кэш от прошлых проходов в `$HOME` не сохраняется между
  запусками VM). Регрессий не найдено.

ASSUMPTION, не канон: ни один источник Honorverse не описывает механику
приказа "смена строя" — решение переиспользовать существующий
station-keeping PD-закон конвергенции (вместо отдельного механизма
"манёвра перестроения" со своей траекторией/таймингом) — инженерный
выбор simplicity/consistency (§43), не числовое допущение. См.
ASSUMPTIONS.md для полного разбора.

Честно НЕ сделано (открыто дальше): §29 items, требующие контекста
цели/оружия (attack, select target, target distribution, missile use,
counter-missile posture, defensive posture, evasive maneuver) — по-прежнему
не реализованы как формационные приказы; "approach" (движение к точке)
по-прежнему не отдельный примитив-конструктор (собирается вручную из
change_course+change_speed); многоуровневая иерархия команд §28
(Fleet/Task Force/Squadron/Division/Element/Ship) по-прежнему плоская;
управление носовой ориентацией (angular_velocity) от AI/приказов
по-прежнему нигде не реализовано; ECM по-прежнему не деградирует
точность/захват PD; §25 consumer'ов для COMMUNICATIONS/POWER/
STRUCTURAL_INTEGRITY/DEFENSIVE_SYSTEMS по-прежнему нет; Milestone 11+
не начаты.

## 2026-09-19 (проход: Milestone 11 начат — §30/§31 Individual Ship Orders / Individual Override)

Milestone 10 (Formation Command) остаётся в основном закрытым (см.
предыдущую запись — "change formation" реализован); этот проход
переходит к Milestone 11 ("Individual ship command + overrides",
AGENTS.md §56), начиная с §30 Individual Ship Orders и §31 Individual
Override — до сих пор НЕ реализованных вообще: до этого прохода у
формации был единственный источник команд (FormationOrder на ведущего,
Milestone 10), а у отдельного корабля не было НИКАКОГО способа получить
явную индивидуальную интенцию, независимую от формации, и не было
никакого представления "этот корабль сейчас переопределяет свою
формацию" (§31 явно требует: "The override must be represented in
simulation state").

* Новый `IndividualOrder` (`individual_order.gd`): Kind
  HOLD/CHANGE_COURSE/CHANGE_SPEED/CHANGE_ORIENTATION. Первые три —
  прямые аналоги уже существующих `FormationOrder` Kind (та же
  exact-stop acceleration clamp логика, см. предыдущие записи), но
  применяются к ЛЮБОМУ отдельному кораблю, а не только к ведущему
  формации. `CHANGE_ORIENTATION` — НОВЫЙ вид, закрывающий давно
  честно залогированный пробел ("управление носовой ориентацией
  (angular_velocity) от AI/приказов нигде не реализовано", упоминался
  в нескольких предыдущих записях подряд): поворачивает нос корабля
  (`orientation`) к заданному мировому направлению, независимо от
  вектора скорости, той же exact-stop clamp техникой (угловая скорость
  разгоняется до `max_angular_speed_rad_s` ровно настолько, чтобы не
  проскочить остаточный угол за один тик) — сходится без колебаний,
  без подбора коэффициентов, симметрично уже принятому подходу для
  CHANGE_COURSE/CHANGE_SPEED. `withdraw_orders()` — составной
  retreat/disengage (change_course + change_speed), зеркально
  `FormationOrder.withdraw_orders`.
* Новый `IndividualCommandState` (`individual_command_state.gd`):
  `current_order`/`order_queue` + `issue_order(s)`/`issue_order_now`/
  `clear_orders`/`is_active()` — зеркало `FormationState`'s
  соответствующих полей/методов, но на уровне ОДНОГО корабля, а не
  формации. `is_active()` — явное представление §31's "override must be
  represented in simulation state".
* `SimulationWorld`: новый `individual_orders: Dictionary` (ship_id ->
  IndividualCommandState) + публичный API
  `issue_individual_order(s)`/`issue_individual_order_now`/
  `return_ship_to_formation`/`is_ship_overriding_formation` + новая
  `_resolve_individual_orders(dt)`, запускается в `tick_simulation`
  МЕЖДУ `_resolve_formation_keeping` и `_resolve_damage_response` —
  т.е. индивидуальный приказ ПЕРЕЗАПИСЫВАЕТ thrust/angular_velocity,
  которые station-keeping уже посчитал этому кораблю этим же тиком
  (это и есть ВЕСЬ механизм §31 "override" — никакого отдельного флага
  "режим переопределения" на формации или корабле не потребовалось,
  активный `IndividualCommandState` сам по себе достаточное
  представление), а критическое повреждение (`_resolve_damage_response`,
  идёт ПОСЛЕ) по-прежнему может перебить даже явный индивидуальный
  приказ — экипаж тонущего корабля уклоняется рефлекторно вне
  зависимости от постоянного приказа. §30 "return to formation" — не
  манёвр, а просто `clear_orders()`: как только `is_active()` становится
  false, `_resolve_formation_keeping` (выполняется КАЖДЫЙ тик независимо
  от индивидуальных приказов) автоматически снова начинает управлять
  этим кораблём со следующего тика — ничего специально "возвращать" не
  нужно. Работает одинаково и для кораблей БЕЗ формации вовсе (реальная
  цель Milestone 11 — независимое индивидуальное командование, не
  только оверрайд формации) — функция вообще не знает о понятии
  "член формации", только "у этого ship_id есть активный
  индивидуальный приказ".
* 16 новых тестов в новом `test_individual_orders.gd`: базовые
  свойства `IndividualCommandState` (неактивен по умолчанию,
  issue_order активирует, clear_orders деактивирует, issue_order_now
  прерывает очередь); ship без индивидуального приказа не считается
  "overriding"; HOLD обнуляет thrust И angular_velocity каждый тик и
  никогда не завершается сам; CHANGE_COURSE/CHANGE_SPEED на одиночном
  корабле БЕЗ формации сходятся и завершаются; очередь withdraw_orders
  реально проходит оба приказа подряд; CHANGE_ORIENTATION из identity
  ориентации; CHANGE_ORIENTATION из НЕ-identity стартовой ориентации
  (см. ASSUMPTIONS.md — дискриминирующий тест против путаницы body-
  frame/world-frame, самый важный тест этого прохода); разворот на 180
  градусов (fallback на произвольную ось при точно антипараллельных
  векторах) — потребовал увеличенного тайм-бюджета теста (см.
  ASSUMPTIONS.md, честно найденная и исправленная ошибка НЕ в коде, а
  в первой версии самого теста); сквозной тест через
  `SimulationWorld.tick_simulation()` — член формации на станции
  (нулевой thrust station-keeping), затем явный индивидуальный приказ
  переопределяет его thrust, ПОКА формационный `current_order` (HOLD_
  FORMATION) остаётся нетронутым, приказ доходит до завершения, корабль
  физически смещается со станции, и station-keeping реально снова
  берёт управление на следующий тик; `return_ship_to_formation` может
  отменить ЕЩЁ НЕ завершённый приказ, не только уже законченный;
  удаление корабля с активным приказом не приводит к краху и чистит
  `individual_orders`.
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 26 файлов
  `project/simulation/tests/` (было 25, +1 новый файл) зелёные (Godot
  v4.3-stable_linux.x86_64, скачан заново). Регрессий не найдено.

INTERPRETATION, не канон: "orientation" здесь — направление НОСА
корабля (`orientation * Vector3.FORWARD`), независимо от вектора
скорости, что согласуется с уже принятой в проекте моделью
всенаправленного impeller-thrust. См. ASSUMPTIONS.md за полный разбор
допущений этого прохода (body-frame reasoning для angular_velocity,
исходную ошибку в тесте на 180°, зачем IndividualOrder НЕ переиспользует
FormationOrder несмотря на дублирование CHANGE_COURSE/CHANGE_SPEED).

Честно НЕ сделано (открыто дальше): §30 items, требующие контекста
цели/оружия (target, target priority, weapon mode, missile launch,
counter-missile policy, point-defense policy, sensor mode, ECM mode,
defensive posture) — вне рамок этого прохода; §29 items с тем же
требованием контекста (attack, select target, target distribution,
missile use, counter-missile posture, defensive posture, evasive
maneuver) — по-прежнему не реализованы как формационные приказы;
"approach" (движение к точке) по-прежнему не отдельный примитив-
конструктор ни на уровне формации, ни на уровне отдельного корабля;
многоуровневая иерархия команд §28 (Fleet/Task Force/Squadron/
Division/Element/Ship) по-прежнему плоская; ECM по-прежнему не
деградирует точность/захват PD; §25 consumer'ов для COMMUNICATIONS/
POWER/STRUCTURAL_INTEGRITY/DEFENSIVE_SYSTEMS по-прежнему нет; §31's
"account for communication limitations" (аналог §33's
COMMAND_TRANSFER_DELAY_S) для индивидуальных приказов не рассмотрен —
индивидуальный приказ применяется мгновенно в тот же тик, когда issued,
без задержки на "передачу приказа кораблю"; Milestone 12+ не начаты.

## 2026-09-19 (проход: Milestone 11 продвинут — §30 "target"/"target priority"/"weapon mode")

* Новый класс `ShipCombatDirective` (`project/simulation/ship_combat_directive.gd`):
  постоянная (не kinematic-очередь, без понятия "завершения") боевая
  директива на корабль — `manual_target_ship_id` (§30 "target"/"target
  priority") и `weapons_free` (§30 "weapon mode": true = свободен открывать
  огонь как раньше, false = held fire). Намеренно ОТДЕЛЬНЫЙ класс от
  `IndividualCommandState` (kinematic-приказы §30 course/speed/orientation
  из прошлого прохода) — держать позицию/цель это не манёвр с завершением,
  а постоянная позиция, так что впихивать её в order/queue модель было бы
  нечестно.
* `TacticalAI.select_directed_weapon_target(ship, contacts, hostile_ship_ids,
  target_ship_id)`: та же "no cheat vision" проверка usable-контакта, что и
  у `select_weapon_target`, но выбирает НАЗНАЧЕННУЮ цель, а не ближайшую.
  Возвращает all-null форму, если цель не в списке hostile, не usable
  контакт, либо пустая строка — никакого решения о fallback сама функция
  не принимает.
* `SimulationWorld`: `ship_combat_directives: Dictionary` (ship_id ->
  ShipCombatDirective, лениво создаётся, чистится в `remove_ship`),
  публичное API `set_ship_target`/`clear_ship_target`/`set_ship_weapons_free`,
  и общий `_resolve_weapon_target(ship_id, ship, contacts, hostile_ids)`,
  используемый ОБОИМИ `_resolve_weapons_ai` и `_resolve_missile_launch_ai`:
  если есть валидная (ещё usable) назначенная цель — стреляем по ней;
  иначе честный fallback на прежний автоматический nearest-hostile выбор
  (не "перестать стрелять", если назначенная цель уничтожена/потеряна с
  датчиков — ни одна доктрина не ожидает "продолжать стрелять по старому
  пеленгу цели, которую больше не видно"). `weapons_free == false`
  пропускает и энергооружие, и пуск ракет для этого корабля ЦЕЛИКОМ, но
  ракетные трубы по-прежнему считают cooldown каждый тик (`tube.advance`
  вызывается БЕЗУСЛОВНО до проверки директивы) — held fire это решение
  fire control, не повреждение оборудования.
* НЕ затронута точечная защита (PD) — её выбор цели (`select_pd_target`)
  основан на угрозе (какая ракета реально целится в ЭТОТ корабль), а не на
  выборе вражеского КОРАБЛЯ, принципиально другая задача — см.
  ASSUMPTIONS.md.
* Новый файл `test_ship_combat_directive.gd` (11 сквозных тестов через
  `SimulationWorld.tick_simulation()`, 22 assertion): дефолты директивы
  (no target, weapons_free=true, бихевиорально идентично прошлому
  поведению); без designation — бьёт по ближайшему (regression-guard);
  designation бьёт по НЕ ближайшей, специально назначенной цели;
  `clear_ship_target` возвращает автоматику; designation на уничтоженную
  ДО тика цель честно даёт fallback на оставшегося hostile, а не "не
  стрелять вообще"; `weapons_free=false` подавляет и энергооружие
  (cooldown у mount остаётся нетронутым — выстрела не было вовсе), и пуск
  ракет (ammo не тратится, но `time_since_last_launch_s` у трубы всё
  равно продвигается); designation одинаково применяется к пуску ракет
  (не только к энергооружию); прямые проверки мутации state через
  `set_ship_target`/`clear_ship_target`/`set_ship_weapons_free`;
  `remove_ship` чистит `ship_combat_directives`. Плюс 4 юнит-теста
  `select_directed_weapon_target` в `test_tactical_ai.gd` (валидная
  designation среди нескольких hostile; designation вне hostile-списка;
  designation с UNKNOWN-контактом; пустая строка designation).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 27 файлов
  `project/simulation/tests/` (было 26, +1 новый файл) зелёные
  (Godot v4.3-stable_linux.x86_64, скачан заново). Регрессий не найдено.

Честно НЕ сделано (открыто дальше, см. ASSUMPTIONS.md): §30/§29 items,
которые всё ещё нужны — "missile launch" как отдельное разовое действие
(не постоянная посадка weapons_free), "counter-missile policy"/
"point-defense policy" (PD принципиально другая задача выбора цели, см.
выше), "sensor mode", "ECM mode", "defensive posture", "target priority"
за пределами простого single-target designation (ранжирование
нескольких целей); "approach" как отдельный примитив; многоуровневая
иерархия команд §28 (Fleet/Task Force/Squadron/Division/Element/Ship,
по-прежнему плоская); ECM не деградирует точность/захват PD; §25
consumer'ов для COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
DEFENSIVE_SYSTEMS по-прежнему нет; коммуникационная задержка (§31 "account
for communication limitations") для индивидуальных приказов/директив
по-прежнему не рассмотрена — designation/weapons_free тоже применяются
мгновенно, тем же тиком; Milestone 12+ не начаты.

## Milestone 11: §30 "missile launch" as a discrete one-time order (`order_missile_launch`)

Closes the honestly-logged gap from the previous pass ("'missile launch'
as a distinct one-shot action separate from this standing posture"):
until now, the ONLY way anything fired missiles was the fully-automatic
`_resolve_missile_launch_ai` running every tick against each ship's
standing target (manual designation or automatic nearest-hostile) and
standing `weapons_free` posture. There was no way to say "fire a salvo at
THIS target right now" as a single, explicit, one-time action.

* New `SimulationWorld.order_missile_launch(ship_id: String,
  target_ship_id: String = "") -> int`: fires from every ready, in-range
  missile tube belonging to `ship_id` at the resolved target immediately,
  and returns the number of missiles actually launched. Does NOT touch
  the ship's standing `ShipCombatDirective` at all (leaves
  `manual_target_ship_id`/`weapons_free` completely unread-from-state and
  unwritten) — the automatic AI keeps using its own standing posture,
  unaffected, starting the very next tick. Empty `target_ship_id` falls
  back to the same standing-target resolution the automatic AI already
  uses (`_resolve_weapon_target`); a non-empty `target_ship_id` that
  isn't a currently usable hostile contact fires nothing and does NOT
  fall back to automatic selection — a one-time order names its target or
  fails, it doesn't get silently redirected (see ARCHITECTURE.md for the
  full fallback-policy comparison against the standing-designation case).
* Respects every existing physical/fire-control gate exactly like the
  automatic AI does: `weapons_free == false` suppresses it too (kept ONE
  consistent meaning for that flag everywhere), a critically damaged
  (disengaging) ship never launches, and tube readiness (ammo, cooldown,
  `condition`-scaled effective range) is never waived by an order —
  matches `fire_weapon()`'s existing convention for energy weapons.
* Callable directly (same "explicit shot trigger" convention `fire_weapon`
  already established), not wired into `tick_simulation`'s per-tick
  resolution loop at all — this is deliberate: a discrete order has no
  "converging" state to check across ticks, unlike the kinematic
  `IndividualOrder` queue (see ARCHITECTURE.md for why this did NOT
  become a new `IndividualOrder.Kind` or a new state class).
* New file `test_missile_launch_order.gd` (8 tests, 18 assertions):
  no-target falls back to standing/automatic selection; explicit target
  overrides the standing designation for one salvo WITHOUT mutating that
  standing designation (verified directly against
  `ship_combat_directives`); an invalid explicit target fires nothing and
  does not fall back; hold-fire suppresses the explicit order too;
  critically damaged ships don't launch; out-of-range targets don't
  launch; only ready tubes fire and the returned count matches exactly
  (a spent tube alongside a ready one); a nonexistent ship or a ship with
  no tubes returns 0 without error.
* Full headless run CONFIRMED: all 28 files in `project/simulation/tests/`
  (was 27, +1 new file) green (Godot v4.3-stable_linux.x86_64, freshly
  downloaded). No regressions.

Честно НЕ сделано (см. ASSUMPTIONS.md за полный список): "counter-missile
policy"/"point-defense policy"; "sensor mode"/"ECM mode"/"defensive
posture"; "target priority" за пределами single-target designation
(ранжирование/приоритизация нескольких целей); "approach" как отдельный
примитив; многоуровневая иерархия команд §28 (Fleet/Task
Force/Squadron/Division/Element/Ship, по-прежнему плоская); ECM не
деградирует точность/захват PD; §25 consumer'ов для
COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/DEFENSIVE_SYSTEMS по-прежнему
нет; коммуникационная задержка для директив/приказов (включая эту новую
missile launch order — применяется мгновенно, тем же вызовом);
`weapons_free` по-прежнему единый флаг на энергооружие и ракеты вместе,
не два раздельных; Milestone 12+ не начаты.

## 2026-09-19 (проход: Milestone 11 продвинут — §25/§31 задержка передачи индивидуальных приказов)

Закрывает СРАЗУ ДВА честно залогированных пробела предыдущих проходов
одним механизмом: §31 "account for communication limitations" для
индивидуальных приказов (было: приказ вступал в силу мгновенно, в тот же
тик) и §25's собственный worked example "communications damage →
degraded command/reporting" (COMMUNICATIONS был единственной подсистемой
из "6 из 11" с потребителем урона, не считая STRUCTURAL_INTEGRITY/POWER/
DEFENSIVE_SYSTEMS, которые по-прежнему честно заблокированы на
несуществующих системах — см. ship_subsystems.gd class doc; не
пересмотрено в этом проходе).

* `SimulationWorld`: новая очередь `_pending_command_transmissions`
  (Array of Dictionary{ready_at, ship_id, callable}) и её резолвер
  `_resolve_pending_command_transmissions()`, вызываемый ПЕРВЫМ делом в
  `tick_simulation` (сразу после `world_sim_time += dt`, до
  `_sync_subsystem_driven_conditions`) — так что приказ, чья передача
  завершается именно в этот тик, уже виден `_resolve_individual_orders`/
  `_resolve_weapon_target` в этом же тике (тот же принцип "без лишнего
  тика задержки сверх смоделированной", что и у
  `_sync_subsystem_driven_conditions`).
* Новые константы `INDIVIDUAL_ORDER_BASE_TRANSMISSION_DELAY_S = 1.0` и
  `_MIN_CONDITION_FOR_COMMS_TIMING = 0.05` (тот же floor-конвенция, что и
  `PointDefenseMount`/`MissileTube`'s `_MIN_CONDITION_FOR_TIMING`).
  Задержка = base / max(condition COMMUNICATIONS корабля-получателя,
  floor) — при здоровых средствах связи (condition=1.0) задержка равна
  базовой (1с, честный ASSUMPTION, НЕ канон — намеренно много меньше
  `COMMAND_TRANSFER_DELAY_S` (3с), т.к. это разные явления: тот
  моделирует распознавание ЭКИПАЖЕМ крупного неопределённого события
  (потеря ведущего), этот — рутинную механическую задержку передачи
  приказа в пределах той же тактической дистанции при исправной связи).
* Новые публичные "transmit_*" обёртки — ДОПОЛНИТЕЛЬНЫЕ, не заменяющие:
  `transmit_individual_order_now`, `transmit_ship_target`,
  `transmit_clear_ship_target`, `transmit_ship_weapons_free`,
  `transmit_return_ship_to_formation`. Каждая просто откладывает вызов
  СУЩЕСТВУЮЩЕГО мгновенного API (`issue_individual_order_now`/
  `set_ship_target`/...) через `Callable.bind`, ничего не переписывая в
  самих мгновенных функциях — ВСЕ 27 ранее существовавших тестовых
  файлов и любой существующий вызывающий код (сценарии, будущий UI,
  что угодно, что хочет мгновенный эффект) продолжают работать
  ПОБИТОВО идентично, без единого изменения поведения.
  `order_missile_launch` (§30 "missile launch") сознательно БЕЗ
  transmit_*-обёртки — она уже задокументирована как мгновенный
  "explicit shot trigger" по конвенции `fire_weapon()`, пересмотр этого
  вне рамок прохода.
* `remove_ship` теперь также вычищает `_pending_command_transmissions`
  для удаляемого id — иначе повторно используемый ship_id мог бы
  "воскресить" чужой приказ, отправленный до удаления корабля.
* Новый файл `test_command_transmission.gd` (9 сквозных тестов через
  `SimulationWorld.tick_simulation()`, 18 assertion): приказ НЕ
  применяется сразу после `transmit_*`; применяется после базовой
  задержки при здоровой связи; корабль без `ShipSubsystems` вообще
  использует базовую (не наихудшую) задержку; ухудшенная связь
  (condition=0.5) удлиняет задержку примерно вдвое (эмпирически, не
  только по формуле); `transmit_ship_target`/`transmit_ship_weapons_free`
  задерживаются идентично; `transmit_clear_ship_target`/
  `transmit_return_ship_to_formation` тоже задерживаются; существующий
  мгновенный API остаётся синхронным без изменений; удаление корабля
  отбрасывает его висящую передачу, не воскрешая её на переиспользованном
  id; и один сквозной тест, что приказ, дошедший через передачу, реально
  зануляет тягу/угловую скорость (не только "появляется в состоянии", а
  действительно переопределяет `_resolve_formation_keeping`, ровно как
  мгновенный API).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 28 файлов
  `project/simulation/tests/` (было 27, +1 новый файл) зелёные
  (Godot v4.3-stable_linux.x86_64, скачан заново). Регрессий не найдено.

Честно НЕ сделано (см. ASSUMPTIONS.md за полный список): STRUCTURAL_
INTEGRITY/POWER/DEFENSIVE_SYSTEMS всё ещё без потребителя урона (см.
ship_subsystems.gd class doc за причину для каждой — не "забыли
подключить", а честно заблокировано на отсутствующих системах: замена
HullState-заглушки, модель энергобюджета, и неопределённость, что вообще
физически представляет DEFENSIVE_SYSTEMS отдельно от wedge/sidewalls);
"counter-missile policy"/"point-defense policy"; "sensor mode"/"ECM
mode"/"defensive posture"; "target priority" за пределами single-target
designation; "approach" как отдельный примитив; многоуровневая иерархия
команд §28; ECM не деградирует точность/захват PD; задержка передачи для
`order_missile_launch` (сознательно оставлена мгновенной, см. выше);
Milestone 12+ не начаты.

## 2026-09-19 (проход: Milestone 12 начат — §45 Replay, первый срез: запись команд/событий + доказанный детерминированный replay)

* Новый файл `simulation/replay_log.gd` (`ReplayLog`): плоская запись
  одного прогона симуляции — `random_seed` (inert placeholder, см. ниже),
  `commands` (каждый вызов публичного API отдачи приказов через
  `SimulationWorld`, с `tick`/`sim_time`/`name`/`args`) и `events`
  (дискретные боевые/статусные события, с `tick`/`sim_time`/`type`/`data`).
  `to_dict()`/`from_dict()`/`save_to_file()`/`load_from_file()` — полный
  JSON round-trip (используется `FileAccess`+`JSON.stringify`/
  `JSON.parse_string`, путь `user://...` в headless-тестах).
* `SimulationWorld.replay_log` (по умолчанию `null` — запись выключена,
  ноль дополнительной работы/поведенческих изменений для всех
  существовавших до этого прохода тестов/сценариев) + `start_recording(seed)`/
  `stop_recording()` + приватные `_record_command`/`_record_event`.
  Новый счётчик `_tick_index` (независимый от `SimClock.tick_count`,
  т.к. большинство тестов гоняют `tick_simulation()` напрямую, без клока)
  используется как временная метка записи.
* `_record_command` подключён во ВСЕ публичные точки отдачи приказов
  корабль/огонь: `issue_individual_order_now`, `return_ship_to_formation`,
  все 5 `transmit_*`, `set_ship_target`, `clear_ship_target`,
  `set_ship_weapons_free`, `order_missile_launch` (записывается даже
  когда приказ не привёл к реальному пуску — "что было приказано", а не
  "что произошло").
* Новые SimulationWorld-обёртки для формационных приказов —
  `issue_formation_order`/`issue_formation_orders`/
  `issue_formation_order_now`/`clear_formation_orders` — ДОПОЛНИТЕЛЬНЫЕ,
  тонкие обёртки над уже существующими `FormationState.issue_order*`/
  `clear_orders` (прямой вызов на `FormationState`, как делают все
  существующие тесты/сценарии, остаётся полностью рабочим и
  непострадавшим) — нужны только затем, чтобы формационные приказы тоже
  проходили через точку, которую можно записать в `ReplayLog`, тем же
  способом, каким `transmit_individual_order_now` уже проводит
  индивидуальные приказы через `SimulationWorld`, а не напрямую через
  `IndividualCommandState`.
* `IndividualOrder`/`FormationOrder` получили `to_dict()`/`static from_dict()`
  (только JSON-совместимые типы — int/float/Array/Dictionary) — нужны,
  чтобы `_record_command` мог сериализовать сам объект приказа, а не
  только простые ship_id/bool/String аргументы.
* `SimulationWorld.apply_recorded_command(entry)` — диспетчер одной
  записанной команды обратно к соответствующему публичному API
  (реконструирует объект приказа через `IndividualOrder.from_dict`/
  `FormationOrder.from_dict`, где нужно). Это и есть "проигрыватель"
  replay — предполагаемое использование: построить новый
  `SimulationWorld` с ТЕМ ЖЕ начальным сценарием (запись НЕ включает
  снимок стартовой расстановки кораблей, см. ниже), затем вызывать это
  один раз на каждую записанную команду, перемежая с `tick_simulation()`
  так, чтобы каждая команда попадала на свой записанный тик.
* **Новая реальная механика, не заглушка**: `_resolve_ship_destruction()`
  — закрывает ранее честно не замеченный пробел. `HullState.is_destroyed()`
  существовал и УЖЕ читался логикой потери ведущего формации
  (`TacticalAI.is_guide_lost`), но ничто в тик-цикле никогда не убирало
  корабль с нулевой целостностью корпуса из `ships` — он продолжал бы
  вечно засекаться сенсорами, выбираться целью и интегрироваться
  физикой. Теперь: в начале каждого тика (тот же "уборка отстаёт на один
  тик" паттерн, что уже использует `_cleanup_inactive_missiles` для
  ракет) корабль с `hull.is_destroyed()` записывается как событие
  `ship_destroyed`, вычищается из member_offsets всех формаций и
  удаляется через уже существующий `remove_ship`. Корабль без
  `HullState` вообще (большинство существующих тестов/сценариев) никогда
  не уничтожается автоматически — поведение идентично тому, что было до
  этого прохода.
* Подключены 3 дополнительных типа событий (список §37 Battle Reports
  далеко не исчерпан — см. "честно НЕ сделано" ниже): `weapon_hit` (в
  `fire_weapon`, только когда `outcome == HIT_UNPROTECTED` и
  `damage_dealt > 0` — иначе лог захлёбывался бы записью каждого
  промаха/попытки выстрела каждый тик), `missile_launched` (в
  `_launch_missile_from_tube`, покрывает и `order_missile_launch`, и
  автоматический AI-пуск), `formation_leader_lost` (в
  `_transfer_formation_command`, теперь принимающей `formation_id`
  параметром именно для этой записи).
* Новый файл `test_replay_log.gd` (13 тестов, 56 assertion): запись
  команд выключена по умолчанию (ноль оверхеда); прямой вызов и
  `transmit_*`/формационные/`order_missile_launch` команды пишутся
  корректно; события `weapon_hit`/`missile_launched`/`ship_destroyed`
  пишутся с верными данными; уничтожение корабля чистит его из
  формации; корабль без `HullState` никогда не уничтожается
  автоматически; `ReplayLog`/`IndividualOrder`/`FormationOrder`
  round-trip через `to_dict`/`from_dict` и через файл (`user://`); и
  ГЛАВНЫЙ тест — `_test_replay_reproduces_the_original_run`: полный
  40-тиковый дуэльный прогон (пуск ракеты на тике 5, hold-fire на тике
  10, снятие hold-fire на тике 20) записывается, лог сохраняется в файл
  и загружается обратно, затем ПОЛНОСТЬЮ независимый второй
  `SimulationWorld` с идентичной стартовой расстановкой проигрывает
  только записанные команды через `apply_recorded_command` — итоговые
  позиция/скорость обоих кораблей и целостность корпуса цели совпадают
  ТОЧНО (`is_equal_approx`/`==`) с оригинальным прогоном. Это и есть
  практическое доказательство ТЗ §43+§45 для этого кодабейса: раз нигде
  в симуляции нет непосеянной случайности (grep подтверждает — ни
  `randf`/`randi`/`RandomNumberGenerator` нет нигде в `simulation/`, кроме
  этого самого комментария), запись команд + детерминированный тик
  ДОСТАТОЧНЫ для полного replay, без необходимости хранить снимки каждого
  тика.
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 29 файлов
  `project/simulation/tests/` (было 28, +1 новый файл) зелёные
  (Godot v4.3-stable_linux.x86_64, скачан заново в этой сессии).
  Регрессий не найдено.

Честно НЕ сделано (см. ASSUMPTIONS.md за полный список): периодические
полные снимки состояния для быстрого seek (§45 сам делает это опциональным
— "if practical"; сегодня seek = replay с нуля через
`apply_recorded_command`, что для одного боя на 40 тиков мгновенно, но не
масштабируется на длинный сценарий с тысячами тиков без снимков);
большинство событий §37 Battle Reports (contact detected, missile launch
detected, incoming missile, subsystem damaged, formation broken за
пределами потери ведущего, command lost, retreat initiated); ЛЮБАЯ
playback UI (play/pause/speed/навигация по событиям/траектории/ракетные
треки — это рендер-слой, который сам ещё только ранний прототип);
`random_seed` на `ReplayLog` ничем не используется (в симуляции просто
нет случайности, которую можно было бы сеять); Milestone 13-17 не начаты.

## Unreleased — ТЗ дополнено: AGENTS.md §63 "Ship Destruction, Wrecks, and Progressive Combat Damage"

* Новая секция §63 по запросу пользователя: уничтоженные корабли НЕ
  должны исчезать — должны становиться видимыми, инертными обломками,
  подчиняющимися инерции (§12), персистентными минимум на время боя
  (точная политика хранения — открыта, §63.2); прогрессивное
  повреждение подсистем (§25) должно ощутимо влиять на бой ДО
  уничтожения, вплоть до состояния "mission kill" при живом корпусе.
* CLOUD.md §2.9 — краткий кросс-референс.
* CANON_RULES.md — "Двенадцатая сверка": честно зафиксировано, что §63
  это геймдизайн-требование пользователя/явное разъяснение §25, НЕ
  подтверждённая цитата канона про обломки — прямая цитата книг не
  найдена в этом заходе.
* ASSUMPTIONS.md — честно зафиксирован ТЕКУЩИЙ разрыв реализации:
  `_resolve_ship_destruction()` до сих пор вызывает `remove_ship()`
  (полное удаление), что теперь прямо противоречит §63.1 — не решено в
  этом проходе (документационное обновление ТЗ, не код).
* Документационное обновление, без изменений в коде/тестах в этом
  заходе.

## Unreleased — ТЗ дополнено: AGENTS.md §4.1 "Где канон молчит — логика и здравый смысл"

* Формализован принцип (по просьбе пользователя): канон — приоритет
  номер один везде, где он есть; пробелы заполняются реальной физикой/
  инженерной логикой и (для тактики/доктрины) реальной военно-морской
  историей — не произвольной выдумкой. Результат всегда
  INTERPRETATION/ASSUMPTION, не CANON, документируется как раньше
  (существующая дисциплина §4 не меняется, меняется только КАК
  заполняется пробел).
* Кросс-референс в CLOUD.md §1.1.
* CANON_RULES.md — "Тринадцатая сверка": зафиксировано, что это
  формализация уже применявшейся практики (§61/§62 — существующие
  примеры), не новый канонический факт.
* Документационное обновление, без изменений в коде/тестах.

## Unreleased — ТЗ дополнено: AGENTS.md §25.1 "Никаких полосок здоровья"

* Новая §25.1 (по прямому запросу пользователя): урон показывается
  через реальную боеспособность (какие подсистемы повреждены/
  отключены и что это стоит кораблю — меньше работающего оружия,
  хуже слежение, медленнее повороты, помехи связи) и видимые
  повреждения корпуса/обломки (§63), НЕ через единую абстрактную
  "полоску здоровья". Единый скаляр (`HullState`) остаётся допустим
  только как ВРЕМЕННАЯ dev-заглушка (уже и так требовалось заменить
  до §59) — теперь явно запрещено делать его постоянным/видимым
  игроку представлением. Точечные числовые показатели по конкретной
  подсистеме (например "SENSORS: 40%") разрешены — запрещена именно
  одна недифференцированная полоска "здоровья корабля".
* Кросс-референсы: §38 (Tactical UI — "system status" переформулирован
  явно), §50 (Visual Style — добавлено в Avoid list), CLOUD.md §1.2.
* ASSUMPTIONS.md — зафиксировано, что это усиливает уже существовавшее
  требование заменить `HullState` (§58/§59), не новая техническая
  задача сама по себе; открытый пункт на будущее — Tactical UI должен
  читать per-subsystem состояние, не единый скаляр.
* Документационное обновление, без изменений в коде/тестах.

## 2026-09-19 (проход: §63.1 Ship Destruction — уничтоженные корабли теперь становятся обломками, не исчезают)

Пользователь дополнил ТЗ (AGENTS.md §63) уже ПОСЛЕ предыдущего прохода,
который добавил `_resolve_ship_destruction()` с `remove_ship()` —
ASSUMPTIONS.md честно зафиксировал это как разрыв реализации ("§63.1
явно требует ОБРАТНОГО"). Этот проход закрывает разрыв.

* `ShipPhysicsState.is_wreck: bool` — новый флаг. Никогда не читается
  `integrate()`/физикой напрямую (обломок продолжает интегрироваться
  ТОЧНО как любой корабль без тяги — момент импульса не исчезает, §12) —
  читается только резолверами `SimulationWorld`, каждый из которых
  теперь пропускает обломок целиком.
* `_resolve_ship_destruction()` переписан: вместо `remove_ship(ship_id)`
  — `ship.is_wreck = true`, `ship.commanded_thrust_local = Vector3.ZERO`
  (тяга обнуляется ОДИН раз — экипажа больше нет держать курс, но
  `velocity`/`angular_velocity` не трогаются вообще, инерция продолжается),
  `individual_orders`/`ship_combat_directives`/`ecm_states` для этого
  корабля стираются ("нельзя отдать приказ", §63.1), корабль убирается
  из member_offsets всех формаций (как и раньше). Корабль ОСТАЁТСЯ в
  `ships`/`hulls`/`sensor_contacts`/`teams` — §63.1 прямо требует, чтобы
  он оставался валидным sensor-контактом и персистентным объектом.
  Добавлена защита от повторной обработки (`if ship.is_wreck: continue`
  в начале сбора списка уничтоженных) — иначе обломок с вечно
  `hull.is_destroyed()==true` писал бы `ship_destroyed` событие каждый
  тик заново.
* Обломок исключён из ВСЕХ боевых/командных резолверов явной проверкой
  `ships[ship_id].is_wreck` в начале цикла: `_resolve_weapons_ai`,
  `_resolve_missile_launch_ai`, `_resolve_point_defense`,
  `_resolve_damage_response` (иначе обломок с hull=0 читался бы как
  "критически повреждён" и получал бы выдуманную тягу "отступления" —
  прямое нарушение "продолжает подчиняться только инерции"),
  `_resolve_individual_orders` (двойная защита — на случай, если
  устаревший/replay-приказ всё же придёт после смерти).
* `_update_sensors`: обломок больше не выступает НАБЛЮДАТЕЛЕМ (нет
  экипажа/энергии на сенсоры) — но остаётся целью наблюдения ДРУГИХ
  кораблей (внутренний цикл не тронут).
* `_hostile_ship_ids()`: INTERPRETATION — обломок исключён из списка
  возможных целей для оружия/PD/расчёта угрозы для отступления. §63.1
  говорит "больше не корабль ни для каких боевых целей" — прочитано как
  включающее "не имеет смысла как цель", не только "не может стрелять
  сам" (реальная военно-морская логика по AGENTS.md §4.1, прямой цитаты
  канона про обстрел обломков не найдено).
* `test_replay_log.gd`: переписан `_test_ship_destruction_removes_ship_
  and_records_event` → `_test_ship_destruction_becomes_a_wreck_and_
  records_event` (проверяет: корабль остаётся в `ships`/`hulls`,
  `is_wreck==true`, тяга обнулена, СКОРОСТЬ до уничтожения сохранена
  бит-в-бит, событие пишется ровно один раз даже за 6 тиков подряд).
  Новый `_test_wreck_is_excluded_from_combat_and_command`: обломок с
  оружием и активным индивидуальным приказом не стреляет и не получает
  тягу от приказа; враг не стреляет по обломку. Итого 63 assertion (было
  56), все 29 headless-тестов зелёные (Godot v4.3-stable, эта сессия).
* CHANGELOG/ARCHITECTURE/ASSUMPTIONS обновлены; ASSUMPTIONS.md запись
  "Уничтоженные корабли пока УДАЛЯЮТСЯ..." отмечена как ЗАКРЫТА.

Честно НЕ сделано (см. ASSUMPTIONS.md): политика хранения/чистки
обломков (§63.2, "сколько хранить" явно UNKNOWN); прогрессивный
"mission kill" как отдельная проверяемая AI-логика поверх набора
subsystem-повреждений (§63.3 — зависит от закрытия STRUCTURAL_INTEGRITY/
POWER/DEFENSIVE_SYSTEMS §25-пробелов); визуализация обломков в
рендер-слое (§7/§38 — рендер сам ещё ранний прототип); замена
`HullState` на полноценную per-subsystem модель (§59, отдельно
зафиксировано в hull_state.gd/§58 как ожидаемая до этого прохода).

## Unreleased — ТЗ дополнено: AGENTS.md §43.1 "Одни правила для всех кораблей"

* Новая §43.1 (по запросу пользователя): физика (§6/§11-15) и урон
  (§25/§25.1/§63) считаются одним и тем же кодом для ЛЮБОГО корабля —
  без ветвления по стороне (свои/чужие/нейтральные, AI/игрок).
  Принадлежность к команде (`teams`, §26) — только для выбора целей,
  никогда не вход в формулы урона/ускорения. Прямое расширение уже
  существовавшего §26 "no cheat vision" с сенсоров на физику/урон.
* Кросс-референс в CLOUD.md §1.3.
* CANON_RULES.md — "Четырнадцатая сверка": проверил существующий код
  (WeaponResolution/MissileResolution/ShipPhysicsState/
  SubsystemDamageResolution) — team/ownership нигде не используется как
  вход в физику/урон, `teams` задействован только в `is_hostile()`.
  Текущая реализация уже соответствует новому пункту ТЗ — не найдено
  нарушений, требующих исправления.
* Документационное обновление, без изменений в коде/тестах.

## Unreleased — ТЗ дополнено: crossing the T / doubling / TOT / succession-window initiative (§33.1/§34.1/§34.2/§41.1)

* По идеям пользователя (сверены с кодом, все ссылки подтверждены):
  §41.1 (crossing the T через существующие AttackGeometry.Sector/
  broadside_arc/BOW_STERN_ACUTE_ANGLE), §34.1 (doubling — формационное
  целеуказание вместо независимого nearest-contact у каждого корабля),
  §34.2 (missile time-on-target — координация прибытия залпа против
  PD), §33.1 (автономное поведение подчинённого корабля в окне
  передачи командования, явно отделено от лаг-по-скорости-света).
* CANON_RULES.md "Пятнадцатая сверка" — проверка ссылок на код,
  подтверждена корректность, честно сохранено разделение
  COMMAND_TRANSFER_DELAY_S vs light-speed comm lag.
* ASSUMPTIONS.md — зафиксировано: спецификация есть, реализации нет ни
  по одному из четырёх пунктов; §33.1 оценён как самый дешёвый для
  следующего прохода.
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- ТЗ дополнено: формационное взаимное прикрытие ПРО (§22.1)

* Новая §22.1 (по сцене из книг, описанной пользователем): прорехи в
  собственной ПРО/клине корабля (уже §41.1 bow/stern gap) в строю
  прикрываются соседним кораблём -- часть причины, почему "wall of
  battle" (§61) вообще является доктриной. Разрыв строя (потеря
  корабля, выход из позиции, отставание, рассредоточенный ордер)
  реально снимает это прикрытие и обнажает прореху для поражения.
* Кросс-референс в CLOUD.md §2.11.
* CANON_RULES.md -- "Шестнадцатая сверка": искал точную цитату сцены
  через WebSearch/WebFetch (Honorverse Wiki, TV Tropes, StarDestroyer.Net
  BBS, Baen) -- не нашёл цитируемого источника (несколько источников
  недоступны: 402/robots.txt). Честно помечено как INTERPRETATION, а
  не CANON, собранное из уже принятых §41.1/§33.1/§61 -- по аналогии
  с "Двенадцатая сверка" (обломки кораблей), где источник тоже не был
  найден.
* ASSUMPTIONS.md -- зафиксирован пошаговый план реализации (сектор
  стрельбы ПРО → формационная проверка прикрытия → влияние на
  резолюцию → AI-целеуказание в непокрытые прорехи), реализации нет.
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- §22.1 уточнено: событие подтверждено как CANON (с исправлением источника)

* Проверены присланные пользователем (от Grok) цитаты из "Field of
  Dishonor" -- НЕ подтверждены поиском, вероятная галлюцинация,
  исключены из ТЗ.
* Реальный источник найден: Honorverse Wiki, "First Battle of Hancock"
  -- эпизод из "The Short Victorious War" (HH3), НЕ Field of Dishonor.
  Pavel Young самовольно рассредоточивает эскадру при выведенном из
  строя адмирале Sarnow -> "defense nets were disrupted" -> HMS
  Cassandra без прикрытия, тяжёлые повреждения -> Harrington
  восстанавливает строй.
* AGENTS.md §22.1 / CLOUD.md §2.11: классификация СОБЫТИЯ повышена до
  CANON (источник -- пересказ Wiki, не прямая цитата книги); само
  механическое правило (секторные прорехи + покрытие соседом)
  остаётся INTERPRETATION, как и было.
* CANON_RULES.md -- "Шестнадцатая сверка, уточнение".
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- §22.1 подтверждена CANON напрямую по первоисточнику (Baen)

* Пользователь дал прямую ссылку на бесплатный Prologue Field of
  Dishonor на baen.com -- в отличие от предыдущих зеркал (archive.org/
  Scribd/e-reading.life, все были недоступны), эта страница открылась.
  Все фразы, ранее не подтверждённые поиском ("Formation Reno",
  "fine-meshed, interlocking network", "point defense net" и т.д.),
  найдены в тексте напрямую.
* AGENTS.md §22.1 / CLOUD.md §2.11: классификация -- CANON, теперь
  подтверждено по первоисточнику, не только пересказом Wiki.
  Prologue -- реконструкция события из "The Short Victorious War"
  (HH3), показанная на суде над Pavel Young в Field of Dishonor (HH4).
* CANON_RULES.md -- "Шестнадцатая сверка, финальная итерация".
* Документационное обновление, без изменений в коде/тестах.

## 2026-09-19 (проход: §33.1 Succession-window initiative -- закрыт накопленный долг)

По явному указанию пользователя (обработка честно залогированного долга
в порядке возрастания номера раздела ТЗ) -- §33.1 был оценён предыдущим
проходом как "самый дешёвый" из четырёх пунктов (§33.1/§34.1/§34.2/
§41.1) и реализован первым.

* Обнаружено, что `_resolve_formation_keeping`'s guide-lost ветка уже
  СЛУЧАЙНО давала нужное поведение как побочный эффект раннего
  `continue` (нет нового приказа тяги => держится последний вектор;
  оружейный AI/`ship_combat_directives` никогда не были завязаны на
  статус guide формации => цель не сбрасывается). Не потребовалось
  добавлять новое состояние -- вместо этого поведение сделано явным
  (расширенный doc-комментарий, ссылающийся на §33.1) и закреплено
  тестами, чтобы будущая правка не могла случайно его сломать без
  предупреждения.
* `simulation_world.gd`: doc-комментарий над guide-lost веткой
  `_resolve_formation_keeping` расширен -- явно объясняет, ПОЧЕМУ
  существующая структура кода (`continue`, пропуск станционного
  удержания на время окна передачи командования) реализует все три
  требования §33.1 ("hold current tactical vector" / "keep engaging
  last target" / "keep covering neighboring ship"), а не переписывает
  механизм.
* `test_formation.gd`: два новых теста --
  `_test_succession_window_member_holds_last_commanded_thrust`
  (участник формации держит бит-в-бит тот же `commanded_thrust_local`
  всё окно передачи командования) и
  `_test_succession_window_manual_target_designation_survives_guide_
  loss` (ручная цель §30 переживает и окно, и саму передачу
  командования новому guide). Итого 109 assertion в test_formation.gd
  (было 46), все 29 headless-тестов проекта зелёные (Godot v4.3-stable,
  эта сессия).
* ASSUMPTIONS.md -- запись §33.1/§34.1/§34.2/§41.1 обновлена: §33.1
  отмечен ЗАКРЫТЫМ, добавлена отдельная запись с деталями и честными
  границами (разворот/ориентация формационного участника никогда не
  управлялась `_resolve_formation_keeping`, так что "держать вектор" не
  включает разворот; §22.1 формационное прикрытие ПРО отдельно всё ещё
  не начато).

Честно НЕ сделано (следующие кандидаты по списку пользователя, по
возрастанию): §34.1/§34.2 Doubling/TOT (архитектурное изменение --
переход от per-ship независимого выбора цели к формационному проходу
назначения), §41.1 Crossing the T (нужен intercept-vector solver,
которого сейчас нет вовсе).

## Unreleased -- ТЗ дополнено: конкретные ТТХ кораблей/оружия/ракет (§8.1)

* Новая §8.1 в AGENTS.md (по запросу пользователя): сourced-числа для
  Medusa-class и Agamemnon-class (длина/масса/ускорение/вооружение по
  бортам/ёмкость магазина), Mark 13/23 MDM/31/Viper ракет (ускорение,
  дальность, терминальная скорость, боеголовки), grav lance/энерго-
  торпеды/graser-торпед (дальность, относительная мощность). Источник
  -- Honorverse Wiki, только числовые факты, без копирования прозы.
* Явная оговорка: это примеры конкретных классов/поколений, не
  универсальные константы (та же логика, что у §21.2).
* Кросс-референс в CLOUD.md §1.4.
* CANON_RULES.md -- "Семнадцатая сверка": что найдено и что осталось
  честно UNKNOWN (прямое лазерное/грейзерное вооружение, автопушки,
  compensator rating, часть Марок ракет без полных цифр).
* ASSUMPTIONS.md -- зафиксирован список пробелов и рекомендация для
  следующего прохода реализации (завести минимум 2 класса корабля и
  несколько Марок ракет как реальные data-driven записи в §8/§48).
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- ТЗ дополнено: иерархия классов кораблей + именованные классы РМН/Хейвен (§8.2)

* Новая §8.2 в AGENTS.md: CANON-иерархия типов кораблей по массе
  (LAC/фрегат/эсминец/лёгкий крейсер/тяжёлый крейсер/линейный
  крейсер/дредноут/супердредноут) с диапазонами тоннажа; именованные
  примеры классов RMN (Medusa/Agamemnon, полные ТТХ уже в §8.1),
  Хейвена (Sultan/Warlord BC с массой/длиной/ускорением, Nevada -- тип
  без цифр) и Grayson (8 классов, только типы, без цифр).
* Кросс-референс в CLOUD.md §1.5.
* CANON_RULES.md -- "Восемнадцатая сверка".
* ASSUMPTIONS.md -- зафиксированы пробелы (Nevada и все классы Grayson
  без числовых ТТХ).
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- ТЗ дополнено: вооружение по классам + именованные корабли (§8.3)

* Новая §8.3 в AGENTS.md: полный состав вооружения Sultan-class и
  Warlord-class (Хейвен BC); конкретные именованные корабли (HMS
  Thunderer/HMS Revenge класса Medusa у RMN, PNS Kerebin класса Sultan
  и PNS Yavuz у Хейвена) как пример структуры данных
  "класс + экземпляр-корабль", а не только плоский список классов.
* Явная оговорка: список кораблей намеренно неполный, не выдумывать
  сверх найденного с источником.
* Кросс-референс в CLOUD.md §1.6.
* CANON_RULES.md -- "Девятнадцатая сверка".
* ASSUMPTIONS.md -- зафиксирован пробел (класс PNS Yavuz не
  переподтверждён) и статус реализации (пока ничего не заведено в код).
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- ТЗ доработано: убраны имена кораблей, добавлена методика §8.4

* §8.3 (AGENTS.md) урезана по прямому указанию пользователя -- удалён
  раздел с именованными кораблями (HMS Thunderer/Revenge, PNS
  Kerebin/Yavuz), оставлено только вооружение классов Sultan/Warlord.
  CLOUD.md §1.6 синхронизирован.
* Новая §8.4 -- методика (не источник канона) для честного выведения
  недостающих характеристик класса/оружия: якорь того же типа+эпохи,
  масса по аналогу, ускорение обратно пропорционально массе ВНУТРИ
  одной эпохи (с явным предупреждением про межэпоховую несравнимость,
  показанным на собственных данных §8.1/§8.3), число мортир/труб по
  соотношению якоря с округлением до симметричного чётного числа,
  характеристики оружия — всегда от записи оружия, не от корабля.
  Каждое выведенное число обязано фиксировать якорь и метод.
* Кросс-референс в CLOUD.md §1.7.
* CANON_RULES.md -- "Двадцатая сверка".
* ASSUMPTIONS.md -- зафиксирован статус (метод есть, применение к
  классам Грейсона/Nevada — следующий шаг).
* Документационное обновление, без изменений в коде/тестах.


## 2026-09-19 (проход: §34.2 Missile Time-on-Target -- следующий честно залогированный пробел)

По списку пользователя (после §33.1, следующий по возрастанию номера
среди §34.1/§34.2/§41.1 -- §34.1 уже был закрыт отдельным проходом
ранее в этот же день) -- реализован §34.2, staggered missile launch
для одновременного прибытия ракет формации к общей цели.

* `kinematics_utils.gd` -- новая `estimate_boost_coast_time_to_distance_s`:
  чисто кинематическая ИНЖЕНЕРНАЯ ОЦЕНКА времени долёта до дистанции при
  разгоне+дрейфе (та же идеализация, что уже использует
  `MissileState.estimated_powered_range_m()`), честно игнорирует
  движение цели за время полёта -- полноценный intercept-solver всё ещё
  не существует (общая нерешённая задача с §41.1 Crossing the T).
* `missile_state.gd` -- вынесены `DEFAULT_DRIVE_MAX_ACCELERATION_MPS2`/
  `DEFAULT_DRIVE_BURN_TIME_S` как именованные константы (был плоский
  литерал), чтобы оценка §34.2 ссылалась на ТЕ ЖЕ значения, что реально
  получает каждая AI-пущенная ракета, без риска рассинхронизации двух
  копий одного числа.
* `simulation_world.gd` -- новый проход `_resolve_missile_tot_coordination`
  (между §34.1 и `_resolve_weapons_ai`/`_resolve_missile_launch_ai`) +
  персистентный (НЕ пересобираемый каждый тик, в отличие от
  `_formation_assigned_targets`) словарь `_missile_salvo_fire_at`,
  использующий тот же паттерн "abs `ready_at`/`world_sim_time`", что уже
  применяется для §25/§31 `_pending_command_transmissions`. Внутри
  формации с общей §34.1-целью самый дальний стрелок (наибольшее время
  полёта) стреляет сразу как "задающий темп" выстрел; более близкие
  стрелки получают расписанный `fire_at`, чтобы их более быстрые ракеты
  прибыли одновременно с ракетой дальнего стрелка. `_resolve_missile_launch_ai`
  доработан: перед пуском проверяет расписание, и (важная защита от
  устаревания) сверяет цель расписания с ФАКТИЧЕСКИ резолвнутой целью
  этого тика -- если корабль получил ручное §30 целеуказание на ДРУГОЙ
  корабль после того, как расписание было построено, устаревшее
  ожидание снимается немедленно, а не удерживает выстрел по цели, для
  которой синхронизация больше не имеет смысла.
* AGENTS.md/CLOUD.md -- обновлён статус §34.1/§34.2/§33.1 с OPEN на
  IMPLEMENTED (первые срезы) там, где текст оставался устаревшим с
  прошлых проходов, которые эти пункты закрывали, но не обновили
  cross-reference секции.
* `test_missile_launch_ai.gd` -- три новых теста (итого 31 assertion,
  было ~18): `_test_formation_mates_stagger_missile_launch_for_simultaneous_arrival`
  (сквозной тест через `SimulationWorld.tick_simulation()` -- дальний
  корабль стреляет немедленно, ближний ждёт РОВНО расчётную задержку,
  затем стреляет; проверено явным пересчётом ожидаемой задержки той же
  формулой, что использует продакшн-код), `_test_lone_formation_member_still_fires_immediately`
  (вырождение в старое поведение при отсутствии соратника по той же
  цели) и `_test_manual_target_override_cancels_a_stale_tot_hold`
  (устаревшее расписание корректно снимается ручным §30 override).
  Все 29 (теперь 30, с новым тестовым файлом не создавалось -- расширен
  существующий) headless-тестов проекта прогнаны заново (Godot
  v4.3-stable, эта сессия) -- 0 провалов.
* ASSUMPTIONS.md -- §34.2 отмечен ЗАКРЫТЫМ, зафиксированы честные
  границы: оценка полёта игнорирует манёвр цели; координация только
  ВНУТРИ одной формации по §34.1-назначенной цели (не по ручной §30
  цели, не между разными формациями); порог "уже достаточно
  синхронно" = `dt * 0.5`, инженерный выбор без канонической опоры.
* Честно НЕ сделано: §41.1 Crossing the T (нужен intercept-vector
  solver, общий с честной идеализацией §34.2 выше); межформационная
  координация TOT; учёт per-Марка характеристик ракеты вместо общих
  DEFAULT-констант (базы ракет как data-driven записей всё ещё нет, см.
  §8.1-8.4).

## 2026-09-22 (проход: §22.1 Formation mutual defensive coverage -- первый срез, только wedge)

По списку пользователя -- следующий честно залогированный пробел после
закрытия §41.1 Crossing the T тем же днём (§22.1 явно называлась
"следующий кандидат" в записи ASSUMPTIONS.md по итогам предыдущего
прохода).

* `simulation_world.gd` -- новый `_formation_bow_stern_coverage(ship_id)`
  -> `{"bow": bool, "stern": bool}`: живой (не wreck) сосед по той же
  формации (guide или member), в пределах новых констант
  `FORMATION_COVERAGE_MAX_DISTANCE_M` (5000 м) и
  `FORMATION_COVERAGE_CONE_HALF_WIDTH_RAD` (30°) от носовой/кормовой оси
  корабля, при ДЕЙСТВУЮЩЕМ guide формации (тот же `TacticalAI.
  is_guide_lost` ворот когезии, что уже использует
  `_resolve_formation_keeping`/`_is_station_kept_formation_member`) --
  переиспользует ту же идею "классифицировать направление в своей
  локальной системе", что уже есть в `AttackGeometry`, вместо новой
  формулы угла.
* `ship_defense_state.gd` -- новый `ResolutionKind.FORMATION_COVERED` +
  два новых опциональных bool-параметра `resolve_attack()`
  (`formation_bow_covered`/`formation_stern_covered`, оба по умолчанию
  false -- ни один существующий вызов/тест не затронут). Когда у
  корабля НЕТ действующего sidewall на носу/корме (не поднят ИЛИ
  выгорел) И сосед прикрывает эту ось -- вместо полного UNPROTECTED
  (transmitted_fraction 1.0) теперь FORMATION_COVERED с множителем 0.5
  (ASSUMPTION, честно без канонической опоры, как и остальные плейсхолдеры
  в этом файле). Сознательно НЕ применяется к уже существующему случаю
  обхода ПОДНЯТОГО sidewall под острым углом (§41.1) -- отдельная,
  более узкая область действия этого первого среза.
* `weapon_resolution.gd`/`missile_resolution.gd` -- новый
  `Outcome.FORMATION_COVERED` в обоих; `WeaponResolution.fire()` принимает
  новый опциональный `target_formation_coverage: Dictionary`,
  `MissileResolution.resolve_detonation()` -- два новых опциональных
  bool-параметра, оба прокидываются в `resolve_attack()`. Для ракеты с
  несколькими стержнями (§21.1) `overall_outcome` теперь различает три
  степени тяжести: HIT_UNPROTECTED (хуже всего) > FORMATION_COVERED >
  SIDEWALL_ATTENUATED > WEDGE_BLOCKED.
* `simulation_world.gd` -- два реальных места вызова в живом тик-цикле:
  `fire_weapon()` (единая точка, через которую идут и прямые вызовы, и
  `_resolve_weapons_ai`) считает покрытие цели и прокидывает в
  `WeaponResolution.fire()`; `_update_missiles()` считает то же самое
  для реально детонирующей ракеты и прокидывает в
  `MissileResolution.resolve_detonation()`.
* AGENTS.md §22.1/CLOUD.md §2.11 -- статус обновлён с "ничего не
  реализовано" на "wedge-половина реализована (2026-09-22), PD-половина
  и AI-сторона честно остаются открытыми" (PD по-прежнему полностью
  omnidirectional, никакой firing-arc модели не появилось; AI/
  целеуказание не читает новую функцию вообще).
* Тесты: `test_wedge_sidewall.gd` -- 2 новых юнит-теста на
  `ShipDefenseState.resolve_attack()` напрямую (покрытие ослабляет
  ранее-полностью-незащищённый нос; покрытие НЕ влияет на случай обхода
  поднятого sidewall). `test_formation.gd` -- 3 новых теста: чистая
  геометрия `_formation_bow_stern_coverage()` (сосед на оси близко ->
  покрытие; сосед слишком далеко -> нет; сосед вне конуса (abeam) ->
  нет), покрытие отключается при потерянном guide, и один СКВОЗНОЙ тест
  через реальный `SimulationWorld.tick_simulation()` +
  `SimulationWorld.fire_weapon()` (два отдельных мира -- с прикрывающим
  соседом по формации и без -- один и тот же выстрел по нос-баку:
  FORMATION_COVERED против HIT_UNPROTECTED, урон 50 против 100). Все 30
  headless-тестовых файлов проекта прогнаны заново (Godot v4.3-stable,
  эта сессия) -- 0 провалов, включая существующие 123 (было 109)
  ассерта в test_formation.gd и все существующие ассерты в
  test_wedge_sidewall.gd/test_weapon_resolution.gd/
  test_missile_resolution.gd (сигнатуры расширены только опциональными
  параметрами со значениями по умолчанию -- обратная совместимость
  подтверждена прогоном, а не только чтением кода).

Честно НЕ сделано (следующие кандидаты по списку пользователя, по
возрастанию): PD firing-arc/facing модель с нуля; AI/целеуказание,
предпочитающее непокрытые бреши (§26/§34); формационное прикрытие для
случая обхода поднятого sidewall под острым углом; data-driven база
классов кораблей/оружия (§8/§48); intercept-vector solver (общий для
§34.2/§41.1).

## 2026-09-22 (проход: Milestone 10/29 debt -- "approach" как отдельный примитив формационного приказа)

Закрывает честно залогированный пробел, повторявшийся в списке "Честно
НЕ сделано" нескольких предыдущих проходов подряд: §29 Formation Orders
перечисляет "approach" среди списка формационных приказов, но до этого
прохода приказа "лечь на курс к точке" не существовало как отдельного
примитива -- `FormationOrder`'s doc comment честно называл "approach" и
"withdraw" конвенционными композитами двух кинематических видов
(CHANGE_COURSE + CHANGE_SPEED), но реально была реализована только
withdraw-композиция (`withdraw_orders`); approach-композиции не было
нигде в коде.

* `formation_order.gd` -- новый `Kind.APPROACH` (пятый вид, НЕ композит):
  новые поля `target_point_world: Vector3`, `approach_speed_mps: float`,
  `arrival_tolerance_m: float = 200.0` (ASSUMPTION, честно без
  канонической опоры -- инженерная дистанция "пара корпусов" на
  масштабе этого проекта, тот же принцип, что и у уже существующих
  `heading_tolerance_rad`/`speed_tolerance_mps`). Новый конструктор
  `FormationOrder.approach(target_point_world, speed_mps)`. Ключевое
  отличие от CHANGE_COURSE: APPROACH не замораживает курс в момент
  выдачи приказа -- он хранит ЦЕЛЕВУЮ ТОЧКУ, а не готовый вектор
  скорости. `is_complete()` для APPROACH проверяет дистанцию до точки
  (`guide.position.distance_to(target_point_world) <= arrival_tolerance_m`),
  а не совпадение скорости -- принципиально другое условие завершения,
  т.к. approach -- это навигационная цель ("долететь"), а не
  манёвренная ("выйти на курс/скорость и держать").
* `simulation_world.gd` -- новая `_approach_target_velocity(guide, order)`:
  чистое pure pursuit (прямое наведение на ТЕКУЩУЮ позицию цели-точки,
  без экстраполяции/lead-решения -- ASSUMPTION, не канон, см. doc
  comment функции), вызывается КАЖДЫЙ тик в `_resolve_formation_orders`
  ДО проверки `is_complete()`, пересчитывая `order.target_velocity_mps`
  заново из текущей позиции guide -- в отличие от CHANGE_COURSE/
  CHANGE_SPEED, чьё `target_velocity_mps` фиксируется один раз при
  выдаче приказа. После пересчёта используется ТА ЖЕ существующая
  exact-stop clamp-логика тяги, что и у остальных видов приказов --
  никакой отдельной "навигационной" физики не добавлено, только новый
  способ вычислить цель по скорости на каждый тик.
* `to_dict()`/`from_dict()` расширены тремя новыми полями (обратная
  совместимость: `from_dict()` использует `.get()` с дефолтами для
  старых сериализованных приказов без этих полей).
* Тесты: `test_formation.gd` -- 2 новых сквозных теста через
  `SimulationWorld.tick_simulation()`: (1) guide с нулевой начальной
  скоростью долетает до точки по прямой и приказ самозавершается на
  дистанции в пределах `arrival_tolerance_m` -- фиксация состояния
  ИМЕННО В ТОТ ТИК, когда приказ завершился (не после ещё N тиков --
  первая версия теста поймала реальный баг конструкции теста, не кода:
  guide по инерции пролетает точку насквозь и к концу фиксированного
  цикла тиков уже снова ВНЕ допуска, т.к. завершение приказа не тормозит
  корабль, ровно как у CHANGE_COURSE/CHANGE_SPEED); (2) guide стартует с
  ПОБОЧНОЙ скоростью, направленной вообще не к цели (эмулирует остаток
  от предыдущего приказа) -- и всё равно долетает до точки, что доказывает
  именно ПОСТОЯННЫЙ пересчёт курса по тикам, а не однократный, как у
  change_course. `test_replay_log.gd` -- 1 новый тест round-trip
  `to_dict()`/`from_dict()` для APPROACH-приказа. Полный прогон ВСЕХ 30
  headless-тестовых файлов проекта (Godot v4.3-stable, скачан заново в
  этой сессии) -- 0 провалов, включая 128 (было 126) ассертов в
  test_formation.gd и 67 в test_replay_log.gd.

Честно НЕ сделано (следующие кандидаты по списку пользователя, без
изменений с прошлого прохода): многоуровневая иерархия команд §28
(Fleet/Task Force/Squadron/Division/Element -- сейчас есть только один
уровень guide+members); PD firing-arc/facing модель с нуля; AI/
целеуказание, предпочитающее непокрытые формацией бреши (§26/§34);
формационное прикрытие (§22.1) для случая обхода поднятого sidewall под
острым углом; data-driven база классов кораблей/оружия (§8/§48);
intercept-vector solver, общий для §34.2/§41.1. APPROACH намеренно НЕ
поддерживает наведение на ДВИЖУЩУЮСЯ цель (только фиксированная
world-space точка) -- если понадобится "approach to a moving contact",
это отдельная будущая надстройка (пересчитывать `target_point_world` из
позиции цели каждый тик снаружи, либо новый параметр-callback), не
решённая этим проходом.

Также в этом проходе: удалена папка `_to_delete/` (gitignored, не
референсится нигде в проекте) -- 4 файла-прототипа с прошлого прохода
(`formation_ai.gd`, `formation.gd`, `test_formation_ai.gd`,
`_calib_test_1789772709.gd`), давно вытесненные текущими
`formation_state.gd`/`formation_order.gd`/`test_formation.gd`.

## 2026-09-22 (проход: Milestone 10/14 debt -- §28 Command Hierarchy, конфигурируемое дерево эшелонов, первый срез)

Godot-бинарь закэширован постоянно в `.tools/godot/` на диске
пользователя (`.gitignore` дополнен `.tools/`) -- больше не будет
перекачиваться каждый проход. По списку честно залогированных пробелов
(по возрастанию, из предыдущего прохода) первый кандидат -- "многоуровневая
иерархия команд §28 (Fleet/Task Force/Squadron/Division/Element)".

* Новый файл `command_echelon.gd` -- `CommandEchelon`: узел дерева
  ОТДЕЛЬНОГО от плоских `FormationState`, конфигурируемый (`kind` --
  свободная строка, не хардкод из 5 уровней, ровно как требует §28:
  "The hierarchy must be configurable"). Узел либо ВНУТРЕННИЙ
  (`child_echelon_ids`), либо ЛИСТ (`commanded_formation_id` указывает
  на существующий `formation_id` в `SimulationWorld.formations`) --
  никогда оба сразу.
* `simulation_world.gd` -- `command_echelons: Dictionary` (echelon_id ->
  CommandEchelon) + `add_command_echelon()`, `get_command_echelon()`,
  `attach_formation_to_echelon()`, `_collect_formation_ids_under_echelon()`
  (рекурсивный обход поддерева, cycle-safe, дедуплицирует) и
  `issue_echelon_order()` -- каскадирует один и тот же `FormationOrder`
  на ВСЕ формации под эшелоном, каждой -- СВОЙ независимый клон через
  уже протестированный `to_dict()/from_dict()` round-trip (не общий
  объект-ссылка -- иначе тик одной формации по чужому мутирующему полю
  APPROACH сломал бы другую). Записывается в replay ОДИН раз на уровне
  эшелона (`apply_recorded_command` -- новый case `issue_echelon_order`),
  список формаций-получателей переизвлекается заново при replay, а не
  замораживается снимком.
* Ничего в существующем плоском формационном коде (`_resolve_formation_orders`,
  `_resolve_formation_keeping`, §33 succession, §22.1 coverage, §34.1
  doubling) не изменено -- дерево эшелонов сидит СТРОГО НАД плоскими
  формациями, каскадируя обычные `FormationOrder` в их обычные
  `order_queue`.
* Тесты: новый `test_command_echelon.gd` -- 24 теста (класс `CommandEchelon`
  напрямую: is_leaf/to_dict/from_dict/защитное копирование массива;
  построение дерева: root, parent-child линковка, отказ self-parent,
  отказ на несуществующего родителя, отказ добавить ребёнка к
  родителю-листу, отказ сделать формацию у эшелона с детьми; сбор
  formation_id: один лист, двух-дивизионная эскадра, пустой внутренний
  узел, дедупликация одной формации под двумя листьями, несуществующий
  эшелон; каскад через РЕАЛЬНЫЙ `SimulationWorld.tick_simulation()` --
  CHANGE_COURSE на две независимые формации одновременно, независимость
  клонов APPROACH-приказа (разные target_velocity_mps для гвардов в
  разных стартовых позициях к одной точке), безопасный no-op на
  неизвестный эшелон/formation_id; replay -- запись ровно ОДНОЙ команды
  на каскад из двух формаций, полный round-trip воспроизведения через
  `apply_recorded_command` на свежем мире). Полный прогон ВСЕХ 31
  headless-тестовых файлов проекта (Godot v4.3-stable, бинарь взят из
  постоянного кэша `.tools/godot/`, скачан ОДИН раз этой сессией) -- 0
  провалов, включая существующие `test_formation.gd` (128 ассертов),
  `test_replay_log.gd` (67), `test_simulation_world.gd` (10) --
  подтверждено, что новый код НЕ затронул ни одного уже существующего
  формационного/replay-механизма.

Честно НЕ сделано (следующие кандидаты по списку пользователя, по
возрастанию, после закрытия §28-первого-среза): PD firing-arc/facing
модель с нуля; AI/целеуказание, предпочитающее непокрытые формацией
бреши (§26/§34); формационное прикрытие (§22.1) для случая обхода
поднятого sidewall под острым углом; data-driven база классов кораблей/
оружия (§8/§48); intercept-vector solver, общий для §34.2/§41.1;
approach-приказ на движущуюся цель; эшелон-уровневый leader/succession
(§33 по-прежнему только на уровне формации, не эшелона); дифференцированная
трансляция приказа по подчинённым (например честная "target distribution"
на уровне Fleet -- сейчас только идентичный fan-out одного и того же
приказа всем); reparenting/detach эшелона в рантайме.


## 2026-09-22 (проход: §22.1 PD mount firing-arc/facing model -- первый срез)

Следующий честно залогированный пробел по списку (по возрастанию,
после закрытия §28-первого-среза) -- "PD firing-arc/facing model from
scratch", явно названный в самом тексте §22.1 ("any future PD mount
firing-arc restriction") как ожидаемое продолжение уже реализованного
bow/stern wedge-гэпа (§41.1). До этого прохода `PointDefenseMount` был
полностью всенаправленным: `PointDefenseResolution.engage()` проверял
дистанцию и сенсорный трек, но НИКОГДА -- в каком секторе относительно
собственной ориентации корабля находится цель.

* `point_defense_mount.gd` -- новое поле `arc_sectors: Array`
  (`Array[AttackGeometry.Sector]`) + `can_bear_on(sector)`, ТОЧНО ТА ЖЕ
  модель, что уже используется `WeaponMount` (тот же грубый
  6-секторный `AttackGeometry.Sector`, не непрерывный конус) -- плюс
  статические хелперы `broadside_arc()`/`bow_chaser_arc()`/
  `stern_chaser_arc()`, буквально идентичные одноимённым хелперам
  `WeaponMount`. Единственное СОЗНАТЕЛЬНОЕ отличие от `WeaponMount`:
  пустой `arc_sectors` там означает "не может стрелять никуда" (каждый
  существующий `WeaponMount` уже конструируется с явной дугой), а
  здесь пустой `arc_sectors` означает ВСЕНАПРАВЛЕННЫЙ огонь -- потому
  что класс `PointDefenseMount` существовал ЗАДОЛГО до появления
  понятия дуги, и каждый существующий сценарий/тест конструирует
  `PointDefenseMount.new()` без дуги, ожидая прежнего поведения. Это
  задокументировано прямо в doc comment поля, а не оставлено неявным.
  CANON-обоснование того, что PD вообще позиционен, а не единая башня
  на весь корабль: CLOUD.md §2.2 приводит для одного и того же класса
  РАЗДЕЛЬНЫЕ счётчики "broadside ...PD" и "chase ...PD" -- точно как
  для энергетического оружия.
* `point_defense_resolution.gd` -- новый `Outcome.NO_ARC` + geometry
  gate в `engage()`, добавленный СРАЗУ после проверки дистанции (до
  накопления времени реакции): `AttackGeometry.classify(incoming_missile.position,
  ship.position, ship.orientation)` -- ТА ЖЕ форма вызова, что уже
  использует `WeaponResolution.fire()` для энергетического оружия
  (атакующий = объект, на который целятся; цель = сам корабль-носитель,
  чья ориентация задаёт локальный фрейм). Выход из дуги сбрасывает
  накопленный прогресс (`_reset_tracking()`) -- ТОЧНО ТАК ЖЕ, как уже
  делает выход из дистанции: если корабль развернулся и цель ушла из
  сектора, который может держать данная батарея, накопленные попадания
  не сохраняются (мэтч с уже протестированным
  `_test_missile_leaving_range_resets_progress`).
* Новый файл теста `test_point_defense_arc.gd` -- 26 ассертов:
  регрессия (всенаправленный mount по умолчанию стреляет из ЛЮБОГО из
  6 секторов -- ни один существующий тест/сценарий не сломан);
  bow-chaser блокирует STERN и пропускает BOW; broadside пропускает
  PORT/STARBOARD и блокирует BOW; stern-chaser симметрично; разворот
  корабля на 180° сбрасывает накопленные попадания у bow-chaser mount
  (тот же контракт, что и выход из дистанции); и ОДИН полный
  сквозной тест через РЕАЛЬНЫЙ `SimulationWorld.tick_simulation()` --
  два независимых мира с bow-chaser батареей, в одном ракета реально
  подлетает с BOW (живой sensor-detection §23 + PD engagement в одном
  и том же тике) и гарантированно получает выстрел (`cooldown_remaining_s
  > 0`), в другом та же батарея с ракетой, подлетающей со STERN, ни
  разу даже не начинает трекинг (`_tracking_target == null`) за те же
  тики. Найден и исправлен баг СОБСТВЕННОГО черновика теста при его
  написании: ракета, размещённая ровно в 5000 м от цели, немедленно
  проходила `warhead_armed` порог (`terminal_detonation_range_m * 0.1`
  = 5000 м) и детонировала в ТОМ ЖЕ тике, до того как PD успевал
  выстрелить -- не имеет отношения к дуге как таковой, просто слишком
  близкий старт для смыслового теста; исправлено смещением тестовых
  ракет на 15 000 м (внутри `max_engagement_range_m` = 20 000 м,
  далеко за порогом самоподрыва).
* Прогнаны ТОЛЬКО реально затронутые тестовые файлы (не полный набор,
  по правилу "точечно, без прогонов на всякий случай"):
  `test_point_defense.gd`, `test_point_defense_sensors.gd`,
  `test_pd_missile_continuous_degradation.gd`,
  `test_point_defense_arc.gd` (новый), плюс `test_weapon_resolution.gd`
  как проверка, что паттерн `AttackGeometry.classify`, скопированный
  из него, не задет -- все 0 провалов.

Честно НЕ сделано (не входило в объём этого первого среза): выбор
цели ДЛЯ КАЖДОГО mount'а по его собственной дуге -- `TacticalAI.select_pd_target`
по-прежнему выбирает ОДНУ цель на весь корабль (ближайшую), а не
по одной на дугу/пеленг, так что корабль, атакуемый одновременно с
разных пеленгов, всё ещё не может распределить огонь между батареями,
прикрывающими разные сектора -- это ХОРОШО ИЗВЕСТНАЯ, отдельно
зафиксированная цель следующего среза, не тихо пропущенная; ни один
существующий сценарий/загрузка корабля пока не назначает реальную
позиционную PD-батарею (broadside+chase) по образцу данных CLOUD.md --
все существующие сценарии по-прежнему конструируют всенаправленные PD
mounts (сама механика дуги готова и протестирована, но её ещё никто
не использует в реальной загрузке корабля); ECM по-прежнему не
деградирует точность/трекинг PD (не входило в объём этого прохода).

## Unreleased -- ТЗ доработано: Replay (Milestone 12) деприоритизирован

* По решению пользователя: Milestone 12 (Replay) откладывается.
  Существующий первый срез (`replay_log.gd` -- запись команд/событий,
  без снапшотов/перемотки/UI) остаётся как есть, не расширяется, пока
  не будут пройдены Milestone 13-17 (сценарии, 2v2/эскадра, крупные
  флотские сражения, редактор сценариев, финальный билд).
* AGENTS.md §56 -- добавлена явная приоритетная заметка над списком
  Milestone'ов и пометка прямо в тексте Milestone 12.
* Кросс-референс в CLOUD.md §2.6.
* Документационное обновление, без изменений в коде/тестах.

## Unreleased -- ТЗ доработано: §56.1 - максимально быстрый путь к играбельной версии

* Новая §56.1 в AGENTS.md (по прямому запросу пользователя): временно
  ставит визуальный/игровой слой ВЫШЕ приоритетом, чем Milestone 12-17
  и дальнейшая полировка симуляции. Причина: симуляционное ядро
  (Milestone 1-11) сильное и хорошо протестировано (32 headless-теста),
  но визуального/игрового слоя нет вообще (ни камеры, ни моделей
  кораблей, ни управления, ни HUD).
* Минимальный объём "вертикального среза": камера + placeholder-
  геометрия кораблей (правильная геометрия клина/обшивки, БЕЗ
  канон-точных моделей корпуса -- временно, под допущение §7 "unknown
  visual details may be interpreted", зафиксировать как placeholder в
  ASSUMPTIONS.md); простая визуализация огня оружия (только рендер
  уже посчитанного состояния, §42); минимальный §25.1-совместимый HUD
  (состояние подсистем, цель, сенсорные контакты) прямо из
  существующего состояния; управление напрямую через уже реализованные
  API (`FormationOrder`/`IndividualOrder`/`ShipCombatDirective`); ОДИН
  зашитый стартовый сценарий (не общая система сценариев Milestone 13);
  простой win/lose на существующей логике разрушения (§63); черновой
  Windows-экспорт.
* Явно вне рамок этого среза: художественные модели корпусов, звук,
  полировка меню, общая система сценариев/редактор, Replay (уже
  деприоритизирован), производительность на крупных флотах, дальнейшая
  UI-полировка командной иерархии, любые НОВЫЕ механики симуляции --
  задача только показать уже готовое, не добавлять бэкенд.
* Кросс-референс в CLOUD.md §1.8.
* Документационное обновление, без изменений в коде/тестах.

## Unreleased — §8/§48 Data-Driven Ship Database: первый реальный срез (Milestone 13 подготовка)

* Новый `ShipClassData` (`ship_class_data.gd`, `extends Resource`) —
  первая реализация схемы §8 Ship Database как настоящих полей вместо
  прозы: identity (canonical_name/ship_type/faction/era/source_note),
  габариты/масса, `rated_acceleration_g` (CANON-цифры даны в G,
  конвертация в м/с² — на стороне фабрики), состав вооружения по зонам
  (broadside/fore/aft) отдельно для лазеров/грейзеров/ракетных труб/ПРО-
  мортир, плюс поля-справочники для counter-missile tubes и общей
  ёмкости магазина. `source_note` обязателен (`is_valid()` проверяет) —
  каждая запись обязана фиксировать CANON-цитату или ASSUMPTION-метод
  (§8.4 шаг 6), а не быть числами без провенанса.
* Новый `ShipFactory` (`ship_factory.gd`) — строит полностью
  вооружённый корабль в `SimulationWorld` из `ShipClassData`:
  `ShipPhysicsState` (масса, `max_thrust_n = rated_accel_mps2 * mass_kg`
  — так что `effective_max_acceleration()` воспроизводит CANON G-рейтинг
  ТОЧНО через F/m, §62.1, а не отдельный хардкод-кап), `HullState`
  (оставлен как есть — плоская ASSUMPTION-заглушка 10000, НЕ
  масштабируется по массе класса в этом проходе, т.к. нет канонической
  цифры-якоря для такой формулы, см. ASSUMPTIONS.md), опционально
  `ShipSubsystems` (по умолчанию включено). Энергетические/ПРО-мортиры
  создаются С РЕАЛЬНЫМИ дугами (`WeaponMount.broadside_arc()`/
  `bow_chaser_arc()`/`stern_chaser_arc()`, `PointDefenseMount`
  аналогично) — это ПЕРВАЯ загрузка корабля во всём проекте, которая
  реально назначает позиционные ПРО-батареи (broadside+chase) вместо
  всенаправленного дефолта; ровно тот пробел, что был честно записан
  при добавлении самого механизма дуг (§22.1 PD firing-arc). Ракетные
  трубы — общий магазин класса делится поровну по всем создаваемым
  трубам (ASSUMPTION-распределение, целочисленное деление, остаток не
  довыдумывается); зонная (broadside/fore/aft) разбивка труб сохранена
  как CANON-данные, но `MissileTube` по-прежнему не моделирует дугу
  (честный пробел, см. ниже) — так что зона пока не меняет поведение
  трубы, только её число.
* Новые файлы данных `project/data/ships/medusa_class.tres` (RMN,
  superdreadnought, CANON: длина 1383 м, масса 8,554,750 т, ускорение
  402.3 G, полный состав вооружения по бортам/носу/корме — AGENTS.md
  §8.1) и `sultan_class.tres` (Haven, battlecruiser, CANON: масса
  859,250 т, длина 707 м, ускорение 489.2 G, полное вооружение —
  §8.2/§8.3). Каждый `.tres` несёт `source_note` с точной цитатой,
  какие числа CANON, а какие — ASSUMPTION-распределение по зонам (когда
  источник даёт только суммарную цифру без разбивки broadside/chase,
  как лазеры/грейзеры/ПРО у Sultan-class).
* Новый файл теста `test_ship_class_data.gd` (25 assertion, все
  зелёные): оба `.tres` грузятся и валидны; `ShipFactory.spawn_ship`
  даёт корабль с массой/ускорением/числом маунтов/труб/ПРО-батарей,
  ТОЧНО равным сумме зонных полей данных; отдельная регрессионная
  проверка, что заспавненные ПРО- и энергетические маунты реально несут
  `broadside_arc()`/чейз-дугу, а не всенаправленный дефолт (закрывает
  ранее честно записанный пробел); распределение магазина по трубам не
  превышает и не сильно недобирает CANON-цифру; ОДИН полный сквозной
  тест через `SimulationWorld.tick_simulation()` — два полностью
  дата-driven корабля (Medusa + Sultan) тикаются 60 раз, позиция
  остаётся конечной (не NaN), корабль под ручной тягой реально движется
  в заданном направлении. Полный набор всех 32 тестовых файлов прогнан
  ОДИН раз перед этим коммитом — 0 провалов, регрессий не найдено.

Честно НЕ сделано (см. ASSUMPTIONS.md за полный список): только 2
класса заведены как данные (Medusa/Sultan) — остальные именованные
классы §8.2 (Agamemnon, Warlord, весь список Grayson) остаются
справочной прозой, не `.tres`-записями; `HullState.max_integrity` не
масштабируется по классу; `MissileTube` по-прежнему не моделирует
зону/дугу — зонные поля данных пока чисто информационные для труб;
`counter_missile_tubes`/`counter_missile_magazine_total` записаны как
CANON-справка, но НЕ порождают отдельный launcher (в проекте вообще нет
отдельного класса "труба контрракет" — контрракета это просто
`MissileState` с целью-ракетой, см. `counter_missile_resolution.gd`);
Milestone 13 (Scenarios, §46) как таковой ещё не начат — это
ФУНДАМЕНТ под него (данные + фабрика), а не сценарный загрузчик/формат
сценария/UI редактора (§47) — следующий естественный срез.

## Unreleased -- §56.1 vertical slice: sidewall visualization + OrbitCamera (первые реальные шаги по playable-приоритету)

* Новый `SidewallMeshBuilder` (`scripts/sidewall_mesh_builder.gd`) --
  плоские панельные меши для port/starboard (broadside) и bow/stern
  sidewall-стен (ТЗ §16), геометрически ОТДЕЛЬНЫЕ от wedge (тот --
  V-образный тент, эти -- плоские панели, т.к. канон описывает sidewall
  как боковую "стену", а не приподнятый гребень). Только геометрия --
  видимость решает `ShipView`, не этот файл.
* `ShipView` (`scripts/ship_view.gd`) теперь строит и показывает 4
  sidewall-панели на корабль в дополнение к уже существовавшим hull+
  wedge -- ПЕРВАЯ визуализация во всём проекте, которая реально отражает
  §16 sidewall-состояние (`ShipDefenseState.port_sidewall_condition`/
  `starboard_sidewall_condition`/`bow_sidewall_raised`+`bow_sidewall_
  condition`/`stern_sidewall_raised`+`stern_sidewall_condition`), а не
  только wedge. Видимость каждой из 4 панелей решается независимо через
  два чистых статических метода (`_broadside_sidewall_visible`/
  `_bow_stern_sidewall_visible`), юнит-тестируемых без сцены.
* Новый `OrbitCamera` (`scripts/orbit_camera.gd`, `extends Camera3D`) --
  ТЗ §56.1 item 1 ("camera the player can see the battle through (free/
  orbit camera is enough)"). ПКМ-драг -- орбита, колесо мыши -- зум,
  стрелки + +/- -- клавиатурный фоллбэк без мыши. Начальный кадр
  побитово воспроизводит старую фиксированную камеру (тот же
  view_dir/дистанция/fov), чтобы не менять уже проверенный дефолтный
  вид -- всё новое включается только когда игрок реально трогает
  мышь/клавиши. `main.gd`'s `_frame_camera_on_ships()` теперь вызывает
  `camera.frame_on(midpoint, spread)` вместо ручной установки
  transform'а напрямую (с фоллбэком на старое поведение, если камера --
  простой `Camera3D` без скрипта).
* `project/scenes/main.tscn`: `Camera3D`-нода получила `orbit_camera.gd`
  как script (второй `ext_resource`), новых нод в сцену не добавлено.
* Новые тестовые файлы: `test_sidewall_mesh_builder.gd` (геометрия
  панелей: broadside плоская по X и покрывает length/height, bow/stern
  плоская по Z и покрывает width/height), `test_ship_view_sidewalls.gd`
  (все 6 комбинаций видимости: broadside показан/скрыт на пороге
  burnout, bow/stern скрыт БЕЗ raised даже при полном condition,
  показан при raised+condition>0, скрыт при raised+burned-out),
  `test_orbit_camera.gd` (чистая сферическая математика offset:
  нулевые yaw/pitch смотрят вдоль +Z, pitch=90° поднимает камеру строго
  по Y, yaw=90° -- строго по X, длина offset всегда равна distance
  независимо от угла). Все три новых теста + весь существующий набор
  (33 тестовых файла, включая до этого прохода незакоммиченный
  test_ship_class_data.gd, вошедший в предыдущий коммит параллельного
  прохода) прогнаны ОДИН раз перед этим коммитом -- 0 реальных
  провалов (один файл, test_crossing_t_maneuver.gd, дал ложное "FAIL" в
  простом grep-детекторе прогона из-за нестандартного текста
  собственного PASS-сообщения; проверено отдельно -- exit code 0,
  "CROSSING-T BEHAVIOR TEST PASSED").
* Обнаружен и явно задокументирован (НЕ регрессия этого прохода,
  проверено откатом на предыдущий commit и повторным запуском) уже
  существовавший headless-артефакт: живой запуск `main.tscn` печатает
  `ERROR: Parameter "m" is null (mesh_get_surface_count)` один раз на
  каждый процедурный `MeshInstance3D` (было 6 на 2 корабля, стало 14
  после добавления sidewall-панелей) -- следствие dummy rendering
  driver'а Godot в headless-режиме, не признак сломанного меша (геометрия
  проверена отдельными юнит-тестами, ошибок не даёт). См. ASSUMPTIONS.md.

Честно НЕ сделано (§56.1 item 2 остальное + items 3-8 полностью, следующие кандидаты):
canon-фигуры/материалы вместо плейсхолдер-геометрии (явно вне рамок
этого среза); визуализация выстрелов энергетического оружия/ракет
(item 3); минимальный HUD с состоянием подсистем/целей/контактов
(item 4); игровой ввод приказов на хоткеях, включая единую Input Map
(item 5, естественно объединяется с камерой позже); захардкоженный
стартовый сценарий 1v1/2v2 (item 6); win/lose состояние (item 7);
Windows-экспорт (item 8). Sidewall-панели сейчас чисто декоративны --
никакой новый геймплей/приказ их не раздвигает (raise/lower для bow/
stern уже есть в бэкенде через `ShipDefenseState`, но нет UI/AI-пути,
который бы это переключал в живой демо-сцене `main.gd` -- обе демо-ноды
только двигаются тягой, sidewalls остаются в default-состоянии).

## Unreleased -- §56.1 vertical slice item 3: weapon-fire visualization (энергооружие + ракеты)

* `SimulationWorld` (`simulation/simulation_world.gd`): новое транзитное
  поле `last_tick_weapon_shots` -- список выстрелов энергооружием,
  реально произведённых ТЕКУЩИМ тиком (мушка/бим-эффект для рендера),
  наполняется в `fire_weapon()` для ЛЮБОГО исхода, при котором ствол
  реально расходует цикл перезарядки (т.е. прошёл проверки NOT_READY/
  OUT_OF_RANGE/NO_ARC у `WeaponResolution.fire` -- луч рисуется даже если
  выстрел заблокирован клином/сайдволом, не только при пробитии),
  очищается в начале каждого `tick_simulation()`. Специально СДЕЛАНО
  ОТДЕЛЬНО от существующего `_record_event`/`replay_log` -- тот лог
  no-op, пока не вызван `start_recording()` (Milestone 12 Replay, вне
  рамок §56.1), а живому рендеру эти данные нужны каждый тик независимо
  от того, идёт ли запись реплея.
* Новый `BeamFxMeshBuilder` (`scripts/beam_fx_mesh_builder.gd`) -- чистая
  геометрия одного луча (два скрещенных тонких квада вдоль ЛОКАЛЬНОЙ -Z,
  от z=0 до z=-length_m, единая конвенция с `attack_geometry.gd`'s "-Z =
  bow"), не решает, произошёл ли выстрел -- только форма.
* Новый `WeaponFx` (`scripts/weapon_fx.gd`, `extends Node3D`, рендер-
  only узел, ТЗ §42): `update(world)` на каждый тик спавнит по одному
  лучу на запись в `last_tick_weapon_shots` (цвет луча -- чистая функция
  от УЖЕ ВЫЧИСЛЕННОГО `outcome`: красный -- пробитие, синий -- клин,
  янтарный -- сайдвол-затухание, серый -- прикрыт формацией; сама эта
  функция ничего не решает, только красит), луч гаснет за 0.18с через
  `_process(delta)`. Отдельно и НЕПРЕРЫВНО (не по событию, т.к. ракета --
  длящаяся сущность §18, а не одномоментное событие) синхронизирует
  маркеры-сферы для каждой живой записи в `world.missiles`, добавляя/
  убирая маркер diff'ом по ключам словаря каждый тик.
* `main.gd`: `weapon_fx` создаётся и добавляется в сцену, `_on_tick`
  вызывает `weapon_fx.update(world)` ПОСЛЕ того, как `SimulationWorld`
  сам обработал тик (порядок гарантирован порядком `connect()`-вызовов
  на `clock.simulation_tick` -- `SimulationWorld._ready()` подключается
  первым внутри `add_child(world)`, `main.gd` -- вторым; задокументировано
  в обоих файлах). Честно: демо-сцена ДО СИХ ПОР не назначает `teams`
  двум демо-кораблям, так что `_resolve_weapons_ai`/`_resolve_missile_launch_ai`
  никого не выбирают целью и `last_tick_weapon_shots`/`world.missiles`
  в текущей живой сцене всегда пустые -- реальная стрельба появится
  только с item 6 ("Hardcoded 1v1/2v2 scenario vs TacticalAI"), это
  намеренно оставлено ЕМУ, а не сделано здесь заранее, чтобы не дублировать
  ту работу.
* Новый `test_beam_fx_mesh_builder.gd` (4 чистых геометрических теста:
  длина луча вдоль -Z, ширина по X/Y, более длинный выстрел даёт более
  длинный меш, ровно 2 скрещенных квада = 12 вершин) -- прогнан отдельно,
  "ALL TESTS PASSED". Согласно правилу §56.1 "fast visible-result", полный
  набор симуляционных тестов НЕ гонялся -- вместо этого headless
  `--import`/`--check-only` смоук на изменённые файлы + живой headless-
  прогон `main.tscn` (`--quit-after 60`), сравненный С ПОМОЩЬЮ временной
  копии репозитория ДО/ПОСЛЕ этого прохода (git stash в отдельной
  scratch-копии, не в рабочей папке пользователя): ровно те же 14
  повторений уже задокументированного (см. предыдущую запись этого файла)
  dummy-renderer артефакта `mesh_get_surface_count` -- новых ошибок этот
  проход не добавил.
* Попутно исправлена случайная дублирующая строка `.tools/` в
  `.gitignore` (шум предыдущего прохода, no-op сам по себе, но лишний
  diff) -- не относится к item 3, просто убрано по пути.

Честно НЕ сделано (item 3 остальное + items 4-8):
демо-сцена всё ещё не стреляет живьём (см. выше -- ждёт item 6); нет
частиц/затухающего следа для ракет (просто маркер-сфера, положение из
`MissileState.position`, без хвоста/дыма); минимальный HUD (item 4);
ввод приказов на хоткеях (item 5); захардкоженный сценарий против
`TacticalAI` (item 6); win/lose (item 7); Windows-экспорт (item 8).

## Unreleased -- §56.1 vertical slice item 6: hardcoded 1v1 scenario vs TacticalAI (демо-корабли реально стреляют)

* `main.gd`: демо-корабли `alpha`/`beta` теперь получают команду через
  `world.set_team()` (`"red"`/`"blue"`, взаимно враждебны по
  `is_hostile()`), а их `WeaponMount` регистрируется НЕПОСРЕДСТВЕННО на
  `world` через `world.add_weapon_mount()` -- вместо отдельного
  собственного словаря `mounts`, который раньше вообще не был виден
  `SimulationWorld`, так что `_resolve_weapons_ai`/
  `_resolve_missile_launch_ai` никогда не находили ни целей, ни стволов.
  Отдельного класса/инстанса "AI" заводить не пришлось: обе стороны без
  игрока автоматически ведутся общей логикой `_resolve_weapon_target` ->
  `TacticalAI.select_weapon_target` (ближайший подходящий контакт), т.е.
  этот захардкоженный сценарий и есть "TacticalAI vs TacticalAI" ровно в
  том смысле, что описан в чеклисте -- управляемой игроком стороны пока
  нет (это item 5, ещё не сделан).
* Per-tick advance кулдауна стволов (`WeaponMount.tick(dt)`) остаётся
  ответственностью `main.gd` (как и раньше) -- `SimulationWorld.
  tick_simulation()` его сам не вызывает (в отличие от
  `MissileTube.advance(dt)`, который уже дергается изнутри
  `_resolve_missile_launch_ai`); просто цикл теперь бежит по
  `world.weapon_mounts`, а не по своему старому словарю.
* **Найден и исправлен баг при первой же живой проверке**: изначально
  стволы были заданы с `WeaponMount.bow_chaser_arc()` (только сектор
  BOW). При стартовой геометрии демо-сцены (обе демо-ноды стоят бортом
  друг к другу вдоль оси X, у `beta` разворот на 180° по Y, тяга у обеих
  вдоль СВОЕЙ локальной -Z) `AttackGeometry.classify()` относит цель к
  сектору STARBOARD у ОБЕИХ сторон с первого тика -- ни одна из них не
  разворачивается носом на врага (для этого нужен реальный маневровый
  AI, вне рамок этого среза), так что bow chaser никогда не находил
  сектор и `WeaponResolution.fire()` всегда возвращал `NO_ARC`
  (`fire_weapon()` намеренно не пишет `NO_ARC`/`OUT_OF_RANGE`/
  `NOT_READY` в `last_tick_weapon_shots`, т.к. это "выстрел не
  состоялся", не событие для рендера) -- ни выстрелов, ни лучей.
  Заменено на `WeaponMount.broadside_arc()` (PORT+STARBOARD), что
  реально соответствует стартовой геометрии этой конкретной сцены.
* Проверено ДВУМЯ способами (полный набор симуляционных тестов по
  правилу §56.1 "fast visible-result" не гонялся):
  1. Одноразовый (не закоммиченный, написан и удалён в рамках этого
     прохода) headless-скрипт на `SceneTree`, воспроизводящий ровно
     wiring `main.gd._ready()` без рендера и гоняющий
     `world.tick_simulation()` вручную 10 симулированных секунд --
     первый выстрел на тике 1 (`t=0.017s`), 6 выстрелов всего за 10с
     (упирается в recharge_time_s=4.0 обоих стволов). С исходным
     `bow_chaser_arc()` тот же скрипт честно показывал "0 выстрелов за
     10с", что и подтвердило баг ДО замены на broadside.
  2. Headless `--import` (чисто, без ошибок) + headless прогон
     `res://scenes/main.tscn --quit-after 120` -- 0 новых типов
     ошибок/предупреждений; уже задокументированный dummy-renderer
     артефакт `mesh_get_surface_count`/"Parameter m is null" вырос с 14
     повторений (баз��вая линия из прохода item 3, когда выстрелов ещё
     не было) до 16 -- на 2 больше, что соответствует РЕАЛЬНО
     появившимся beam-мешам от состоявшихся выстрелов в первые ~2с
     живого прогона, не новый баг.

Честно НЕ сделано (item 6 остальное + items 4-8):
2v2 вариант сценария (сделан только 1v1 -- минимально достаточно,
задача явно просила "SIMPLEST hardcoded setup"); ракетные трубы демо-
кораблям не добавлены (энергооружие -- уже достаточно, чтобы визуально
проверить весь путь item 3 живьём; ракетные маркеры остаются
подтверждены только юнит-тестами, не живым прогоном -- это выбор
конфигурации сценария, не пробел в возможностях `WeaponFx`); минимальный
HUD (item 4); ввод приказов на хоткеях (item 5); win/lose (item 7);
Windows-экспорт (item 8).

## Unreleased -- §56.2 ТЗ: UI/control correction (тактический плот, мышь, канон-масштаб)

Документационное обновление, без изменений в коде/тестах. По итогам
живого теста первой сборки §56.1 пользователь отклонил текущий UI
(корабли почти впритык, управление хоткеями, простой текстовый HUD) --
"недозвёздные войны", не Honorverse. Явное уточнение жанра: тактическая
стратегия. Добавлен AGENTS.md §56.2 (переоткрывает и частично заменяет
пункты 1/4/5 §56.1): управление мышью как основное, дистанции боя в
канон-масштабе (символически, не буквальным 1:1 рендером -- см.
ASSUMPTIONS.md), обязательный тактический плот с контактами и отдельно
маркированными ракетами. CLOUD.md §1.9 и CANON_RULES.md
("Двадцать первая сверка") синхронизированы. Definition of done для
§56.2 явно требует живого подтверждения от пользователя, не только
headless-тестов -- §56.1 уже был один раз преждевременно назван
"закрытым" по headless-данным и провалился при первом живом запуске.

## Unreleased -- §56.2 items A+B: tactical plot with IFF-coloured contacts + distinctly-marked missiles

По итогам отклонения пользователем §56.1-сборки (см. предыдущую запись)
реализован сам тактический плот, а не только документация. Новые файлы:

* `scripts/tactical_plot_projector.gd` (`class_name TacticalPlotProjector`,
  `extends RefCounted`) -- чистая геометрия world->plot, тот же паттерн, что
  `BeamFxMeshBuilder`/`SidewallMeshBuilder`: статическая `project(world_pos,
  origin, plot_range_m, plot_pixel_radius)` возвращает экранный офсет,
  ИСТИННУЮ (несжатую) дистанцию, пеленг и флаг `clamped`. Дистанция-
  компрессия (ASSUMPTIONS.md §56.2, INTERPRETATION, формула зафиксирована
  в этом проходе): линейный радиальный масштаб, авто-подбираемый под
  дальность самого дальнего живого контакта (`x1.15`, пол `50_000 м`);
  контакт дальше текущего `plot_range_m` не пропадает, а "прижимается" к
  краю плота по своему истинному пеленгу (с кольцом-маркером "clamped"
  вокруг иконки) -- при этом текстовая дистанция у иконки ВСЕГДА реальная,
  канон-масштабная цифра, именно это и держит "цифры канон-масштабны" из
  требования §56.2 item 2, а не буквальное положение иконки.
* `scripts/tactical_plot.gd` (`class_name TacticalPlot`, `extends Control`) --
  сам плот: чистый рендер уже посчитанного состояния `SimulationWorld`
  (§42, тот же принцип, что `Hud`/`WeaponFx`), никакой новой боевой логики.
  Источник контактов -- `world.sensor_contacts[pov_ship_id]`, а НЕ
  `world.ships`/`world.missiles` напрямую: `SimulationWorld._update_sensors`
  уже пишет туда И корабли, И ракеты одним и тем же вызовом
  `SensorResolution.update_contacts` (проверено чтением исходника) --
  значит плот честно отражает §23 sensor detection/fog-of-war (то, что
  реально не задетектировано, не рисуется; потерянный контакт тускнеет, а
  не мгновенно пропадает/обновляется) вместо всеведущей отрисовки world-
  правды. Корабль/ракета различаются тем же способом, что уже использует
  `SimulationWorld._update_missiles` для counter-missile: `contact.target
  is MissileState`.
  - Item A: каждый живой корабль-контакт -- треугольная иконка, цвет по
    IFF/hostility (`world.is_hostile()` -- красный враг, зелёный тот же
    team, жёлтый нейтрал/неизвестно), + вектор-лидер скорости (60с полёта,
    ASSUMPTION-константа как `WeaponFx.BEAM_WIDTH_M`, не канон-цифра), +
    текстовая подпись "id  дистанция  пеленг". Своя иконка (квадрат) в
    центре плота для POV-корабля.
  - Item B: живой ракетный контакт -- отдельная форма (жёлтый X-крест, цвет
    как у 3D-маркера `WeaponFx`), НЕ треугольник корабля -- различимо с
    первого взгляда, не только по цвету, как и требует чеклист.
  - Контакты не в состоянии DETECTED/TRACKED (т.е. ESTIMATED/UNCERTAIN/
    LOST -- устаревшие, не живая детекция этого тика) рисуются
    полупрозрачными -- честное отражение §23-состояния, которое иначе
    терялось бы.
  - Кольца дальности (4 шт, с km/Mkm подписями) + рамка плота, авто-масштаб
    каждый `update()`.
  - `mouse_filter = Control.MOUSE_FILTER_IGNORE` пока намеренно (item C,
    мышь, ещё не сделан -- см. state.md) -- плот пока ничего не перехватывает.
* `simulation/tests/test_tactical_plot_projector.gd` -- 8 headless-тестов на
  чистую функцию проекции: пеленг 0/90/180 градусов для контактов по -Z/+X/
  +Z, истинная `range_m` сохраняется даже при `clamped=true`, линейность
  масштаба внутри `plot_range_m` (половина дальности -> половина пикселей),
  прижатие к краю при выходе за `plot_range_m`, вырожденный случай "контакт
  в точке origin" не падает, дальний контакт всегда даёт офсет больше
  ближнего (до прижатия к краю). Все 8 прошли.
* `scripts/main.gd`: `tactical_plot` заведён и добавлен в сцену рядом с
  `hud` (НЕ вместо неё -- item E), `tactical_plot.update(world,
  HUD_POV_SHIP_ID)` вызывается каждый тик сразу после `hud.update(...)`,
  тот же POV-корабль ("alpha"), что и HUD.
* `ASSUMPTIONS.md`: заполнена "CHOSEN this pass" часть существовавшего
  стаб-раздела "§56.2 -- distance-compression convention" -- зафиксирована
  именно линейная радиальная схема (не логарифмическая) и её обоснование;
  явно отмечено, что это только конвенция РЕНДЕРА плота, а не изменение
  самих дистанций симуляции (item D, отдельно, ещё не сделан).

Проверено (правило §56.1/§56.2 "fast visible-result": полный набор
симуляционных тестов не гонялся):
1. `godot --headless --import` -- чисто, без ошибок.
2. Новый `test_tactical_plot_projector.gd` headless -- `ALL TESTS PASSED`
   (8/8).
3. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit 0,
   РОВНО те же 16 повторений уже задокументированного dummy-renderer
   артефакта `mesh_get_surface_count`/"Parameter m is null" (baseline с
   прохода item 6 §56.1), ноль новых типов ошибок/предупреждений --
   `draw_string`/`ThemeDB.fallback_font`/остальные вызовы `Control._draw()`
   не породили ничего нового в headless-режиме.

Честно НЕ сделано (items C/D/E остаются открытыми в .tools/state.md):
мышь как основной ввод (item C -- клик по контакту, клик/драг для курса,
контекстное меню на приказы) вообще не начат, плот сейчас
`MOUSE_FILTER_IGNORE` и ничего не перехватывает; канон-масштаб дистанций
самого сценария (item D) не начат -- захардкоженные alpha/beta всё ещё
на ~10км друг от друга, а не сотнях тысяч км (см. явное предупреждение в
state.md про связку с `demo_laser.max_range_m`, чтобы при переносе
кораблей не сломать уже живьём проверенную стрельбу); item E (текстовый
HUD остаётся) выполнен как побочный эффект wiring'а A/B, но не отмечен
закрытым в чеклисте -- ждёт того же живого подтверждения, что и весь
§56.2. **НЕ проверено вживую пользователем** -- это чисто headless-
верифицированный проход, ровно то, от чего явно предостерегает
state.md/AGENTS.md §56.2 definition of done: собранный .exe с новым
плотом ещё не запускался и не подтверждён человеком.

## 2026-09-23 (внеплановый проход по срочному баг-репорту: §56.1 items 4/5 -- "нет HUD, не реагируют клавиши")

Ручной внеочередной запуск (fire_trigger, не по расписанию) по срочному
репорту пользователя: реальный запуск `project/build/HonorHarington.exe`
на его Windows-машине (первая живая проверка §56.1 вне headless) показал
рабочую 3D-сцену с кораблями, реально стреляющими друг в друга, но
**вообще без HUD-текста на экране** и **вообще без реакции на любые
клавиши**. Явно другой по формулировке отчёт, чем последующий §56.2
разговор про UI/UX ("не мышь, мелкий масштаб") -- здесь речь про полностью
нерабочие item 4 и item 5, а не про их недостаточное качество.

Расследование (без доступа к реальной Windows-машине или Vulkan в этом
окружении):
* `scripts/main.gd._ready()` безусловно доходит до `add_child(hud)` и
  `add_child(player_input)` -- прочитано построчно, никаких ранних
  return/exception-путей между ними нет.
* `project.godot` [input]-имена действий (`order_select_ship`,
  `order_target_next`, `order_target_clear`, `order_weapons_toggle`,
  `order_turn_left`, `order_turn_right`, `order_speed_up`,
  `order_speed_down`) побайтово совпадают с тем, что читает
  `player_input.gd` через `event.is_action_pressed(...)` -- опечаток нет.
* Написан и прогнан временный диагностический скрипт (SceneTree,
  `--script`, не закоммичен) под `xvfb-run` + кэшированный линуксовый
  `Godot_v4.3-stable_linux.x86_64` (Vulkan в этом окружении вообще
  недоступен -- ни `vulkaninfo`, ни lavapipe/swiftshader, ни ICD-файлов;
  проверено явно) с `--rendering-driver opengl3` (реальный, не dummy,
  рендер-бэкенд): `Hud`/`HudLabel` создаётся, `visible == true`, текст
  после первого тика -- настоящие 286 символов с корректным содержимым
  (`ALPHA`/`SUBSYSTEMS`/`TARGET`/`CONTACTS`), позиция (12,12), CanvasLayer
  `layer=1`/`visible=true`. Инжектированное нажатие `order_turn_left` дошло
  до `PlayerInput._unhandled_input`, записалось в
  `_desired_heading_by_ship["alpha"]` и реально сдвинуло
  `ship.commanded_thrust_local` за несколько тиков (передача приказа с
  задержкой связи, как и задокументировано в самом player_input.gd).
  Скриншот (`dev_probe_tmp/hud_probe.png`, не закоммичен) визуально
  подтвердил то же самое.
* То же самое (текст/visible, без реального рендера) воспроизведено и в
  чистом `--headless` (dummy-рендерер) -- значит для регресс-тестов
  Xvfb не обязателен, реальный `get_texture()`/скриншот там не работает
  (`Parameter "t"/"m" is null`), но `Label.text`/`.visible` -- это просто
  данные, не зависящие от рендер-бэкенда.

Итог: логика Hud/PlayerInput корректна везде, где её можно проверить из
этого окружения (headless И реальный OpenGL-рендер под Xvfb). Гипотеза
"item 4/5 сломаны в самом GDScript" НЕ подтвердилась. Осталось два вероятных
объяснения, ни одно из которых нельзя проверить без живой Windows-машины:
(a) окно не получает OS-фокус ввода при запуске (известный класс проблем
Godot/Windows-экспорта -- например, окно SmartScreen или debug console
wizard перехватывает фокус) -- правдоподобно объясняет item 5, но НЕ item 4
(рендер HUD-текста не требует фокуса ввода); (b) что-то специфичное для
Vulkan/Forward+ (`project.godot` `rendering_method="forward_plus"`) на
конкретном GPU/драйвере пользователя -- проверить в этом окружении
невозможно (Vulkan отсутствует вообще).

Сделано в коде (минимально, без расширения scope на §56.2):
* `scripts/main.gd._ready()`: в самом конце добавлены
  `get_window().grab_focus()` и `DisplayServer.window_move_to_foreground()`
  -- защитная, малорисковая попытка исправить именно item 5 (гипотеза (a)).
  Безопасно под `--headless` (проверено, ни одной новой ошибки/варнинга),
  само по себе НЕ объясняет и не может "случайно тоже починиться" item 4.
* `simulation/tests/test_hud.gd` (11 проверок) -- фиксирует регрессом, что
  `Hud._ready()` создаёт видимый `HudLabel`, и `update()` после одного тика
  даёт непустой текст с ожидаемыми секциями (в том числе для неизвестного
  ship_id -- не должно быть пустого HUD).
* `simulation/tests/test_player_input.gd` (15 проверок) -- фиксирует, что
  все action-имена из project.godot реально читаются PlayerInput, и что
  order_turn_left/order_speed_up/order_weapons_toggle реально доходят до
  world (первые два -- напрямую в `_desired_heading_by_ship`/
  `_desired_speed_by_ship`, третий -- через
  `world._pending_command_transmissions`, т.к. `transmit_ship_weapons_free`
  комм-задержан и не применяется мгновенно).
* `.gitignore`: добавлены `project/dev_probe_tmp/`/`project/probe.log`
  (черновые файлы диагностики, не коммитятся).

Проверено (правило §56.1/§56.2 "fast visible-result" всё ещё в силе,
полный симуляционный набор не гонялся):
1. `godot --headless --import` -- чисто.
2. `test_hud.gd` -- ALL TESTS PASSED (11/11).
3. `test_player_input.gd` -- ALL TESTS PASSED (15/15).
4. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit 0,
   те же самые 16x baseline `mesh_get_surface_count`/"Parameter m is null"
   (dummy-рендерер), ноль новых типов ошибок -- `grab_focus()`/
   `window_move_to_foreground()` ничего не сломали в headless-режиме.
5. Отдельный Xvfb-прогон реальной сцены (см. выше) -- не регресс-тест,
   разовая живая диагностика, скриншот и логи оставлены в
   гитигнорнутом `project/dev_probe_tmp/`.

**НЕ заявляется "§56.1 items 4/5 исправлены"** -- честно неизвестно,
помог ли фокус-фикс на реальной машине, и HUD может всё ещё быть невидим
по причине (b), которую отсюда никак не проверить. Пользователю отправлен
явный запрос пересобрать и перезапустить `HonorHarington.exe` и подтвердить
живьём: виден ли теперь HUD-текст, реагируют ли клавиши -- см. .tools/state.md
"Side note" за эту дату.

## Unreleased -- §56.3 ТЗ: полный tactical command UI (адаптация пользовательской спецификации)

Документационное обновление, без изменений в коде/тестов. Пользователь
предоставил детальную UI/UX-спецификацию (файл CHATGPT.md в корне
проекта, написан с помощью ChatGPT) и референс-изображение
(двухпанельный макет: fleet-view со списком эскадр + тактический
плот-миникарта сверху, приближённый 3D-вид с панелями оружия/приказов
снизу) как ответ на живой тест §56.2 -- "не так я себе это
представлял". Спецификация перенесена дословно в AGENTS.md §56.3 (как
раздел, расширяющий/уточняющий §56.2, не отменяющий уже сделанное:
плот, IFF-контакты, ракетные маркеры, клик-выбор мышью, канон-масштаб
дистанции), референс-картинка скопирована в
docs/reference/tactical_command_ui_reference.png. Зеркало в CLOUD.md
§1.10, запись в ASSUMPTIONS.md (design-решение, не канон) и
CANON_RULES.md ("Двадцать вторая сверка", не канон-сверка, просто
фиксация источника). §56.3's MVP acceptance test (15 шагов, почти всё
мышью) теперь официальный критерий готовности command-UI части
вертикального среза.

## §56.3 item A: multi-select mechanics on the tactical plot (2026-09-23, scheduled dev pass, manually fired)

Первый пункт нового чек-листа §56.3 (полный tactical command UI, см.
AGENTS.md §56.3 / CLOUD.md §1.10 / .tools/state.md). Реализована
механика мультивыбора на существующем tactical plot (§56.2 items A/B):

* `scripts/selection_state.gd` (новый, `class_name SelectionState`) --
  единое, разделяемое между видами хранилище текущего выбора (id-шники
  кораблей/контактов), без какой-либо orders-логики внутри (по тому же
  принципу separation of concerns, что и весь остальной проект --
  §42). CTRL+LMB = toggle, SHIFT+LMB = add-only (никогда не убирает),
  plain click/drag = replace. Точное обоснование выбора семантики для
  неоднозначного текста ТЗ (SHIFT конкретно) -- ASSUMPTIONS.md "§56.3
  item A".
* `scripts/tactical_plot_selection.gd` (новый, `class_name
  TacticalPlotSelection`) -- чистая геометрия hit-testing (точка/
  прямоугольник против списка уже отрисованных иконок), без Control/
  Viewport, unit-тестируемая headless тем же способом, что и
  `tactical_plot_projector.gd`.
* `scripts/tactical_plot.gd`: `mouse_filter` переключён с IGNORE на
  STOP (плот теперь реально получает клики); `_gui_input()` обрабатывает
  LMB press/release + drag-порог (4px) для различения клика и
  drag-box; хранит `_last_icons` (id/pos/radius) как единственный
  источник правды для hit-testing, перестраивается каждый `_draw()` --
  тот самый leftover-комментарий из state.md времён §56.2 ("keep each
  contact's last-drawn icon_pos/radius for hit-testing") наконец
  реализован. Выбранные иконки получают визуальное кольцо-выделение
  (`_draw_selection_ring`); во время активного drag рисуется
  полупрозрачный прямоугольник резинки. Собственный корабль (POV)
  теперь тоже входит в hit-test список (можно выбрать себя же).
* `scripts/main.gd`: создаёт один `SelectionState` в `_ready()` и
  передаёт его в `tactical_plot.selection` -- эта же переменная
  `main.selection` будет переиспользована будущими §56.3 item B/D
  панелями (список эскадр, контекстное меню приказов), не
  изобретается параллельная копия выбора для каждой панели.
* Тесты (новые, оба ALL TESTS PASSED): `simulation/tests/
  test_selection_state.gd` (9 проверок -- select_only/toggle/add_only/
  clear/is_selected/сигнал selection_changed, включая отсутствие
  лишнего emit на no-op clear), `simulation/tests/
  test_tactical_plot_selection.gd` (8 проверок -- hit_test точное
  попадание/промах/пустой список/выбор ближайшей иконки при
  перекрытии, box_test попадание/промах/пустой список/устойчивость к
  перевёрнутому прямоугольнику drag).
* `ASSUMPTIONS.md`: новая запись "§56.3 item A" фиксирует
  интерпретацию неоднозначного текста ТЗ про SHIFT+LMB.

НЕ входит в этот пасс (следующие пункты того же чек-листа, ещё НЕ
реализованы): B (именованная командная группа / squadron-list панель),
C (RMB move order с визуализацией курса), D (контекстное меню
приказов), E-I. Сценарий по-прежнему 1v1 (item H ещё не сделан), так
что живой мультивыбор больше чем одного своего корабля пока
некого выбирать -- проверено юнит-тестами (синтетические иконки) и
headless smoke, НЕ живым запуском с несколькими кораблями.

Проверено (режим "fast visible-result" §56.3 всё ещё в силе, полный
симуляционный набор не гонялся):
1. `godot --headless --import` -- чисто.
2. `test_selection_state.gd` -- ALL TESTS PASSED (9/9).
3. `test_tactical_plot_selection.gd` -- ALL TESTS PASSED (8/8).
4. `test_tactical_plot_projector.gd` (регресс, не менялся) -- ALL
   TESTS PASSED (8/8, без изменений).
5. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit
   0, те же самые 16x baseline `mesh_get_surface_count`/"Parameter m is
   null" (dummy-рендерер), ноль новых типов ошибок -- смена
   `mouse_filter`/добавление `_gui_input` на TacticalPlot ничего не
   сломала в headless-режиме (там нет реальных mouse-событий, но
   компиляция и `_ready()`/`_draw()` пути выполнились чисто).

**НЕ заявляется "§56.3 закрыт"** -- это только пункт A из 9+1. Мышь
реально не тестировалась живым пользователем (headless-окружение не
может сгенерировать настоящие InputEventMouseButton/Motion события с
модификаторами) -- см. .tools/state.md, тот же урок §56.1/§56.2 ещё
раз: не объявлять готовым без живого подтверждения.

## 2026-09-26 -- §56.3 item C: RMB move order + курсовая визуализация (scheduled dev pass)

Реализован и headless-проверен пункт C чек-листа §56.3 (AGENTS.md §1.10.6):
ПКМ по точке на tactical plot -> приказ движения для текущего выбора
(одиночный корабль или командная группа), с визуализацией курса.

* `scripts/tactical_plot_projector.gd`: новый `unproject()` -- точный
  алгебраический обратный к уже существующему `project()` (пиксель на
  плоте -> мировая точка), тот же origin/plot_range_m/plot_pixel_radius,
  что и прямое проецирование, так что "куда кликнул -- то и получил"
  гарантированно согласовано с тем, что отображается.
* `scripts/move_order_controller.gd` (новый): чистый транслятор
  ввод->приказ, тот же паттерн что и CommandGroupController/PlayerInput.
  Отбор: только реальные корабли (`world.ships`) команды игрока
  (`player_team`, а не просто "одна команда друг с другом" как у
  CommandGroupController -- ошибочно увести вражеский AI-корабль кликом
  недопустимо). Маршрутизация: если выбранный корабль состоит в
  formation (guide или member, проверяется каждый раз против
  `world.formations`, без кэширования) -- приказ идёт ОДИН РАЗ на всю
  formation через `world.issue_formation_order_now` (FormationOrder.
  APPROACH, уже существующий примитив -- см. class doc), с дедупликацией
  при выборе нескольких участников одной formation; одиночный корабль
  без formation получает `IndividualOrder` через
  `world.transmit_individual_order_now` (та же comm-delay семья §25/§31,
  что уже использует PlayerInput -- ПКМ-приказ не является особым
  мгновенным случаем).
* Два непростых открытия, зафиксированных отдельно в ASSUMPTIONS.md
  "§56.3 item C" (важно для будущих пассов, не самоочевидно из кода):
  1. `world.issue_echelon_order` только СТАВИТ В ОЧЕРЕДЬ приказ
     (`FormationState.issue_order`, а не `issue_order_now`) -- нет
     `issue_echelon_order_now`. Свежий move-order обязан ЗАМЕНЯТЬ
     текущий, а не вставать за ним в очередь, поэтому используется
     `world.issue_formation_order_now` напрямую по formation_id, а не
     маршрут через echelon.
  2. Одиночный корабль: выбор между `IndividualOrder.change_course`
     (уже движется -- держит скорость, меняет только курс) и
     `.change_speed` (стоит на месте -- разгон до курса) -- НЕ
     стилистический выбор. `CHANGE_SPEED.is_complete()` проверяет только
     МОДУЛЬ скорости, не направление; если новая целевая скорость равна
     текущей (ровно случай "уже быстрый, просто перенаправить"),
     is_complete() становится true на первом же тике ДО применения тяги
     на разворот -- корабль тогда вообще не поворачивает. Найдено
     собственным headless-тестом этого пасса при первой (наивной)
     реализации с одним `change_speed`.
* Визуализация (`tactical_plot.gd._draw_move_orders`/
  `_draw_move_order_arrowhead`): линия курса + стрелка + маркер конечной
  точки поверх уже существующего plot-рендера, читает UI-side
  `MoveOrderController.active_move_orders` (не пересчитывает
  simulation-truth заново -- §1.10.6 явно допускает ASSUMPTION по
  визуальному стилю). Точка отмечается достигнутой (запись убирается) в
  `prune_completed()`, вызываемом раз в тик из main.gd. Сознательно НЕ
  рисуется "точка разворота" -- манёвренная модель проекта не имеет
  отдельной фазы разворот-затем-тяга (тяга всенаправленная относительно
  ориентации), см. ASSUMPTIONS.md.
* Тесты (новые, оба ALL TESTS PASSED): `simulation/tests/
  test_move_order_controller.gd` (29 проверок -- пустой выбор,
  исключение чужой команды/не-кораблей, comm-delay для одиночного
  приказа, change_course vs change_speed в зависимости от текущей
  скорости, клик по своей же позиции -- no-op, маршрутизация на
  formation вместо individual для участника группы, дедупликация при
  выборе двух участников одной formation, запись/очистка
  active_move_orders включая "корабль пропал из world.ships"),
  `simulation/tests/test_tactical_plot_projector.gd` (+3 новые проверки
  для `unproject()` -- точная обратность project(), клик в центр ->
  origin, сохранение Y origin'а; итого файл всё ещё ALL TESTS PASSED).

НЕ входит в этот пасс (следующие пункты того же чек-листа, ещё НЕ
реализованы): D (контекстное меню приказов по цели), E (панель оружия),
F (командная камера + общий layout панелей), G (поведение зума), H
(сценарий 1v1 -> эскадра vs эскадра), I (individual override внутри
группы). LMB-drag как альтернативный способ задать курс (тоже упомянут
в §1.10.6) НЕ реализован этим пассом -- ПКМ-клика оказалось достаточно
для этого среза, drag отложен (см. ASSUMPTIONS.md).

Проверено (режим "fast visible-result" §56.3 всё ещё в силе, полный
симуляционный набор не гонялся):
1. `godot --headless --import` -- чисто.
2. `test_move_order_controller.gd` -- ALL TESTS PASSED (29/29).
3. `test_tactical_plot_projector.gd` (регресс + 3 новые) -- ALL TESTS
   PASSED (11/11).
4. `test_command_group_controller.gd` (регресс, не менялся) -- ALL
   TESTS PASSED.
5. `test_individual_orders.gd` (регресс, не менялся) -- ALL TESTS
   PASSED (36/36).
6. `test_command_transmission.gd` (регресс, не менялся) -- ALL TESTS
   PASSED (18/18).
7. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit
   0, те же самые 16x baseline `mesh_get_surface_count`/"Parameter m is
   null" (dummy-рендерер), ноль новых типов ошибок -- новый RMB-обработчик
   и `_draw_move_orders`/`prune_completed`/wiring в main.gd ничего не
   сломали в headless-режиме.

**НЕ заявляется "§56.3 закрыт"** -- это только пункт C из 9+1 (A/B уже
были закрыты ранее). Мышь реально не тестировалась живым пользователем
(headless-окружение не может сгенерировать настоящие
InputEventMouseButton с правой кнопкой) -- см. .tools/state.md, тот же
урок ещё раз: не объявлять готовым без живого подтверждения.


## 2026-09-26 (scheduled dev pass) -- §56.3 item D: contextual order menu (target designation + routing)

ТЗ §56.3 item D / §1.10.7 ("Приказы по цели"): после выбора собственного
корабля/группы плоский (без Ctrl/Shift) LMB-клик по вражескому контакту
на tactical plot теперь открывает контекстное меню приказов, НЕ заменяя
текущий выбор собственных кораблей -- см. ASSUMPTIONS.md "§56.3 item D"
для полного разбора выбранной модели взаимодействия и всех решений по
маршрутизации.

* `scripts/selection_state.gd`: новое поле `designated_target_id` +
  `designate_target()`/`clear_designated_target()`/`has_designated_target()`
  -- отдельная от `selected_ids` концепция ("цель" половина
  собственный-выбор + вражеская-цель), не второй мульти-select.
* `scripts/order_menu_controller.gd` (новый, `OrderMenuController`):
  владеет всей логикой eligibility/маршрутизации приказов.
  `try_open(contact_id, screen_pos)` открывает меню только если
  контакт -- реальный вражеский корабль (не ракета/нейтрал/свой) И в
  текущем выборе есть хотя бы один свой корабль. Держит НИКАКОЙ ссылки
  на UI-контрол меню (та же §42 конвенция "controller пишет данные, view
  читает раз в тик", что и MoveOrderController/TacticalPlot) -- только
  `is_open`/`menu_screen_pos`/`menu_entries`, что делает класс полностью
  headless-тестируемым без Viewport/Control.
  * ATTACK/FOCUS FIRE -> `transmit_ship_target` + `transmit_ship_weapons_free(true)`
    на каждый eligible корабль (честно идентичны при текущих примитивах,
    см. ASSUMPTIONS.md).
  * HOLD FIRE / WEAPONS FREE -> `transmit_ship_weapons_free`.
  * APPROACH -> переиспользует `MoveOrderController.issue_move_order`
    (§56.3 item C) на текущую позицию цели -- ни строчки новой логики
    построения приказа.
  * WITHDRAW -> `FormationOrder.withdraw_orders`/`IndividualOrder.withdraw_orders`
    (уже существующие композиты) через ИММЕДИАТНЫЙ ОЧЕРЕДНОЙ API
    (`issue_formation_orders`/`issue_individual_orders`), НЕ через
    comm-delayed `transmit_*` -- см. ASSUMPTIONS.md почему двухшаговый
    композит не может безопасно идти через "_now"-семейство.
  * MAINTAIN FORMATION -> `issue_formation_order_now(..., FormationOrder.hold_formation())`,
    только если хотя бы один eligible корабль реально в formation.
  * DEFEND/COVER/FOLLOW/INTERCEPT НЕ реализованы и НЕ показаны в меню --
    честный пробел (нет "follow/escort" механики нигде в
    simulation/*.gd, проверено grep'ом до написания класса).
* `scripts/order_menu.gd` (новый, `OrderMenu` -- Control): чистая
  презентация/hit-testing. Обычный Control с ручной отрисовкой строк
  (draw_string/draw_rect) -- в проекте нигде не используется
  PopupMenu-виджет, см. ASSUMPTIONS.md. Полноэкранный modal-overlay
  (mouse_filter STOP пока открыт) -- клик вне строк меню закрывает его
  без действия. `sync()` вызывается раз в тик из main.gd (после
  `move_order_controller.prune_completed()`), читает
  `controller.is_open/menu_entries/menu_screen_pos`.
* `scripts/tactical_plot.gd`: новое поле `order_menu_controller`
  (опциональный collaborator, как `move_order_controller`); плоский клик
  по контакту сначала пробует `order_menu_controller.try_open(...)`, и
  только при false откатывается к прежнему `select_only([hit_id])`.
  Также новая визуальная отметка `_draw_designated_target_marker`
  (оранжевый ромб) вокруг `selection.designated_target_id`, отличная по
  форме и цвету от кольца выбора.
* `scripts/main.gd`: создаёт `order_menu`/`order_menu_controller`,
  добавляет `order_menu` В СЦЕНУ ПОСЛЕ `tactical_plot` (для приоритета
  input, см. ASSUMPTIONS.md), вызывает `order_menu.sync()` в `_on_tick`.
* Тесты (новый, ALL TESTS PASSED): `simulation/tests/test_order_menu_controller.gd`
  (42 проверки -- пустой выбор/свой корабль/не-корабль как цель = noop,
  успешное открытие меню + сохранение выбора + список пунктов включая
  отсутствие DEFEND/COVER/FOLLOW/INTERCEPT, MAINTAIN FORMATION
  появляется только для участника formation, dismiss ничего не issue'ит
  и снимает designation, ATTACK/HOLD FIRE проходят через comm-delay,
  APPROACH переиспользует MoveOrderController, WITHDRAW разворачивает
  корабль ОТ цели (не через comm-delay), MAINTAIN FORMATION issue'ит
  HOLD_FORMATION один раз на formation, execute() закрывает меню и
  снимает designation).

НЕ входит в этот пасс (следующие пункты того же чек-листа, ещё НЕ
реализованы): E (панель оружия), F (командная камера + общий layout
панелей), G (поведение зума), H (сценарий 1v1 -> эскадра vs эскадра), I
(individual override внутри группы). DEFEND/COVER/FOLLOW/INTERCEPT из
минимального набора §1.10.7 честно НЕ реализованы (см. выше).

Проверено (режим "fast visible-result" §56.3 всё ещё в силе, полный
симуляционный набор не гонялся):
1. `godot --headless --import` -- чисто.
2. `test_order_menu_controller.gd` (новый) -- ALL TESTS PASSED (42/42).
3. Регресс (не менялись, кроме selection_state.gd -- только аддитивно):
   `test_selection_state.gd`, `test_move_order_controller.gd` (29/29),
   `test_command_group_controller.gd`, `test_tactical_plot_selection.gd`,
   `test_tactical_plot_projector.gd`, `test_individual_orders.gd`,
   `test_command_transmission.gd`, `test_formation.gd`,
   `test_ship_combat_directive.gd` -- все ALL TESTS PASSED.
4. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit 0,
   ровно те же 16x baseline `mesh_get_surface_count`/"Parameter m is
   null" (dummy-рендерер), ноль новых типов ошибок -- новое
   `order_menu`/`order_menu_controller` wiring и `tactical_plot.gd`'s
   новая ветка в `_on_left_release` ничего не сломали в headless-режиме.

**НЕ заявляется "§56.3 закрыт"** -- это только пункт D из 9+1
(A/B/C уже были закрыты ранее). Меню реально не тестировалось живым
игроком (headless-окружение не может сгенерировать настоящие мышиные
клики) -- см. .tools/state.md, тот же урок ещё раз: не объявлять
готовым без живого подтверждения.

## 2026-09-26 (scheduled dev pass) -- §56.3 item E: панель выбора оружия для выбранного корабля

ТЗ §56.3 item E / §1.10.8 ("Выбор оружия"): для ОДИНОЧНОГО выбранного
своего корабля появляется постоянная (не модальная) боковая панель со
секциями ENERGY WEAPONS / MISSILES / POINT DEFENSE, читающая РЕАЛЬНО
установленное на этом корабле вооружение (`world.weapon_mounts`/
`world.missile_tubes`/`world.pd_mounts`), НИКОГДА не хардкодя список --
жёсткое требование §1.10.8 ("Нельзя показывать игроку оружие, которого у
корабля нет"). См. ASSUMPTIONS.md "§56.3 item E" для полного разбора
честных пробелов (per-missile-type, COUNTER-MISSILES, PD priority-target).

* `scripts/weapon_panel_controller.gd` (новый, Node, класс
  `WeaponPanelController`): та же §42 конвенция "controller пишет
  данные, view читает раз в тик", что MoveOrderController/
  OrderMenuController -- никакой ссылки на UI Control, только
  `is_visible`/`ship_id`/`rows` (`Array[{"label","kind"}]`, тот же
  one-kind-per-row паттерн, что `OrderMenuController.menu_entries`).
  Панель видна ТОЛЬКО когда текущий выбор -- РОВНО один свой корабль
  (`player_team`) -- групповой выбор/пустой выбор/вражеский контакт
  скрывают панель целиком (нет "чьё именно вооружение показывать" для
  группы, и нет утечки чужого loadout'а игроку).
  * ENERGY WEAPONS: read-only список `world.weapon_mounts[ship_id]`
    (класс оружия laser/graser через `WeaponData.weapon_class`, arc
    через `AttackGeometry.Sector`, ready/cooling статус через
    `mount.is_ready()`).
  * MISSILES (только если есть тюбы): "salvo size" (клик по строке
    циклит 1..tube_count..1, персистентно по ship_id), "throttle/profile"
    (клик циклит FULL BURN <-> EXTENDED RANGE, персистентно по
    ship_id) -- РЕАЛЬНО применяется через новый `throttle_fraction`
    параметр `SimulationWorld.order_missile_launch`/
    `_launch_missile_from_tube`, который вызывает уже существующий
    `MissileState.set_throttle()` (ТЗ §18.1 CANON "stepped down"
    drives) -- ни одной новой ракетной механики не изобретено, только
    впервые подключён к UI уже существующий примитив. "target" --
    read-only отображение `SelectionState.designated_target_id` (та же
    концепция, что item D уже использует для order-menu, второй target
    не заводился). FIRE MISSILES -> `world.order_missile_launch(ship_id,
    target_id, salvo_size, throttle)`.
  * POINT DEFENSE (только если есть PD-mounts): AUTO/HOLD, клик
    переключает -- РЕАЛЬНО новый механизм: `world.ship_pd_hold`
    (Dictionary) + `set_ship_pd_hold`/`transmit_ship_pd_hold` (тот же
    comm-delayed паттерн, что `transmit_ship_weapons_free`),
    `SimulationWorld._resolve_point_defense` пропускает корабль с
    `ship_pd_hold == true` целиком (PD-mounts вообще не engage'ят).
    "priority-target" designation честно НЕ реализован -- см.
    ASSUMPTIONS.md.
  * COUNTER-MISSILES НЕ показан вообще, ни одной disabled-строки --
    честный пробел, не недосмотр: в `simulation/*.gd` нет НИКАКОЙ
    automatic launch-decision механики для counter-missiles (grep
    проверен до написания класса) -- каждый counter-missile в этом
    кодбейзе сегодня вручную сконструированный `MissileState` с
    `target` = входящая ракета (см. `counter_missile_resolution.gd`'s
    own class doc); `CounterMissileResolution` только РЕЗОЛВИТ уже
    летящий counter-missile, никогда не решает его запустить. Реальный
    AUTO/MANUAL/HOLD policy требует сначала эту launch-decision
    механику -- это новая combat-AI фича, не UI/routing изменение (та
    же логика, что уже применена к DEFEND/COVER/FOLLOW/INTERCEPT в
    item D).
* `scripts/weapon_panel.gd` (новый, `WeaponPanel` -- Control): чистая
  презентация, тот же hand-drawn Control стиль (draw_string/draw_rect),
  что `OrderMenu`/`TacticalPlot`. В ОТЛИЧИЕ от `OrderMenu` НЕ модальная
  -- размер Control'а равен размеру собственного бокса (не полный
  viewport), поэтому клики вне панели проходят к tactical_plot/3D-виду
  как раньше без каких-либо изменений там. Позиция -- фиксированный
  bottom-left отступ (интерим-решение, как у `CommandGroupPanel`,
  полноценная раскладка -- item F, см. ASSUMPTIONS.md).
* `simulation/simulation_world.gd`:
  * `order_missile_launch(ship_id, target_ship_id="", max_launches=-1,
    throttle_fraction=1.0)` -- оба новых параметра ОПЦИОНАЛЬНЫ с
    дефолтами, сохраняющими старое поведение для каждого существующего
    вызова/replay-записи без изменений (`max_launches=-1` == без
    ограничения == старое поведение "стрелять из каждого готового
    тюба"; `throttle_fraction=1.0` == full burn == дефолт
    `MissileState` == `set_throttle()` вообще не вызывается).
  * `_launch_missile_from_tube(..., throttle_fraction=1.0)`: вызывает
    `missile.set_throttle()` СРАЗУ после конструирования, ДО
    `add_missile()` -- т.е. до первого тика интеграции, что и требует
    собственный doc comment `set_throttle()`.
  * `ship_pd_hold: Dictionary` (новое поле) + `set_ship_pd_hold`/
    `transmit_ship_pd_hold` + `_resolve_point_defense`'s новая проверка
    в начале цикла. Очистка в `remove_ship`/wreck-конверсии, как
    `ship_combat_directives`.
  * Replay-диспетчер (`_replay_command`): добавлены записи для
    `set_ship_pd_hold`/`transmit_ship_pd_hold`, и `order_missile_launch`
    теперь читает `max_launches`/`throttle_fraction` из `args` с теми же
    дефолтами -- старые записанные replay-логи (без этих полей) читают
    `args.get(..., default)`, поведение не меняется.
* `scripts/main.gd`: создаёт `weapon_panel_controller`/`weapon_panel`,
  тот же shared `world`/`selection`/`player_team`, добавляет
  `weapon_panel_controller.sync()`/`weapon_panel.sync()` в `_on_tick`
  сразу после `order_menu.sync()`.
* Тесты (новый, ALL TESTS PASSED): `simulation/tests/test_weapon_panel_controller.gd`
  (36 проверок -- видимость панели (пусто/вражеский/группа -> скрыта,
  одиночный свой корабль -> видна), ENERGY-секция с arc/статусом,
  MISSILES-секция появляется только при наличии тюбов, salvo size
  циклит и оборачивается по числу тюбов, throttle циклит FULL BURN
  <-> EXTENDED RANGE, FIRE ограничивает запуск ровно salvo size'ом
  из большего числа готовых тюбов И реально применяет пониженный
  throttle к запущенным ракетам (проверено через
  `missile.drive_max_acceleration_mps2`/`drive_burn_time_s`), PD-секция
  появляется только при наличии PD-mounts, toggle реально держится
  через comm-delay (`world.ship_pd_hold`) и реально ПРЕДОТВРАЩАЕТ
  engagement в тот же тик (прямая проверка через
  `PointDefenseMount.cooldown_remaining_s`), COUNTER-MISSILES не
  появляется НИКОГДА даже когда есть все остальные виды вооружения).

НЕ входит в этот пасс (следующие пункты того же чек-листа, ещё НЕ
реализованы): F (командная камера + общий layout панелей -- этой
панели тоже нужен полноценный layout pass), G (поведение зума), H
(сценарий 1v1 -> эскадра vs эскадра, включая выдачу missile tubes хотя
бы одной стороне -- ТЕКУЩИЙ демо-сценарий в main.gd по-прежнему
energy-only, поэтому MISSILES/POINT DEFENSE секции сейчас честно
никогда не показываются в живом прогоне -- только headless-тестами на
синтетических кораблях, тот же статус, что и live-непроверенность
items B/C/D), I (individual override внутри группы).

Проверено (режим "fast visible-result" §56.3 всё ещё в силе, полный
симуляционный набор не гонялся):
1. `godot --headless --import` -- чисто, обновлён
   `.godot/global_script_class_cache.cfg` (новые class_name
   `WeaponPanelController`/`WeaponPanel` иначе не подхватываются
   headless-раннером до явного rescan -- это НЕ баг в новом коде, это
   ожидаемый Godot 4 headless workflow при добавлении новых глобальных
   классов; фиксирую здесь на случай, если следующий пасс наступит на
   ту же "Could not find type" ошибку).
2. `test_weapon_panel_controller.gd` (новый) -- ALL TESTS PASSED (36/36).
3. Регресс: `test_weapon_resolution.gd`, `test_missile_launch_order.gd`,
   `test_point_defense.gd`, `test_ship_combat_directive.gd`,
   `test_order_menu_controller.gd` (42/42), `test_move_order_controller.gd`
   (29/29), `test_command_group_controller.gd`, `test_selection_state.gd`,
   `test_tactical_plot_selection.gd`, `test_tactical_plot_projector.gd`,
   `test_individual_orders.gd`, `test_command_transmission.gd`,
   `test_formation.gd`, `test_missile.gd`, `test_subsystem_damage_consumers.gd`
   -- все ALL TESTS PASSED, ни один существующий вызов
   `order_missile_launch`/`_launch_missile_from_tube` не сломан
   (оба новых параметра опциональны).
4. `godot --headless res://scenes/main.tscn --quit-after 120` -- exit 0,
   ровно те же 16x baseline `mesh_get_surface_count`/"Parameter m is
   null" (dummy-рендерер), ноль новых типов ошибок -- новое
   `weapon_panel`/`weapon_panel_controller` wiring в живой демо-сцене
   (где ни missile tubes, ни PD-mounts ещё не выданы -- см. пункт H)
   ничего не сломало.

**НЕ заявляется "§56.3 закрыт"** -- это только пункт E из 9+1 (A/B/C/D
уже были закрыты ранее). Панель реально не тестировалась живым игроком
(headless-окружение не может сгенерировать настоящие мышиные клики), и
MISSILES/POINT DEFENSE секции конкретно не тестировались живьём вообще
никогда, так как текущий демо-сценарий не даёт кораблям ни тюбов, ни
PD -- см. .tools/state.md, тот же урок ещё раз: не объявлять готовым
без живого подтверждения.

## Unreleased -- пересборка Windows .exe (была устаревшей после §56.3 items A-E)

Не через scheduled-задачу, а напрямую в чате (по запросу пользователя
"сделай консольной командой, не трать лимиты"): `.exe` не пересобирался
с 23.09 12:54, хотя за это время в код вошли §56.3 items A-E
(мультивыбор, командные группы, RMB-приказ курса, контекстное меню
атаки, панель оружия) -- пользователь тестировал заведомо устаревший
билд. Пересобран напрямую: `godot --headless --path project --import`
+ `--export-release "Windows Desktop"`, экспорт-шаблоны взяты из
`.tools/godot_templates_cache/extracted2/` (их стандартный путь
установки не персистентен между VM-сессиями scheduled-задачи, сам файл
кэша в проекте -- персистентен). Новый `.pck` вырос с 363КБ до 437КБ --
подтверждение, что новый код реально вошёл в сборку. В AGENTS.md §56.3
добавлено процессное правило: экспорт .exe -- обязательный последний
шаг ЛЮБОГО прохода, трогающего сцены/скрипты/UI, не разовый пункт
чек-листа.

## 2026-09-26 (scheduled dev pass) -- §56.3 item F: command camera quick-center + первый проход по layout панелей

**Camera quick-center (§1.10.1 "быстро центрироваться на выбранном
корабле, группе или контакте"):** новый hotkey Home (`project.godot`
[input] действие `camera_focus_selection`) -- `scripts/camera_focus.gd`
(`CameraFocus.compute`, чистая функция: выбор -> {pivot, spread} по
world.ships/world.missiles, ИСТИННЫЕ позиции, не sensor-контакты --
камера над боем не ограничена fog-of-war) + `scripts/
camera_focus_controller.gd` (`CameraFocusController`, обычный Node с
`_unhandled_input`, тот же паттерн, что PlayerInput). `OrbitCamera.
focus_on()` (новый метод) переносит pivot и пересчитывает distance под
новый spread, СОХРАНЯЯ текущий yaw/pitch (в отличие от `frame_on()`,
который также задаёт начальный угол) -- код дистанции вынесен в общую
`_required_distance_for_spread()`, которую теперь используют оба
метода. `_base_distance` перебазируется на новую дистанцию (чтобы
зум колесом после фокуса считался от нового масштаба, а не от
исходного общебоевого), `far` только РАСТЁТ, никогда не уменьшается
(дальние корабли не должны пропадать из фрустума при приближении).
Приоритет при одновременном выборе своих кораблей и назначенной цели
(designated_target_id) -- своя группа выигрывает, цель используется
для центрирования только если `selected_ids` пуст (см.
ASSUMPTIONS.md "§56.3 item F").

**Layout, первый проход (не общее решение, честно ограничен по
масштабу -- см. ASSUMPTIONS.md):** `Hud.get_bottom_y()` (новый метод,
реальная измеренная высота Label через `get_minimum_size()`) читается
`main.gd._on_tick` каждый тик и передаётся в `CommandGroupPanel.
update(world, selection, hud_bottom_y)`, которая теперь ставит себя на
`hud_bottom_y + TOP_MARGIN_PX` вместо старой захардкоженной константы
`PANEL_POSITION := Vector2(12, 360)`. Это единственная реальная зона
риска перекрытия в сцене (Hud и CommandGroupPanel -- единственные два
панели в одной области экрана, top-left, и высота Hud зависит от
живого числа контактов/подсистем). WeaponPanel (bottom-left, уже сам
считает позицию от viewport), TacticalPlot (top-right, фиксированный
anchor) и OrderMenu (full-viewport только когда открыт) не трогались --
они не пересекаются ни с чем уже сейчас.

**Тесты (headless-only, режим "fast visible-result" из state.md ещё
активен для §56.3 -- полный прогон отложен до закрытия фазы):** новые
`test_camera_focus.gd` (17 проверок, чистая логика центроид/spread/
приоритет), `test_camera_focus_controller.gd` (5 проверок,
try_focus()/_unhandled_input без реальной камеры -- инстанцировать
OrbitCamera вне дерева сцены нельзя, та же причина, что уже
задокументирована в test_orbit_camera.gd, перепроверено вживую
пробным скриптом перед тем, как это писать), 2 новых кейса в
`test_orbit_camera.gd` (вынесенная `_required_distance_for_spread`),
2 новых кейса в `test_hud.gd` (`get_bottom_y()`), новый
`test_command_group_panel.gd` (2 проверки, позиционирование по
`hud_bottom_y`). Плюс регрессионный прогон 17 существующих сьютов
(weapon_resolution, missile_launch_order, point_defense,
ship_combat_directive, order_menu_controller, move_order_controller,
command_group_controller, selection_state, tactical_plot_selection,
tactical_plot_projector, individual_orders, command_transmission,
formation, missile, subsystem_damage_consumers,
weapon_panel_controller, player_input) -- все зелёные, плюс headless
scene smoke run (exit 0, те же 16x baseline dummy-renderer ошибок,
ноль новых типов). Требовался `godot --headless --import` перед
первым тестовым прогоном, чтобы подхватить два новых global
class_name скрипта (CameraFocus, CameraFocusController) -- та же
грабля, что уже отмечена в прошлом проходе (item E).

**Windows-экспорт пересобран этим же проходом** (процессное правило
из AGENTS.md §56.3: экспорт -- обязательный последний шаг любого
прохода, трогающего сцены/скрипты/UI): `.pck` выросло с 437КБ до
450КБ -- подтверждение, что новый код вошёл в билд.

**НЕ заявляется "§56.3 закрыт" и НЕ заявляется "item F закрыт живым
подтверждением"** -- headless-верификация не заменяет реальный тест
игроком (нет реальной мыши/окна в этом окружении, та же оговорка, что
у items B/C/D/E). Пункты G/H/I/J (§56.3) ещё не начаты.

## 2026-09-26 (scheduled dev pass) -- §56.3 item G: zoom-level behaviour (§1.10.3) -- баг-фикс far-плоскости при zoom-out

**Что сделано.** Читал `AGENTS.md` §1.10.3 (ZOOM LEVELS: FLEET/FORMATION/
SHIP/WEAPON-MISSILE VIEW, переход -- только сменой масштаба камеры)
заново, как требует правило "свежее чтение для нового пункта". Проверил
обе половины требования отдельно, не полагаясь на предположения из
state.md:

* **2D тактический плот (мини-карта):** `TacticalPlot.plot_range_m`
  уже авто-масштабируется каждый `update()` под самый дальний ЖИВОЙ
  контакт (`farthest_m * AUTO_SCALE_MARGIN`, без верхнего предела) --
  дальние контакты и так никогда не теряются на плоте. Изменений не
  требовалось.
* **3D-вид (OrbitCamera):** здесь нашёлся РЕАЛЬНЫЙ баг, не просто
  пробел. `far` считалось как `required_distance * 3.0 + 10000.0`, а
  реально достижимая дистанция zoom-out по колесу мыши/клавишам --
  `_base_distance * MAX_DISTANCE_SCALE` (20x, не 3x). На достаточно
  широком начальном кадрировании боя прокрутка колеса до упора
  "наружу" уводила камеру ЗА собственную far-плоскость -- весь 3D-вид
  становился полностью пустым (не "часть дальних контактов потерялась",
  а буквально ничего не рендерится, потому что сама камера уже за
  клиппинг-плоскостью). Баг был незаметен, потому что ни один
  существующий тест не проверял именно эту крайность (только
  yaw/pitch/distance офсет и формулу `_required_distance_for_spread` в
  изоляции).

**Исправление.** Новая чистая функция
`OrbitCamera._required_far_for_base_distance(base_distance)` =
`base_distance * MAX_DISTANCE_SCALE * 1.2 + 10000.0` -- использует ТУ ЖЕ
константу `MAX_DISTANCE_SCALE`, что и сам zoom-clamp, так что они больше
не могут разойтись повторно; запас 1.2x, чтобы камера не оказывалась
ровно на границе. Применена в `frame_on()` (свежий `far`) и в
`focus_on()` (`maxf(far, ...)` -- правило "far только растёт", уже
установленное в item F, теперь на верной формуле).

**Тесты (headless-only, режим fast visible-result всё ещё активен для
§56.3):** 2 новых кейса в `test_orbit_camera.gd` --
`_test_far_always_exceeds_max_zoom_out_distance` (диапазон
base_distance от 500 до 5,000,000 м -- от плотного фокуса на одном
корабле до боя, растянутого на сотни км) и
`_test_far_grows_with_base_distance` (монотonность формулы). Итого
8/8 в этом сьюте. Регрессия: `test_camera_focus.gd` (17),
`test_camera_focus_controller.gd` (5), `test_hud.gd` (14),
`test_command_group_panel.gd` (6) -- все зелёные. Плюс headless scene
smoke run (`main.tscn --quit-after 120`, exit 0, те же 16x baseline
dummy-renderer ошибок, ноль новых типов).

**Windows-экспорт пересобран этим же проходом** (процессное правило):
`.pck` выросло с 450768Б до 451584Б -- подтверждение, что новый код
вошёл в билд.

**НЕ заявляется "§56.3 закрыт"** -- это фикс одного пункта (G) внутри
большого чек-листа; live-подтверждение игроком по-прежнему отсутствует
(нет реальной мыши/окна в этом окружении, та же оговорка, что у всех
предыдущих пунктов §56.3). Также логирован как отдельный, НЕ
устранённый этим проходом нюанс: если корабли расходятся в течение
долгого боя БЕЗ повторного фокуса игрока (Home), `far`/`_base_distance`
остаются фиксированными на значениях первого кадрирования -- это
отдельный, более медленный крайний случай, не тот баг с zoom-clamp,
что исправлен здесь (см. ASSUMPTIONS.md "§56.3 item G" за полным
разбором). Пункты H/I/J (§56.3) ещё не начаты.
