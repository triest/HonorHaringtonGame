# CHANGELOG.md

## Unreleased — Milestone 1: engine skeleton + 3D world + simulation loop

* Выбран стек: Godot 4.3 / GDScript (см. docs/ASSUMPTIONS.md).
* Добавлен `project/` — минимальный Godot 4 проект.
* `simulation/sim_clock.gd` — детерминированный fixed-timestep clock
  (60 Hz, time scale ТЗ §44, pause/single-step).
* `simulation/ship_physics_state.gd` — 3D инерционная физика корабля
  (position/velocity/acceleration/orientation как Vector3/Quaternion,
  thrust→accel→velocity→position, relative velocity, closure velocity,
  clamp скорости ниже c). ТЗ §6, §11-15.
* `simulation/simulation_world.gd` — держит корабли, тикает от SimClock.
* `scripts/ship_view.gd`, `scripts/main.gd` — рендер-слой, 2 корабля-заглушки
  (BoxMesh) на встречных курсах, для проверки связки Simulation/Rendering.
* `simulation/tests/test_ship_physics_state.gd` — headless smoke-тесты
  (thrust, инерция при нулевой тяге, clamp по c, relative velocity/closure).
  **Прогнаны в этой сессии** через Godot v4.3-stable_linux.x86_64:
  `ALL TESTS PASSED`, exit code 0.
* Импорт проекта и запуск `main.tscn` headless проверены без ошибок скрипта.

### Явно НЕ сделано (не путать с готовым, ТЗ §58/§59)

Wedge, sidewalls, оружие, ракеты, контрракеты, point defense, сенсоры, ECM,
damage по подсистемам, tactical AI, формации, command hierarchy, replay,
сценарии, UI, экспорт в .exe. Всё это — следующие Milestone (ТЗ §56).

## Unreleased — реструктуризация документации

* Пользователь добавил в корень проекта CLOUD.md, ASSUMPTIONS.md,
  CANON_RULES.md с расширенной канон-спецификацией (wedge, sidewalls,
  missiles/laserheads, sensors/ECM, damage, formations/command lag).
* Перенёс ARCHITECTURE.md/BUILD.md/CHANGELOG.md из `docs/` в корень —
  структура репозитория теперь точно соответствует AGENTS.md §5.
* Объединил дублирующиеся CANON_RULES.md и ASSUMPTIONS.md (docs/ + корень)
  в единые файлы в корне.
* Сверил через веб-поиск (Honorverse Wiki) два числовых утверждения из
  нового CANON_RULES.md: ускорение ракет ("thousands of gravities" →
  уточнено примером 46,000G/180s для конкретного класса, не универсальная
  константа) и дистанция поражения laserhead ("thousands of km" → уточнено
  25,000 км для конкретного примера). Остальные пункты CANON_RULES.md
  (Krennikov Coupler, механика overload sidewall, ECM Ghost Rider) пока
  НЕ сверены с первоисточником — помечены TODO.
* `docs/` удалена как каталог (дубли слиты в корневые файлы).

## Unreleased — Milestone 3: Impeller Wedge + Sidewalls + ориентация

* Добавлена вторая сверка канона по оставшимся пунктам CANON_RULES.md —
  см. предыдущую запись выше в этом файле и сам CANON_RULES.md.
* `simulation/attack_geometry.gd` — классификация направления атаки
  (6 секторов) относительно ориентации корабля.
* `simulation/ship_defense_state.gd` — раздельная модель wedge/sidewalls
  (не shieldHP), включая канон про уязвимость носовой стенки под острым
  углом и блокировку ускорения при поднятой носовой/кормовой sidewall.
* `ship_physics_state.gd`: интеграция тяги теперь учитывает
  `defense.bow_sidewall_raised/stern_sidewall_raised`.
* `scripts/main.gd`: оба демо-корабля получили `ShipDefenseState`.
* Новые headless-тесты `test_wedge_sidewall.gd` — 8 проверок, все пройдены.
* Все новые численные константы — задокументированные ASSUMPTION-placeholder
  (ASSUMPTIONS.md), не выданы за канон.

### Явно НЕ сделано на этом Milestone

Визуализация wedge/sidewall геометрии в 3D (только логика решения, без
рендер-мешей), оружие/урон от резолва атаки (resolve_attack() пока не
подключён ни к какой системе оружия — это Milestone 4), damage-модель
подсистем, влияющая на condition sidewalls/wedge.

## Unreleased — Milestone 4: Energy Weapons

* `weapon_data.gd`, `weapon_mount.gd`, `hull_state.gd`,
  `weapon_resolution.gd` — первая рабочая цепочка урона: range → cooldown →
  огневой сектор → wedge/sidewall → hull damage.
* 6 новых headless-тестов, все пройдены (Godot v4.3-stable, exit 0).
* Демо-сцена: корабли получили корпус и оружие (не стреляют автоматически).
* Все числовые допущения (дистанция, урон, recharge) зафиксированы как
  PLACEHOLDER в ASSUMPTIONS.md — канон таких чисел не даёт.

### Явно НЕ сделано

Ракеты/laserhead, контрракеты, point defense, сенсоры/ECM (weapon
resolution пока не проверяет обнаружение цели — целится "в слепую"),
полноценный subsystem damage (§25), AI/ручное управление огнём (никто
пока не вызывает `WeaponResolution.fire()` в игровом цикле), визуализация
выстрелов/трассеров.

## Unreleased — Milestone 5: Missiles + Guidance

* Вынес общую кинематику кораблей/ракет в `kinematics_utils.gd`
  (устранил дублирование clamp-по-c и интеграции, которое появилось бы
  при копировании логики `ShipPhysicsState` в `MissileState`).
  `ShipPhysicsState` отрефакторен на использование этого модуля.
* `missile_state.gd` — ракета как полноценная 3D-сущность симуляции
  (§18), с ограниченным по времени горением двигателя (канон-пример
  46 000G/180с из CANON_RULES.md) и переходом в баллистический полёт
  после выгорания.
* `missile_guidance.gd` — предиктивное наведение (lead pursuit по
  target velocity), не прямое "на текущую позицию цели" (§19). Явная
  обработка потери захвата (SensorState.LOST) без "телепортации" наводки.
* 5 новых headless-тестов, все пройдены; полный набор из 4 файлов тестов
  (19+ проверок) — все зелёные после рефакторинга.
* Все числа (терминальная дистанция подрыва, время жизни) —
  задокументированы как PLACEHOLDER в ASSUMPTIONS.md, отделены от
  цитируемого канона (46 000G/180с — реальный пример, не выдумка).

### Явно НЕ сделано

Настоящий подрыв laserhead и урон по цели через `ShipDefenseState`
(missile долетает до TERMINAL, но пока не наносит урон — это следующий
проход), контрракеты, point defense, ECM влияние на sensor_state, пусковые
трубы/боезапас корабля, визуализация полёта ракеты.

## Unreleased — Milestone 5 (продолжение): Laserhead Detonation

* `missile_resolution.gd` — ракета теперь реально наносит урон через
  wedge/sidewall при подрыве (закрыл пробел, отмеченный в предыдущей
  записи этого файла).
* 4 новых теста, все пройдены. Итого 5 файлов тестов / 23 проверки,
  всё зелёное на headless Godot 4.3.

### Явно НЕ сделано

Контрракеты, point defense, влияние ECM на sensor_state ракеты, пусковые
трубы/боезапас корабля (никто пока не запускает MissileState в игровом
цикле — только прямые unit-тесты), точная геометрия lasing rods,
визуализация полёта/подрыва.

## Unreleased — доработка ТЗ: механика laserhead по книгам (по запросу пользователя)

* AGENTS.md §21 (Laserheads) переписан: полная каноническая
  последовательность подрыва (разведение штанг → позиционирование ~100м
  перед боеголовкой → кольцо гравитационных генераторов → гауссова
  фокусировка → независимо наводимые лучи), количество/тип штанг по
  классам (Mark 23 vs Mark 13), эффективный радиус поражения (25 000 км),
  явно перечислено, что остаётся UNKNOWN (точный триггер подрыва, энергия
  луча на штангу, влияние ECM на наведение штанг отдельно от общего
  наведения ракеты).
* CLOUD.md §2.2 дополнен той же механикой + справочные ТТХ реальных
  классов ракет (Mark 23 MDM, Mark 31, Viper, Apollo control missile) —
  как примеры для конкретных классов, не универсальные константы.
* CANON_RULES.md — третья сверка, источники процитированы.
* ASSUMPTIONS.md — зафиксирован ИЗВЕСТНЫЙ разрыв между обновлённым ТЗ и
  текущей реализацией `missile_resolution.gd` (один луч из позиции ракеты
  вместо разведённых штанг) — TODO для следующего прохода по ракетам,
  НЕ исправлено в этом проходе (этот проход — доработка документов по
  прямому запросу, не код).

## Unreleased — реализация: разведение штанг laserhead (закрытие TODO)

* `lasing_rod.gd` (новый), `missile_state.gd` (rod_count/rod_offset_m/
  rod_spread_half_angle_rad), `missile_resolution.gd` (полностью
  переписан: `compute_rod_positions()` + резолюция каждой штанги
  независимо, суммирование урона, `rod_results` в `DetonationResult`).
* 3 новых теста, все пройдены. Полный набор: 5 файлов / 26 проверок,
  все зелёные (headless Godot 4.3, exit 0 везде).
* Закрыт TODO из предыдущей записи ("один луч из позиции ракеты" →
  честное разведение штанг по канону).

### Явно НЕ сделано

Mark-13-style суббоеприпасы с независимым выбором разных целей (нужен
мультитаргетинг ракеты — MissileState пока несёт один target), визуализация
штанг, контрракеты, point defense, влияние ECM на наведение.

## Unreleased — Milestone 6a: Counter-Missiles + доработка ТЗ по физике полёта ракет

* По ссылке пользователя (Honorverse Wiki, Missile) вытащил детали физики
  полёта (трёхфазная модель, дросселирование, MDM-ступени, вращение для
  PD, mass driver для выхода за клин носителя) и поражающих факторов
  (laserhead эффективнее против sidewall, чем kinetic/fusion) — дополнил
  AGENTS.md §18.1 (новый) и §21.25 (новый), CLOUD.md §2.2.1 (новый).
* `counter_missile_resolution.gd` (новый) + рефакторинг `missile_state.gd`
  (`target` untyped, `GuidanceState.INTERCEPTED`) — контрракеты реально
  наводятся на входящие ракеты (переиспользуя MissileGuidance) и
  перехватывают их по каноническому механизму наложения клиньев.
* `missile_state.gd`: `set_throttle()` + `estimated_powered_range_m()` —
  дросселирование двигателя как реальная механика, не только текст в ТЗ.
* 7 новых тестов (5 counter-missile + 2 throttle/range). Полный набор:
  6 файлов тестов, 38 проверок, все зелёные (headless Godot 4.3, exit 0).
* Честно зафиксирован НОВЫЙ известный пробел: sidewall attenuation пока
  не различает тип атакующего оружия (laserhead vs energy vs kinetic) —
  канон явно требует разницы, код пока её не даёт. TODO для будущего
  прохода.

### Явно НЕ сделано

Point defense (лазерные кластеры для перехвата ракет, отдельно от
контрракет), многоступенчатые MDM, вращение ракеты, пусковые трубы,
damage-type-зависимое ослабление sidewall, визуализация.

## Unreleased — закрытие TODO: damage-type sidewall + переход к прототипу/моделям

* Реализован damage-type-зависимый sidewall (см. выше) — закрыт TODO из
  предыдущей записи. Полный набор тестов: 6 файлов / 39 проверок, все
  зелёные.
* Пользователь попросил переключиться на сборку видимого прототипа
  (модели/визуал) — следующая запись этого файла будет про это, ТЗ-доработка
  временно отложена, но не забыта.

## Unreleased — Первый визуальный прототип

* Процедурная модель корпуса ("flattened spindle" по канону, см. CANON_RULES.md
  пятая сверка) вместо BoxMesh-заглушки — `hull_mesh_builder.gd`.
* Визуализация wedge (полупрозрачные плоскости, честно читают
  существующее состояние `ShipDefenseState`).
* Найден и исправлен баг Milestone 1: камера демо-сцены на самом деле
  никогда не смотрела на корабли (обнаружено только сейчас, при первой
  реальной визуальной проверке — headless-тесты этого не ловят).
* Чёрный фон космоса вместо серого по умолчанию.
* Dev-инструменты для скриншотов через Xvfb (screenshot_capture,
  closeup_capture) — оставлены в проекте как debug-утилита.
* Реально сгенерированы и просмотрены 2 скриншота, отправлены
  пользователю.
* Новый тест-файл `test_hull_mesh_builder.gd` (3 проверки). Полный
  набор: 7 файлов тестов, все зелёные.
* Зафиксирована известная нерешённая проблема: нет origin rebasing /
  double precision для больших (боевых, сотни тысяч - миллионы км)
  дистанций — актуально не для этого прототипа, но для реального боя.

### Явно НЕ сделано

Визуализация sidewalls (только wedge), настоящие per-class модели
кораблей (только одна процедурная заглушка на все корабли), текстуры/
детализация корпуса, UI/HUD поверх 3D-вида, origin rebasing для больших
дистанций.

## Unreleased — Milestone: Point Defense

* `point_defense_mount.gd` + `point_defense_resolution.gd` — PD как
  отдельный слой обороны от контрракет (§22), с временем реакции и
  накоплением попаданий (насыщаемая защита — большой залп может
  "перегрузить" один модуль). Переиспользует `GuidanceState.INTERCEPTED`.
* 6 новых тестов, все пройдены. Полный набор: 8 файлов / 47+ проверок,
  все зелёные.
* Честно зафиксировано: выбор цели, влияние сенсоров/ECM/повреждений —
  не реализовано, и ничто пока не вызывает `engage()` в игровом цикле
  (готовая логика без AI-подключения).

## Unreleased — Milestone: Point Defense + улучшение визуализации клина

* Point Defense (§22): `point_defense_mount.gd` + `point_defense_resolution.gd`,
  6 тестов, все зелёные (см. отдельную запись выше по деталям).
* По замечанию пользователя ("клин мне не нравится") — заменил плоский
  PlaneMesh-клин на процедурный V-образный `wedge_mesh_builder.gd`,
  3 новых теста. Полный набор: 9 файлов тестов, 50+ проверок, все зелёные.
* Новый скриншот сгенерирован и отправлен пользователю для подтверждения.

## Unreleased — Milestone: Sensors (§23), первая версия

* Первоисточник найден и учтён: "On Basilisk Station" гл.3 (Baen sample
  chapter mirror) подтверждает механизм гравитационных сенсоров и то,
  что корабль может снизить обнаруживаемость, погасив impellers.
  Записано в CANON_RULES.md "Седьмая сверка" — явно помечено как
  ОПРОВЕРГАЮЩЕЕ более раннюю неподтверждённую заметку ("третья сверка"),
  а не тихо переписывающее её.
* `simulation/contact_state.gd` — общий `ContactState.Type` enum;
  `MissileState.SensorState` теперь алиас на него (не дублирование).
* `simulation/sensor_contact.gd` + `simulation/sensor_resolution.gd` —
  новая система sensor-контактов: state machine DETECTED→TRACKED→
  ESTIMATED (dead-reckoning)→LOST, обнаружение по дальности +
  "испускает ли цель сигнатуру" (клин поднят / двигатель ракеты горит).
  Все пороговые числа — ASSUMPTION, канон цифр не даёт (см.
  ASSUMPTIONS.md).
* 13 новых тестов (`test_sensor_resolution.gd`), все зелёные. Полный
  набор: 10 файлов тестов, 60+ проверок, все зелёные (полный прогон
  подтверждён — ничего не сломано рефакторингом `SensorState`).
* Честно НЕ сделано: подключение к `MissileGuidance`/
  `PointDefenseResolution` (наведение и ПРО по-прежнему видят цель
  идеально — эта система пока изолированная логика, не подключена
  к живому циклу); ECM (§24) — не начато; активные сенсоры/типы
  сенсоров — не реализовано, используется одна пассивно-гравитационная
  модель.

## Unreleased — Подключение сенсоров к самонаведению ракет

* `MissileGuidance.update_target_tracking()` / `resolve_thrust_direction()`
  — ракета теперь МОЖЕТ наводиться по сенсорной оценке цели
  (`SensorContact`, dead-reckoning при потере контакта), а не по истинной
  позиции всегда. Старый путь (`compute_thrust_direction()` напрямую с
  `SensorState.TRACKED`) остаётся рабочим для обратной совместимости —
  ничего не сломано.
* Найден и исправлен баг до попадания в прод: на первом тике до первого
  обнаружения `sensor_state` был бы `UNKNOWN`, но `compute_thrust_direction`
  раньше держал курс только при `LOST` — ракета целилась бы в
  `Vector3.ZERO`. Исправлено: держит курс при любом "неиспользуемом"
  состоянии (UNKNOWN/LOST/UNCERTAIN), не только LOST.
* `MissileState.sensor_state` по умолчанию теперь `UNKNOWN` (было
  `TRACKED`) — честнее отражает "пока не подключено/не обнаружено"; все
  существующие тесты передают `TRACKED` явно и не затронуты.
* 11 новых тестов (`test_missile_guidance_sensors.gd`), все зелёные.
  Полный набор: 11 файлов тестов, 73+ проверок, все зелёные, полный
  прогон подтверждён.
* Честно НЕ сделано: детонация по терминальной дальности в `integrate()`
  всё ещё смотрит на истинную позицию цели, не на сенсорную оценку;
  Point Defense и контрракеты всё ещё не читают `SensorContact`; ECM
  (§24) не начато; ничто из этого пока не вызывается из живого игрового
  цикла — самого игрового цикла (тикающего все корабли/ракеты разом)
  всё ещё не существует, это следующий необходимый шаг для того, чтобы
  всё написанное реально управляло ходом боя, а не было изолированной,
  пусть и протестированной, логикой.

## Unreleased — Подключение сенсоров к Point Defense

* `PointDefenseResolution.engage()` — новый опциональный параметр
  `sensor_contact`: если передан, ПРО может стрелять только пока контакт
  DETECTED/TRACKED/ESTIMATED (новый `Outcome.NOT_DETECTED` иначе) —
  "нельзя стрелять по тому, чего не видишь". `null` (по умолчанию) —
  старое поведение без сенсорной привязки, обратная совместимость
  сохранена для всех существующих вызовов/тестов.
* 5 новых тестов (`test_point_defense_sensors.gd`), все зелёные. Полный
  набор: 12 файлов тестов, 78+ проверок, все зелёные, полный прогон
  подтверждён без регрессий.
* Честно НЕ сделано: контрракеты (`CounterMissileResolution`) всё ещё не
  сенсорно-завязаны; ECM (§24) не начато; ничто из сенсорной системы
  пока не вызывается из живого игрового цикла — самого игрового цикла
  ещё нет.

## Unreleased — Milestone 7 закрыт: ECM (§24)

* `ecm_state.gd` — данные: глушение (вкл/выкл + множитель дальности),
  ограниченный список decoy-позиций (канон: "only a handful... at a
  time" — реализовано как MAX_DECOYS=4).
* `sensor_resolution.gd` — глушение сокращает эффективную дальность
  обнаружения; decoy подменяет позицию цели, если ближе к наблюдателю
  (детерминированно, без RNG). Источник: Honorverse Wiki "Electronic
  warfare" (вторичный, книжный первоисточник для ECM не найден в этот
  проход — честно зафиксировано).
* 9 новых тестов (`test_ecm.gd`), все зелёные. Полный набор: 13 файлов
  тестов, 87+ проверок, все зелёные, регрессий нет.
* Milestone 7 ("Sensors + ECM", §56) закрыт на уровне протестированной
  логики. Честно НЕ сделано: decoy — не физические летящие дроны
  (только позиции, задаваемые вручную); ECM-поле не встроено по
  умолчанию в ShipPhysicsState; PD/контрракеты получают эффект ECM
  косвенно через SensorContact, отдельного API "заглушить ПРО" нет.
  Следующий логический шаг по §56 — Milestone 8 (Subsystem Damage,
  §25) либо, что более критично для игры в целом, первый настоящий
  живой игровой цикл, тикающий корабли/ракеты/ПРО/сенсоры вместе —
  пока вся протестированная логика существует изолированно.

## Unreleased — Milestone 8 начат: Subsystem Damage (§25)

* `subsystem_type.gd` + `subsystem_state.gd` + `ship_subsystems.gd` —
  контейнер из 11 подсистем ТЗ §25, каждая с condition 0..1.
* Первый реальный потребитель: `SensorResolution` (наблюдатель) читает
  свою `SENSORS` подсистему — повреждённые сенсоры сокращают
  эффективную дальность обнаружения, полностью выбитые блокируют
  обнаружение совсем. Прямая реализация примера из самого ТЗ.
* 33 новых теста (28 + 5), все зелёные. Полный набор: 15 файлов
  тестов, 120+ проверок, все зелёные, полный прогон без регрессий.
* Честно НЕ сделано: 10 из 11 подсистем есть, но не подключены ни к
  одному потребителю; нет логики распределения входящего урона по
  подсистемам; старые отдельные condition-поля (propulsion/weapon/PD/
  sidewall) не унифицированы с этим контейнером. Milestone 8 открыт, не
  закрыт.

## Unreleased — Milestone 8 продвинут: распределение урона по подсистемам

* `subsystem_damage_resolution.gd` — детерминированная (без RNG)
  секторная карта, решающая какая подсистема получает урон при
  попадании; STRUCTURAL_INTEGRITY получает долю от любого хита.
  Секторная карта — инженерная догадка, честно помечена как НЕ канон.
* `WeaponResolution.fire()` / `MissileResolution.resolve_detonation()`
  — новый опциональный параметр `target_subsystems`, обратная
  совместимость сохранена (по умолчанию `null`, старое поведение).
* 13 новых тестов, все зелёные. Полный набор: 16 файлов тестов, 130+
  проверок, все зелёные, полный прогон подтверждён без регрессий.
* Честно НЕ сделано: только SENSORS реально подключён к потребителю
  (SensorResolution); остальные 10 подсистем копят урон, но ничего его
  не читает; старые отдельные condition-поля не унифицированы с этим
  контейнером.

## Unreleased — Milestone 8 продвинут: PROPULSION/MANEUVERING → ускорение

* `ShipPhysicsState.subsystems` (опциональный `ShipSubsystems`) —
  `effective_max_acceleration()` теперь домножается на condition
  PROPULSION и MANEUVERING, если контейнер задан. Складывается со
  старыми полями `propulsion_condition`/`compensator_condition`, не
  заменяет их. Прямая реализация примера ТЗ §25.
* 6 новых тестов, все зелёные. Полный набор: 17 файлов тестов, 136+
  проверок, все зелёные, полный прогон подтверждён без регрессий.
* Статус Milestone 8: 3 из 11 подсистем (SENSORS, PROPULSION,
  MANEUVERING) реально влияют на поведение; урон распределяется по
  всем 11, но 8 из них пока без потребителя. Следующий логичный шаг по
  §56 — либо докрутить оставшиеся подсистемы (WEAPONS/POINT_DEFENSE →
  их существующие condition-поля; POWER/STRUCTURAL_INTEGRITY → пока
  не к чему подключать без более общей модели), либо переходить к
  Milestone 9 (Tactical AI, §26), которая уже назрела: до сих пор нет
  ни одного живого игрового цикла, который сам вызывал бы всю эту
  протестированную логику подряд, тик за тиком.

## Unreleased — Первый живой игровой цикл (Milestone 1 закрыт по существу)

* `simulation_world.gd` переписан: `tick_simulation(dt)` теперь реально
  тикает сенсоры, ракеты (наведение+полёт+детонация), контрракеты, ПРО
  и физику кораблей ВМЕСТЕ, каждый тик, а не только физику кораблей
  как раньше. Это первое место, где весь массив ранее написанных
  Milestones 2-8 работает совместно, а не только по отдельности в
  тестах.
* Честно ограничено: выбор цели ПРО — плейсхолдер ("ближайшая ракета,
  нацеленная на этот корабль"), не тактический AI (§26). Стрельба
  корабль-корабль не автоматическая (`fire_weapon()` — явный вызов).
* 8 новых сквозных тестов, все зелёные. Полный набор: 18 файлов
  тестов, 144+ проверок, все зелёные, полный прогон подтверждён без
  регрессий.
* Следующий логичный шаг по §56 — Milestone 9 (Tactical AI, §26): пока
  ничего не выбирает цели для оружия/ПРО осмысленно, только этот
  ближайший-по-дистанции плейсхолдер для ПРО.

## Unreleased — Milestone 9 начат: Tactical AI (§26), первая версия

* Новый модуль `tactical_ai.gd` (`TacticalAI`, статические функции):
  `select_pd_target(ship, contacts)` и
  `select_weapon_target(ship, contacts, hostile_ship_ids)`. Оба читают
  ТОЛЬКО `SensorContact` (estimated_position, state) — никогда истинную
  позицию/идентичность цели напрямую. Реализация "no cheat vision"
  (§26).
* Это одновременно ИСПРАВЛЯЕТ реальный баг: старый плейсхолдер ПРО в
  `simulation_world.gd` сканировал `missiles` по истинной позиции/
  идентичности — сам был нарушением §26, хоть и не назывался тактическим
  AI.
* `simulation_world.gd`: добавлена минимальная модель принадлежности —
  `teams: Dictionary`, `set_team()`, `is_hostile()` (оба корабля должны
  иметь непустую, РАЗНУЮ команду — иначе нейтрален, никогда не выбирается
  автоматически целью). `_resolve_point_defense` переписан на
  `TacticalAI.select_pd_target`. Новый `_resolve_weapons_ai(dt)`: каждый
  корабль с оружейными установками и назначенной командой сам выбирает
  ближайший пригодный вражеский контакт и стреляет всеми установками —
  через существующий `fire_weapon()`. `tick_simulation` теперь вызывает
  `_resolve_weapons_ai` после ПРО и перед физикой.
* Новый тестовый файл `test_tactical_ai.gd` (8 проверок) на сам модуль
  выбора цели изолированно. `test_simulation_world.gd` расширен двумя
  сквозными тестами: AI стреляет по вражескому кораблю, но не по
  ближайшему нейтральному без команды; AI не стреляет вообще, если
  команда не назначена ни одному кораблю.
* Полный набор: 19 файлов тестов, 155+ проверок, все зелёные, полный
  прогон подтверждён без регрессий.
* Честно ограничено (открыто для §26 дальше): нет взвешивания угрозы
  кроме "ближайший пригодный контакт", нет управления построением, нет
  выбора по манёвру/дистанции отдельно от ближайшего, нет решений о
  запуске ракет (только стрельба существующим оружием по существующим
  установкам), нет реакции на повреждения/потери, нет отступления.

## Unreleased — Milestone 9 продвинут: "respond to damage" / "retreat" / "disengage" (§26)

* `TacticalAI.is_critically_damaged()` + `select_retreat_vector_world()`
  (sensor-contact-only, no cheat vision) добавлены.
* `simulation_world.gd`: `_resolve_damage_response(dt)` заставляет
  критически повреждённый (≤30% целостности корпуса, ASSUMPTION-порог)
  корабль отступать от известных враждебных контактов;
  `_resolve_weapons_ai` перестаёт стрелять для такого корабля же тем же
  критерием.
* `test_tactical_ai_retreat.gd` — 13 новых проверок.
* Полный набор: 20 файлов тестов, 168+ проверок, все зелёные, без
  регрессий.

## Unreleased — Milestone 9 продвинут: "launch missiles" (§26)

* `missile_tube.gd` (`MissileTube`): ammo/cooldown/дальность пусковой
  установки, ASSUMPTION-плейсхолдеры.
* `simulation_world.gd`: `missile_tubes`, `add_missile_tube()`,
  `_resolve_missile_launch_ai(dt)` + `_launch_missile_from_tube()` — AI
  реально решает запускать ракеты по враждебным целям в пределах
  дальности трубы, тем же sensor-contact-only критерием, что и оружие;
  не запускает при отсутствии команды/враждебных/при отступлении.
* `test_missile_launch_ai.gd` — 15 новых проверок.
* Полный набор: 21 файл тестов, 183+ проверки, все зелёные, без
  регрессий.

## Unreleased — ТЗ дополнено: AGENTS.md §61 "Combat Philosophy — 'Nelson in a Skirt'"

* Из второго `Honorverse_Docs.zip` (примечания пользователя по боям) в
  AGENTS.md добавлена новая §61 — формальный design mandate: decisive
  battle mindset, geometry-as-weather-gauge, wall-of-battle formation
  fighting, concentration of force, calculated risk under incomplete
  information, commander-not-pilot, initiative within the plan.
* Краткий кросс-референс добавлен в CLOUD.md §2.7.
* ASSUMPTIONS.md: прежняя неформальная заметка про Nelson/Age-of-Sail
  обновлена — статус повышен до "раздел ТЗ" (было устное указание
  пользователя, теперь формальный §61).
* Архив `Honorverse_Docs.zip` и распакованная `Honorverse_Docs/`
  удалены из папки проекта после переноса содержимого — не часть
  исходников (см. CANON_RULES.md "Десятая сверка").

## Unreleased — Milestone 10 начат: Formation Command, "wall of battle" (§61)

* `formation_state.gd` (`FormationState`): ведущий + member_offsets в
  локальных координатах ведущего.
* `simulation_world.gd`: `formations`, `add_formation()`,
  `_resolve_formation_keeping(dt)` — станция-киппинг с мёртвой зоной,
  безопасный no-op без ведущего, disengage-отступление перекрывает
  удержание строя.
* `test_formation.gd` — 8 новых проверок.
* Полный набор: 22 файла тестов, 191+ проверка, все зелёные, без
  регрессий.

## Unreleased — ТЗ дополнено: AGENTS.md §62 "Inertia, Ship Mass, and Time Scale"

* Новая секция AGENTS.md §62: масса корпуса по классам (CANON,
  Honorverse Wiki), механика инерциального компенсатора и доктрины
  запаса безопасности (CANON), ускорения курьерских катеров до ~800G
  (CANON, остальные классы — UNKNOWN), разграничение тика симуляции и
  презентационного time control (§44), явная граница объёма проекта
  (тактическое реальное время, не гипер-транзит).
* Краткий кросс-референс добавлен в CLOUD.md §2.8.
* CANON_RULES.md: "Одиннадцатая сверка" — источники и цитаты.
* ASSUMPTIONS.md: честно зафиксирован текущий разрыв — `mass_kg` не
  подключён к формуле ускорения (F=ma), это ТОЛЬКО декларация ТЗ, не
  реализация; известный дефолт 500G НЕ привязан к конкретному классу.
* Это документационное обновление ТЗ, без изменений в коде/тестах.

## Unreleased — Milestone 10 продвинут: matching скорости в удержании строя (§29/§32)

* `simulation_world.gd` `_resolve_formation_keeping(dt)`: заменена
  прежняя "тяга-только-по-направлению" (bang-bang к желаемой позиции) на
  PD-регулятор (ошибка позиции + ошибка скорости относительно ведущего),
  закрывающий честно заявленный пробел прошлого прохода ("нет
  затухания/матчинга скорости — член может проскакивать станцию и
  колебаться"). Мёртвая зона теперь проверяет ОБА условия (позиция И
  скорость) — член на станции, но с рассинхронизированной скоростью,
  больше не остаётся без тяги. ASSUMPTION: коэффициенты K_P_STATION=0.02/
  K_D_STATION=0.9 — инженерный выбор (сильно передемпфированный ПД-
  регулятор), не канон; см. ASSUMPTIONS.md. Честно НЕ сделано (не
  входило в этот проход, открыто дальше): угловая скорость ведущего не
  учитывается при развороте офсета (только линейная скорость); резервный
  ведущий/перестроение при потере ведущего (§33) по-прежнему отсутствует.
* `test_formation.gd` — 2 новых теста (4 проверки): член на станции, но
  с ненулевой относительной скоростью, получает тормозящую тягу вместо
  нуля; член далеко от станции сходится к ней за много тиков под новым
  законом управления.
* Полный набор: 22 файла тестов, все зелёные, полный прогон подтверждён
  без регрессий (включая уже существующие 5 тестов `test_formation.gd`
  из первого слайса Milestone 10, которые остаются валидны без
  изменений под новым законом управления).
* ПРИМЕЧАНИЕ О ПАРАЛЛЕЛЬНОЙ РАБОТЕ: в начале этого прохода на диске уже
  был обнаружен новый коммит "Milestone 10 started: Formation Command
  first slice" (95daf7e) и, позже в том же проходе, ещё один
  ("Spec update: AGENTS.md §62...", 9fade27) — оба явно от другого
  параллельно выполняющегося запуска этой же автономной задачи над той
  же папкой проекта. Собственная первая попытка этого прохода (отдельные
  `formation.gd`/`formation_ai.gd`/`test_formation_ai.gd` с похожей, но
  independently-разработанной архитектурой) была отменена (не
  закоммичена, файлы перемещены в `_to_delete/`, не отслеживается git),
  чтобы не дублировать/не конфликтовать с уже принятой в git реализацией
  `formation_state.gd`. Дальнейшая работа в этом проходе построена НА
  ВЕРХУ уже принятой архитектуры, а не заново. Пользователю стоит знать:
  похоже, эта scheduled-задача может запускаться параллельно более чем
  одним экземпляром на одну и ту же папку — возможный источник будущих
  конфликтов git/файлов, стоит проверить настройку расписания.

## Unreleased — Milestone 10 продвинут: резервный ведущий / перестроение при потере ведущего (§33)

* Закрыт открытый пробел предыдущей записи ("резервный ведущий/
  перестроение... по-прежнему НЕ реализовано"). `FormationState` получил
  `succession_order`/`guide_lost_since`; `TacticalAI.is_guide_lost()` +
  `TacticalAI.select_formation_successor()` (новые статические функции,
  читают только истинное состояние `ships`/`hulls` — это своя-к-своим
  бухгалтерия, не "no cheat vision" §26 обнаружение врага); `simulation_world.gd`
  получил `COMMAND_TRANSFER_DELAY_S` (ASSUMPTION, 3с — честно НЕ то же
  самое, что каноническая скорость-света command lag из CANON_RULES.md
  §7, актуальная для флотских дистанций в миллионы км, а не для текущих
  тактических дистанций) + `world_sim_time` (накапливается вызовами
  `tick_simulation`, не зависит от `SceneTree`/`clock`) + новую
  `_transfer_formation_command()`, которая реально переносит команду
  преемнику и переcчитывает офсеты членов строя относительно НОВОГО
  ведущего (заморозка текущего относительного положения, а не
  переиспользование офсетов старого ведущего).
* 5 новых тестов в `test_formation.gd` (~14 проверок): регистрация
  цепочки команд; уничтоженный ведущий → передача первому в цепочке
  ПОСЛЕ задержки распознавания (не мгновенно); недееспособный ведущий +
  недееспособный первый преемник → пропуск ко второму, старый ведущий
  остаётся рядовым членом строя; fallback на порядок вставки Dictionary,
  когда цепочка команд не задана явно; полное отсутствие годного
  преемника → строй безопасно остаётся без ведущего, без падения
  симуляции и без "магической" выдумки преемника (§33: "Do not
  magically transfer information unavailable to subordinate ships").
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново, т.к. на машине не было
  сохранённого бинарника): все 22 файла `project/simulation/tests/`
  зелёные (exit code 0 каждый), включая обновлённый `test_formation.gd`
  (теперь 27 проверок в этом файле, было 12). Регрессий не найдено.
* Честно НЕ сделано (открыто дальше по Milestone 10): передача команды
  не переиздаёт формационные ПРИКАЗЫ §29 — новая геометрия строя вокруг
  нового ведущего не переpланируется, member-офсеты просто заморожены
  там, где корабли физически были в момент передачи; угловая скорость
  ведущего по-прежнему не учитывается формационным PD-регулятором;
  многоуровневая иерархия команд (element/division/squadron/task
  force/fleet, §28) не реализована; §33 step 4 "account for
  communication limitations" реализован как инженерная задержка
  распознавания (ASSUMPTION), а НЕ как реальная модель задержки по
  скорости света в зависимости от дистанции между кораблями — при
  текущих тактических (не флотских) дистанциях проекта разница
  ничтожна, честно зафиксировано, а не тихо выдано за один и тот же
  механизм.

## Unreleased — Milestone 10 gap closed: 4 of 8 subsystems without a damage consumer now wired (§25)

* `simulation_world.gd`: новая `_sync_subsystem_driven_conditions()`,
  вызывается первым шагом каждого тика. Синхронизирует
  `WeaponMount.condition` / `PointDefenseMount.condition` /
  `MissileTube.condition` (новое поле) из собственных подсистем
  корабля-носителя (`WEAPONS`/`POINT_DEFENSE`/`MISSILE_SYSTEMS`) — это
  прямая реализация того, что уже было заложено в комментариях этих
  полей ("damaged by §25 later" / "not yet modeled here"), просто
  никогда не подключено. Корабль без `ShipSubsystems` (subsystems ==
  null) не затрагивается — полная обратная совместимость.
* `weapon_resolution.gd`/`point_defense_resolution.gd` уже читали
  `mount.condition` — теперь оно реально живое, а не всегда 1.0:
  повреждённая WEAPONS пропорционально снижает урон выстрела
  (`damage_per_hit * mount.condition`), полностью уничтоженная
  обнуляет `is_ready()`; POINT_DEFENSE при `condition <= 0` отключает
  `is_ready()` (честно задокументировано как hard on/off, не
  непрерывная деградация — см. обновлённый class doc
  point_defense_resolution.gd).
* `missile_tube.gd`: новое поле `condition` + `is_ready()` теперь также
  требует `condition > 0.0` (MISSILE_SYSTEMS подключена).
* `counter_missile_resolution.gd`: `check_intercept()` получил новый
  опциональный параметр `counter_missile_system_condition` (по
  умолчанию 1.0, полностью обратная совместимость с каждым
  существующим вызовом) — масштабирует эффективный радиус
  wedge-overlap перехвата (`INTERCEPT_KILL_RADIUS_M * condition`).
  INTERPRETATION задокументирована в class doc: представляет
  бортовой fire-control launching-корабля, обеспечивающий терминальные
  поправки наведения уже выпущенному counter-missile (не сам полёт
  ракеты — тот управляется независимо через MissileGuidance).
  `simulation_world.gd`'s `_resolve_counter_missile_intercepts()`
  находит ВЛАДЕЮЩИЙ counter-missile корабль через `missile_owners` и
  подставляет его COUNTER_MISSILE_SYSTEMS condition; безхозный
  counter-missile (owner_id == "") или несуществующий владелец —
  честный fallback на 1.0 (пред-§25 поведение).
* `ship_subsystems.gd`: HONEST STATUS docstring обновлён — было 3 из 11
  реально подключённых consumer'ов (SENSORS/PROPULSION/MANEUVERING),
  стало 7 из 11. Честно НЕ подключены (заблокированы отсутствием более
  крупных систем, не просто "забыли"): COMMUNICATIONS (нет
  command/reporting §26/§28), POWER (нет power-budget модели),
  STRUCTURAL_INTEGRITY (HullState — временный §58 placeholder,
  требующий полной замены перед §59 DoD, подключать поверх него
  бессмысленно), DEFENSIVE_SYSTEMS (нет канонической разницы с уже
  существующими wedge/sidewall системами ShipDefenseState — что именно
  эта подсистема должна отдельно моделировать, UNKNOWN).
* **РЕАЛЬНЫЙ БАГ НАЙДЕН И ИСПРАВЛЕН** при написании нового теста для
  этого прохода (не гипотетический — падал с реальным SCRIPT ERROR):
  counter-missile (у которого `target` — другой `MissileState`, не
  корабль) вооружает боеголовку той же самой generic
  дистанционной проверкой, что и обычная ракета
  (`MissileState.integrate()` не различает тип цели). До исправления
  `simulation_world.gd`'s `_update_missiles(dt)` затем безусловно
  вызывал `MissileResolution.resolve_detonation()`, который читает
  `target.defense` — поля, которого нет на `MissileState` (оно есть
  только на `ShipPhysicsState`) — падение "Invalid access to property
  or key 'defense'". Баг был скрытым: ни один существующий тест
  counter-missile не проходит через `SimulationWorld.tick_simulation()`
  (все дергают `integrate()`/`check_intercept()` напрямую). Исправлено:
  `_update_missiles` теперь явно исключает laserhead-детонацию для
  ракет, чья цель сама является `MissileState` — counter-missile
  разрешается ИСКЛЮЧИТЕЛЬНО через `_resolve_counter_missile_intercepts()`
  (wedge-overlap), никогда через §21 laserhead/rod-детонацию против
  другой ракеты, что и так уже соответствовало документированной
  канонической механике в `counter_missile_resolution.gd`.
* Новый `test_subsystem_damage_consumers.gd` — 8 новых тестов (13
  проверок): синхронизация condition для всех 3 mount/tube типов;
  реальное снижение урона выстрела; полное отключение при
  `condition == 0`; корабль без ShipSubsystems не затрагивается;
  масштабирование радиуса перехвата counter-missile + обратная
  совместимость параметра по умолчанию; сквозной сценарий через
  `world.tick_simulation()`, который и поймал баг выше.
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново): все 23 файла
  `project/simulation/tests/` зелёные (exit code 0 каждый), включая
  новый файл. Регрессий не найдено.
* Честно НЕ сделано (открыто дальше): POINT_DEFENSE/MISSILE_SYSTEMS —
  только hard on/off, не непрерывная деградация точности/скорострельности
  как у WEAPONS; COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
  DEFENSIVE_SYSTEMS по-прежнему без consumer'а (см. выше, честные
  архитектурные блокеры); переданное командование формации по-прежнему
  не переpланирует §29 приказы под нового ведущего; угловая скорость
  ведущего по-прежнему не учитывается PD-регулятором удержания строя;
  многоуровневая иерархия команд §28 не реализована; Milestone 11+ не
  начаты.

## Unreleased — Milestone 10: угловая скорость ведущего учтена в station-keeping (§29/§32)

* Закрыт открытый честный пробел ("угловая скорость ведущего по-прежнему
  не учитывается PD-регулятором удержания строя"), заявленный в
  нескольких предыдущих записях подряд. `simulation_world.gd`'s
  `_resolve_formation_keeping()`: член формации теперь матчит скорость
  своей СОБСТВЕННОЙ станционной точки (`guide.velocity +
  guide.angular_velocity.cross(offset_world)`, стандартная формула
  твёрдого тела v = v_cg + ω×r), а не только линейную скорость ведущего
  как раньше. До этого прохода член на широком офсете от разворачивающегося
  на месте ведущего либо получал нулевую тягу (если сам стоял на месте —
  ложное "на станции"), либо получал паразитную тягу против собственного
  корректного движения (если уже отслеживал разворот) — оба случая были
  реальными багами поведения, не просто отсутствием фичи.
* 2 новых теста в `test_formation.gd` (было 28 проверок в файле по
  предыдущей записи, стало 30): `_test_rotating_guide_sweeps_station_and_member_gets_matching_thrust`
  (неподвижный на офсете член ДОЛЖЕН получить тягу, когда ведущий
  разворачивается на месте — иначе строй "рвётся" на поворотах) и
  `_test_member_already_tracking_guide_rotation_gets_no_spurious_thrust`
  (член, уже двигающийся с ω×r для своего офсета, НЕ должен получать
  паразитную тягу — старая формула её бы дала).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново, на машине не было
  сохранённого бинарника): все 23 файла `project/simulation/tests/`
  зелёные (exit code 0 каждый). Регрессий не найдено.
* Честно НЕ сделано (не входило в scope этого прохода, остаётся открытым):
  передача командования формации по-прежнему не переpланирует §29
  приказы под нового ведущего (member-офсеты просто заморожены);
  многоуровневая иерархия команд §28 не реализована; POINT_DEFENSE/
  MISSILE_SYSTEMS condition по-прежнему hard on/off, не непрерывная
  деградация; COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
  DEFENSIVE_SYSTEMS subsystem damage по-прежнему без consumer'а (см.
  предыдущие записи за архитектурные блокеры); Milestone 11+ не начаты.


## 2026-09-18 (проход: continuous degradation для POINT_DEFENSE/MISSILE_SYSTEMS, §25)

Закрыт честно залогированный пробел: PointDefenseMount и MissileTube
раньше деградировали от урона подсистемы ТОЛЬКО как hard on/off
(`condition > 0.0` gate в `is_ready()`), в отличие от WEAPONS, где урон
выстрела уже масштабировался непрерывно через `mount.condition`.

* `point_defense_mount.gd`: три новых геттера —
  `effective_reaction_time_s()` / `effective_recharge_time_s()` (обе
  делятся на `condition`, floor 0.05 — время реакции/перезарядки растёт
  по мере повреждения) и `effective_engagement_range_m()` (умножается на
  `condition`, без floor — эффективная дальность честно падает до 0).
  Модель деградации ВРЕМЕНИ через деление, а не КАКОГО-ТО ДРУГОГО
  штрафа — INTERPRETATION, не канон (нет источника про то, КАК именно PD
  деградирует от частичного урона). Дальность масштабируется линейно —
  та же модель, что уже использована для `ECMState.jamming_range_multiplier`
  и `CounterMissileResolution.check_intercept()`'s condition-scaled
  intercept radius (согласованность с прошлым проходом, не новая с нуля
  придумка).
* `point_defense_resolution.gd`: `engage()` теперь читает эти effective_*
  геттеры вместо сырых `reaction_time_s`/`recharge_time_s`/
  `max_engagement_range_m` — при `condition == 1.0` поведение бит-в-бит
  идентично предыдущему (геттеры дают тождество), полная обратная
  совместимость с каждым существующим тестом/вызовом.
* `missile_tube.gd`: тот же паттерн — `effective_reload_time_s()` (делится
  на condition) и `effective_max_range_m()` (умножается). `is_ready()`
  теперь сравнивает с `effective_reload_time_s()` вместо сырого
  `reload_time_s`.
* `simulation_world.gd`: `_resolve_missile_launch_ai()`'s дальнобойная
  проверка (`distance > tube.max_range_m`) переключена на
  `tube.effective_max_range_m()`.
* `ship_subsystems.gd`: HONEST STATUS docstring обновлён — POINT_DEFENSE/
  MISSILE_SYSTEMS больше не описываются как "только hard on/off";
  документирован новый continuous-falloff путь.
* Новый `test_pd_missile_continuous_degradation.gd` — 24 проверки:
  тождество геттеров при `condition == 1.0` (обратная совместимость),
  корректное масштабирование при `condition == 0.5`/`0.25`, floor-защита
  от деления на ноль при `condition == 0.0`, реальное удлинение времени
  захвата цели и перезарядки PD, реальное сужение дальности PD и tube,
  respect эффективного reload_time в `MissileTube.is_ready()`, и один
  сквозной тест через `SimulationWorld.tick_simulation()` (тот же
  урок с прошлого прохода — стыковые баги ловит только сквозной тест).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН: все 24 файла
  `project/simulation/tests/` зелёные (exit code 0), включая новый файл
  и оставшиеся 23 (Godot v4.3-stable_linux.x86_64, скачан заново в этом
  проходе — кэш от прошлого прохода в `$HOME` не сохранился между
  запусками VM). Регрессий не найдено, в т.ч. от параллельного прохода
  (см. ниже), чьи изменения (station-keeping угловая скорость) уже были
  в рабочем дереве до начала тестирования этого прохода.

Честно НЕ сделано (открыто дальше): ECM по-прежнему не деградирует
точность/трекинг PD (отдельный, не связанный с §25 пробел); §25
consumer'ов для COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
DEFENSIVE_SYSTEMS по-прежнему нет (архитектурно заблокированы, см. выше
и ASSUMPTIONS.md); переданное командование формации по-прежнему не
перепланирует §29 приказы под нового ведущего; многоуровневая иерархия
команд §28 не реализована; Milestone 11+ не начаты.

## ОБНАРУЖЕНА ПАРАЛЛЕЛЬНАЯ РАБОТА (четвёртый проход подряд с этим наблюдением)

В начале этого прохода `git log` показал 2 новых коммита (angular
velocity в station-keeping + очередная заметка о параллелизме),
которых не было в момент чтения СТАТУС-блока промпта, сделанные другой
сессией (`session_01GYmrSzDqT83f7u9ymfnxXe`) — при этом `list_triggers`
на Honorverse-задачу показывает РОВНО ОДИН зарегистрированный trigger
(`trig_01MR2jCcsytL2gPbDvhVgkG7`), чей `last_run.session_id` совпадает
с ЭТОЙ сессией, а не с той. Заметная деталь: у trigger'а `updated_at`
(20:14:49) отстаёт от его же `last_fired_at` (20:14:53) всего на 4
секунды — то есть предыдущий проход, судя по всему, вызвал
`update_trigger` практически в момент своего же повторного срабатывания,
хотя cron `59 */3 * * *` (минута 59, не 0) не должен подпадать под
описанное в документации инструмента поведение "anchor to now" (то
относится только к `0 * * * *`-подобным часовым паттернам). Причина
остаётся не выясненной до конца этой сессией — либо пользователь сам
вручную перезапускает задачу (`fire_trigger`) вскоре после завершения
предыдущего прохода, либо где-то раньше временно стоял более частый
cron и сейчас доигрывают уже запущенные с того момента долгие сессии.
Практического вреда в этом проходе не было (общий git-репозиторий на
диске, оба прохода работали над РАЗНЫМИ файлами/пробелами, конфликтов
слияния не было — не настоящий concurrent write, просто две сессии
активны в одном временном окне). Открытый вопрос пользователю ОСТАЁТСЯ:
стоит проверить, действительно ли что-то запускает эту задачу чаще, чем
раз в 3 часа.


## 2026-09-18 (проход: §29 Formation Orders — replan вместо freeze при передаче командования)

Закрыт честно залогированный пробел, упомянутый в нескольких предыдущих
записях подряд: "переданное командование формации не перепланирует §29
приказы под нового ведущего (member-офсеты просто заморожены)".

* `formation_state.gd`: новое поле `design_offsets: Dictionary` —
  ОРИГИНАЛЬНАЯ спланированная геометрия строя (ship_id -> Vector3 offset
  в собственной вращающейся системе координат строя), захватывается
  лениво один раз при ПЕРВОЙ передаче командования и больше не
  перезаписывается автоматически. В отличие от `member_offsets`
  (которое перезаписывается при КАЖДОЙ передаче, отражая, кто сейчас
  где стоит), `design_offsets` хранит неизменную "форму" строя.
* `simulation_world.gd`'s `_transfer_formation_command()`: теперь
  вычисляет новый offset выжившего корабля как
  `design_offsets[ship] - design_offsets[new_guide]` — та же
  относительная геометрия, что и всегда, просто пересчитанная
  относительно нового ведущего (строй "смыкается" вокруг преемника на
  исходном интервале), вместо замораживания фактической позиции
  корабля на момент передачи (старое поведение).
* Честный fallback: корабль без design-записи (присоединился к строю
  ПОСЛЕ того, как design уже был захвачен, например подкрепление) не
  имеет спланированной геометрии — для него (и для всех перевозимых в
  ЭТОЙ передаче кораблей, т.к. `can_replan` проверяется по наличию
  НОВОГО ведущего в design, не по каждому кораблю отдельно) сохраняется
  старое поведение "заморозить фактическую позицию". Результат такого
  fallback-прохода становится НОВОЙ design-базой, чтобы СЛЕДУЮЩАЯ
  передача снова могла перепланировать геометрически, а не повторяла
  fallback бесконечно.
* INTERPRETATION, не канон: нет источника Honorverse, описывающего, КАК
  именно перестраивается "стена" при потере ведущего — "сохранить
  спланированную относительную геометрию, сомкнутую вокруг нового
  ведущего" выбрано как наиболее естественное прочтение доктрины
  formation-keeping (строй — это ФОРМА, а не набор абсолютных позиций),
  не числовое допущение. См. ASSUMPTIONS.md.
* 3 новых/переписанных теста в `test_formation.gd` (было 34 проверки в
  файле, стало 40): `_test_transferred_command_replans_from_design_not_frozen_actual_position`
  (заменяет старый `_test_transferred_command_freezes_member_station_relative_to_new_guide`
  — доказывает, что offset теперь берётся из design, а НЕ из
  фактической/устаревшей позиции), `_test_transfer_falls_back_to_freeze_when_new_guide_has_no_design_entry`
  (честный fallback для позднего подкрепления без design-записи) и
  `_test_second_transfer_replans_from_refreshed_design_after_fallback`
  (сквозной сценарий из ТРЁХ последовательных передач командования,
  доказывающий, что после fallback design действительно обновляется и
  СЛЕДУЮЩАЯ передача снова перепланирует геометрически, а не повторяет
  fallback).
* Полный прогон headless-тестов ПОДТВЕРЖДЁН в этой сессии через
  Godot v4.3-stable_linux.x86_64 (скачан заново — кэш от прошлых
  проходов в `$HOME` не сохраняется между запусками VM): все 24 файла
  `project/simulation/tests/` зелёные (exit code 0 каждый). Регрессий
  не найдено.

Честно НЕ сделано (открыто дальше): многоуровневая иерархия команд §28
(Fleet/Task Force/Squadron/Division/Element/Ship) не реализована —
сейчас есть только один плоский уровень "guide + members" на формацию;
остальные §29 Formation Orders (change course/speed/formation/attack/
target distribution и т.д.) как явные игровые ПРИКАЗЫ по-прежнему не
существуют как механика — реализовано только пассивное
formation-keeping + его переpланирование при смене ведущего, а не
активная выдача приказов игроком/ИИ; ECM по-прежнему не деградирует
точность/захват PD; COMMUNICATIONS/POWER/STRUCTURAL_INTEGRITY/
DEFENSIVE_SYSTEMS (§25) по-прежнему без consumer'а; Milestone 11+ не
начаты.

## Расследование параллельного исполнения scheduled-задачи — итог этого прохода

По просьбе пользователя проверена конфигурация самого trigger'а
(`list_triggers`, доступно из облачной сессии, не только из
device_bash): зарегистрирован РОВНО ОДИН trigger
(`trig_01MR2jCcsytL2gPbDvhVgkG7`) с cron `59 */3 * * *` — это
ПРАВИЛЬНЫЙ, ожидаемый интервал ровно раз в 3 часа (минута 59: 00:59,
03:59, ..., 21:59), никаких признаков более частого срабатывания на
уровне конфигурации нет. `last_fired_at` этого прохода (21:59:42)
ровно на 3 часа позже характерного времени предыдущих упоминаний
(18:59 и т.д.) — расписание само по себе не является причиной
наблюдавшихся 4 подряд "чужих" коммитов.

Рабочая гипотеза (не доказана, но правдоподобна и не проверялась
предыдущими проходами): `create_trigger`/`list_triggers` не
гарантируют, что предыдущий запуск ЗАВЕРШИЛСЯ до следующего
срабатывания по расписанию — если один проход по какой-то причине
выполняется дольше 3 часов (завис, застрял на скачивании Godot при
плохой сети, долгий тестовый прогон и т.п.), следующее срабатывание
по cron стартует ПАРАЛЛЕЛЬНО с ещё не завершившимся предыдущим,
создавая ровно ту картину, что наблюдалась 4 раза подряд (два
экземпляра, оба видят один и тот же git-репозиторий, оба коммитят
независимо) — без необходимости в "ручных" перезапусках или
неправильном cron. Это НЕ подтверждено (нет доступа к точной
длительности предыдущих сессий из этой сессии), но это самое простое
объяснение, совместимое со всеми фактами (единственный trigger,
корректный cron, `updated_at`≈`last_fired_at` объясняется тем, что
предыдущий проход вызвал `update_trigger` в конце СВОЕЙ работы, что
могло совпасть по времени с ОЧЕРЕДНЫМ штатным cron-срабатыванием, а не
с чем-то аномальным). Рекомендация пользователю: если проходы
регулярно занимают около или больше 3 часов — либо увеличить интервал
cron, либо это стоит просто принять как известную особенность (не
критично: общий git-репозиторий, оба прохода коммитят независимо, как
уже отмечено в предыдущих записях).
